# Code Tour — how to read this codebase

purpose: a learning-oriented map of the codebase and the order to read it in
audience: anyone new to the code, human or agent
updated: 2026-08-21

A learning-oriented map of the code. Every function also has a detailed comment at its top
(what it does, why, how, what's important) — this file is the **reading order** and a
one-line index so you know where to start and how the pieces connect.

## The big picture

The project is **two halves joined by one JSON file**:

```
SymbTr .txt ──(Python)──► Score/Event ──► note-model JSON ──(TypeScript)──► view + edit + sound
            parse           tuning/synth     export            shared core         web harness
```

- **Python side** (`src/`, `scripts/`) = the reference + data/training tooling. Parses the
  dataset, proves the tuning/synthesis, and exports JSON. Not shipped in the app.
- **The JSON file** = the contract. Python writes it (`export_json.py`), TypeScript reads it
  (`types.ts`). They describe the same shape on two sides of the wire.
- **TypeScript side** = the actual app logic, split deliberately:
  - `packages/core` — **portable logic, no platform APIs** (note model, tuning, scheduling,
    notation, measures, tempo, usul, transpose, metadata). Reused *unchanged* by the future mobile app.
  - `apps/web` — the **React web harness, and the first shipped product surface** (the project
    ships web-first — see ROADMAP §1): it renders the core's output (piano-roll + VexFlow sheet)
    and supplies the *platform adapter* (`webAudioBackend.ts` implements the core's
    `AudioBackend` interface). The later mobile app swaps this UI layer and keeps the core.

**Golden rule for reading:** read the *data shape* before the *functions* (nouns before
verbs). Once you know what an `Event` / `NoteEvent` is, the transforms make sense.

## Reading order

### Part A — Python (the reference + the bridge)

| # | File · function | One line |
|---|---|---|
| 1 | [parser.py](../src/symbtr/parser.py) · `EventKind`, `Event`, `Score` | The data shapes. Read these first. |
| 1 | `Event.kind` / `Event.duration_s` | Is a row a note/rest/grace/meta? (grace = çarpma, Kod 8 / Ms 0.) How long is it? |
| 1 | `Score.notes` / `Score.sounding_events` | Notes only vs. notes+rests (why rests matter for timing). |
| 1 | `parse_file` | **Entry point.** File → `Score`. |
| 2 | [tuning.py](../src/audio/tuning.py) · `koma53_to_freq` | The heart: comma number → frequency in Hz. (`cents_above_ref` = a UI/sanity unit.) |
| 3 | [synth.py](../src/audio/synth.py) · `render_score` → `_render_tone` → `_envelope` → `write_wav` | Notes → audio samples → WAV. |
| 4 | [symbtr_to_audio.py](../scripts/symbtr_to_audio.py) · `main` | Ties 1–3 together (the whole Phase-0 flow on one screen). |
| 5 | [export_json.py](../src/symbtr/export_json.py) · `score_to_dict` → `export_file` | **The bridge.** `Score` → note-model JSON for the TS side. |
| 5 | [symbtr_to_json.py](../scripts/symbtr_to_json.py) · `main` | CLI wrapper around the bridge. |

### Part B — TypeScript core (`packages/core`, the portable logic)

[index.ts](../packages/core/src/index.ts) is just a barrel that re-exports every module below;
the web app imports everything from `@turkish-omr/core`.

| # | File · function | One line |
|---|---|---|
| 6 | [types.ts](../packages/core/src/types.ts) · `NoteEvent`, `NoteModelDocument`, `TuningParams` | Same shape as `Event`/`Score`, in TS. Compare to step 5. |
| 7 | [tuning.ts](../packages/core/src/tuning.ts) · `koma53ToFreq`, `freqFromTuning` | Line-for-line port of step 2 (verified to match). `freqFromTuning` uses a doc's own anchor. |
| 8 | [scheduling.ts](../packages/core/src/scheduling.ts) · `buildTimeline`, `AudioBackend` | Events → flat timeline of timed notes; and the audio *contract* (interface). |
| 9 | [tempo.ts](../packages/core/src/tempo.ts) · `estimateWholeNoteMs`, `beatsToMs`, `estimateBpm` | SymbTr stores no tempo — estimate one (median ms÷beats) to convert note-values ↔ ms. |
| 10 | [measures.ts](../packages/core/src/measures.ts) · `assignBars` → `groupMeasures` | Split a score into bars. `assignBars` reads SymbTr's `offset` (integer = a printed barline); `groupMeasures` groups by the resulting `bar`. `isMeasureValid` drives the editor's Save gate. `measureOfEvent` answers "which bar is this event in" over the same grouping — the editor remembers that number so the palette's Çal can start there. |
| 10a | [edits.ts](../packages/core/src/edits.ts) · `spellingOf`, `withPitch`, `withKoma`, `withAlter`, `withDurationBeats`, `scaleDurations`, `toRest`, `toNote`, `nudgePitch`, `deleteEvent`, `insertInMeasure`, `renumber` | **The ONE place a note's pitch or duration is rewritten** — pure functions composing row 11's helpers. `withPitch` moves `koma53`, `noteName`, `noteAE` and `freqHz` together, so an edit cannot half-apply (it used to: the piano-roll patched the koma and left the notehead behind). `nudgePitch` is the drag's ±1 diatonic step, carrying the accidental; `withAlter` is its mirror (the palette's accidental tool: the alteration changes, the staff position does not); `deleteEvent` takes a host's leading graces with it. `insertInMeasure` is `deleteEvent`'s mirror — it splices into a bar and checks no total, because edits absorb and bar lines never move; it resolves the splice point INSIDE the target bar only (grouping cuts a measure wherever the `bar` number changes) and goes in front of a note's leading graces, not between them. ⚠ Ask `insertIndexIn` which index the new event ended up with: `renumber` rebuilds every event, so it cannot be found again by identity. `scaleDurations` is the tuplet tool's whole edit — ×⅔ makes a triplet, ×³⁄₂ removes one, reduced (2/24 → 1/12) with `durationMs` following the beats. ⚠ WHICH notes may be scaled is not here: that is `plainTupletBase` / `tupletRunFrom` / `closedTupletAt` in `tools/render/rhythm.ts`, beside the code that DRAWS the bracket, so the tool cannot offer a triplet the engraver would refuse. `toRest` / `toNote` are the palette's rest tools: `toRest` clears the WHOLE pitch side (koma, both names, freq, lyric), because a rest still claiming Do5 engraves as silence and reads as a note; `toNote` needs a spelling handed in, since a rest has none — the editor takes it from the click's height. |
| 11 | [notation.ts](../packages/core/src/notation.ts) · `parseNoteName`, `komaOf`/`spellNote`/`komaToName`, `accidentalGlyph`, `toAeuAlter`, `deriveKeySignature` | Note name ⇄ staff position + comma + Turkish (AEU) accidental glyph. The sheet view's brain. `toAeuAlter` snaps any alteration to a standard AEU sign **for the engraved staff only** (the editor keeps the exact koma). |
| 12 | [usul.ts](../packages/core/src/usul.ts) · `USULS`, `findUsul`, `buildMetronomeTrack`, `buildPercussionTrack` | Usul (rhythmic-cycle) table, and the **two** tracks one usul produces. `buildMetronomeTrack` clicks the felt BEATS off `pattern` — correct for non-integer usuls (aksak 9/8). `buildPercussionTrack` (2026-08-11, feature track F2) plays the usul's own hand STROKES off the optional `strokes` array — düm/tek/ka — and is a deliberate near-copy of it: same bar walking, same partial-bar rule, so the two cannot drift. ⚠ Strokes are **not** derivable from `pattern`: a grouping says where the pulses are, a usul says which hand plays each, and düyek's `düm te-ke düm tek` puts two inside one group. The tables are six standard forms and four `[derived]` (our reduction of the beat grouping); `tools/core/usul-test.ts` proves them well-formed and cannot prove them right, so **all ten were verified by the owner's ear on 2026-08-11** and accepted. ⚠ Core stops at `PercussionHit {ms, stroke}` and knows nothing about audio files: which drum, which round-robin and how loud all live in the web backend, which is what keeps this file portable to mobile. |
| 12b | [fingering.ts](../packages/core/src/fingering.ts) · `VIOLIN_TUNINGS`, `positionOnString`, `assignFingering` | Where a finger goes on a **fretless** string (feature F3, 2026-08-16). `ratio = 1 − openHz / noteHz` — an exact formula that accepts any frequency, which is why all 53 commas land where they really are and why a twelve-tone app cannot draw a koma position at all. `assignFingering` walks a whole piece once with a greedy *stay near where the hand just was* rule (plus a small cost for changing string and a small pull towards the nut, so it neither thrashes nor climbs). ⚠ It returns **null** rather than a clamped position for a note no string can reach — and that is not an edge case here: Turkish notation transposes down a fourth, so a low written passage sounds under a standard violin's open Sol. ⚠ `openHz` is derived on THIS project's 53-TET grid (a fifth is 31 commas), not from twelve-tone equal temperament — ~4 cents apart, which is a fifth of a koma. Pure data + maths, so it ports to mobile like `usul.ts`; nothing here knows about pixels. Pinned by `tools/core/fingering-test.ts`. |
| 13 | [transpose.ts](../packages/core/src/transpose.ts) · `transpose` | Chromatic transpose of a score (shift koma + re-spell + recompute freq); pitch-augmentation primitive + the harness's transpose/ahenk control. |
| 14 | [metadata.ts](../packages/core/src/metadata.ts) · `scoreHeader`, `makamDisplay`/`formDisplay`/`titleCase` | Format the score's ASCII metadata slugs into a printed Turkish header (makam/form/usul/composer). |
| 14a | [makam.ts](../packages/core/src/makam.ts) · `detectMakam`, `MAKAM_INTONATION`/`MAKAM_KARAR`, `makamKomaDeltas`/`withKomaDeltas`, `signatureKey` | Pipeline stage 9's makam half: guess a decoded page's makam from its derived signature + karar, and bend the SOUNDING komas to that makam's performed intonation (uşşak's segah −1.5 commas, etc.). Audio only — the staff never moves. Table, sources and the deliberate duplications: [mvp/makam.md](mvp/makam.md). |
| 14b | [makamSignatures.ts](../packages/core/src/makamSignatures.ts) · `MAKAM_SIGNATURES` | **GENERATED** from `data/makam_signatures.json` by `scripts/build_makam_signatures.py --ts-out` (re-emit with `--from-json` so a TS refresh cannot rewrite the JSON). The app ships without `data/` and without Python; this is how the table crosses into the browser. |

### Part C — Web harness (`apps/web`, the first product surface + the platform adapter)

| # | File · function | One line |
|---|---|---|
| 15 | [webAudioBackend.ts](../apps/web/src/webAudioBackend.ts) · `play(timeline, fromMs?, opts?)`, `pause`/`resume`/`stop`, `getPositionMs`, `scheduleProgress`, `buildPeriodicWave` | The browser's *implementation* of `AudioBackend` (web's synth.py): notes, seeking, metronome, the usul's percussion, and the audio clock the playhead reads. Since 2026-08-10 (feature track F0) it is a **look-ahead scheduler on ONE long-lived `AudioContext`**: a 100 ms tick schedules the next ~1 s, and `stop()` stops the live source nodes and drops the master gain instead of closing the context — which it had to stop doing before any sample could be cached, since an `AudioBuffer` belongs to the context that decoded it. ⚠ Pausing needs no special case: the tick asks the AUDIO clock where the horizon is, and that clock freezes while suspended. ⚠ `scheduleProgress` exists for the checks only — the playhead keeps gliding if the scheduler dies, so nothing on screen can tell playing from silent. **Since 2026-08-11 the usul strokes are real recordings**: `scheduleStroke` plays a decoded buffer through `percGain` (never `master` — that is the node the volume slider rides), alternating two round-robins so a repeated stroke does not machine-gun, and falls through to the original synthesis when the kit has not loaded. `ensureKit` is awaited by `play()` **with a 1.5 s budget** — a transport control must not wait on a network, and missing the budget costs one playback of fallback rather than silence. ⚠ `percussionInfo()` is the second checks-only hook, for the same reason as the first: a sampled düm and a synthesised one are identical in the DOM, so nothing else could prove the swap happened. ⚠ **`MASTER_GAIN` is 0.72 and a `DynamicsCompressorNode` limiter sits before the destination** (2026-08-11, after the drum clipped audibly): a note is a *normalised* `PeriodicWave` at gain 1.0, so at the old 0.85 the notes alone used 85% of the range and a sustaining drum on top hard-clipped. The limiter (−1 dBFS, hard knee, 20:1, attack 0) is inert on notes alone and exists because `Vuruş sesi` reaches 200% — a user can always ask for more drum than the range holds. **The levels must not lean on it**; raise nothing to compensate. |
| 15b | [audio/strokeKits.ts](../apps/web/src/audio/strokeKits.ts) · `KITS`, `findKit` · [audio/loadStrokeKit.ts](../apps/web/src/audio/loadStrokeKit.ts) · `loadStrokeKit` | The drums F2 plays, and their provenance. **`strokeKits.ts` IS the audio manifest** the swap discipline requires — a `source` and `license` per file, in TypeScript so it is type-checked and sits in the same diff as the file it describes. `loadStrokeKit` fetches and `decodeAudioData`s six files once per page, memoised per kit with the failure reset `omr/session.ts` uses. ⚠ Its base URL comes from **`VITE_AUDIO_URL`**, falling back to `/audio/`, and that indirection is deliberately built ahead of need: 660 KB of drums ships from the app, but F1's voices will not — measured at ~20 MB each, and retrofitting the split later would touch every call site. `MAX_AUDIO_MB` in `prune-dist.mjs` is the mechanical trigger. ⚠ **That prediction was overturned before it fired** (owner, 2026-08-12): the drums stay with the app because percussion is essential, and F1's voices got their own variable instead — so `VITE_AUDIO_URL` must stay unset in a deploy, since it is one base for the whole `audio/` tree and would take the drums to the Hub with it. |
| 15c | [audio/instruments.ts](../apps/web/src/audio/instruments.ts) · `VOICES`, `pickSample` · [audio/loadInstrument.ts](../apps/web/src/audio/loadInstrument.ts) · `loadInstrument` | The instrument voices F1 plays (clarinet, violin, kanun), same manifest-in-TypeScript discipline as `strokeKits.ts`. ⚠ Every number in `VOICES` is **emitted by `scripts/prepare_voices.py`, not typed** — `hz` especially, because VSCO's clarinet filenames name a pitch an octave below what they sound. `pickSample` is the 53-TET adapter: nearest recording by **cents** (not Hz — a semitone is 12 Hz wide at the violin's G string and 120 at the top), then `playbackRate` onto the exact koma; inside the recorded range it never refuses, outside it gives way to the synth. `loadInstrument` copies `graphBytes` in `omr/session.ts` rather than its neighbour `loadStrokeKit` — Cache Storage, sequential fetches, and a memo deleted on settle, all because 20–35 MB behaves differently from 660 KB. ⚠ It escapes each path segment: these filenames contain `#`, which otherwise truncates the URL into a 404 that looks like a missing upload. |
| 16 | [App.tsx](../apps/web/src/App.tsx) · `App`, `loadDoc`, `updateEvent`, `onSaveMeasure`, `onNudgePitch`, `onDeleteNote`, `onPlayPause`, `applyPlayback`, `applyTranspose`, `buildPlayOptions`, `readPageFile` | The glue: owns **all** the state, derives `displayDoc` + the timeline, wires transport/tempo/metronome(usul)/transpose/edit to core + backend. Since the style pass it renders almost no markup itself — the interface is `ui/` below, and App just composes it. ⚠ The doc is **never** set directly: every write goes through `useDocHistory` (row 16e), so nothing can edit off the undo stack. |
| 16e | [useDocHistory.ts](../apps/web/src/useDocHistory.ts) · `useDocHistory` | Undo/redo for the score. `reset` installs a loaded file (clearing history), `apply(fn, {coalesce})` makes an edit — same-keyed edits inside 600 ms merge into ONE entry, so a pitch drag — on the sheet or in the piano roll — undoes as one gesture, not as forty frames. ⚠ Undo restores the document, not the selection: deletes renumber `index`, so `App` clears the selection instead of translating it. |
| 16a | [ui/status.ts](../apps/web/src/ui/status.ts) · `OmrStatus`, `busy`, `readingStatus`, `pageSummary`, `stripsSummary`, `whereOf` | **The DOM contract the deploy checks assert on.** A read's *facts* (`state`, `kind`, `where`, counts), kept apart from the *sentence* saying so, and rendered onto `#omr-status` as `data-*` by `StatusLine`. Read this before touching any smoke tool or any user-visible wording. |
| 16b | [ui/strings.ts](../apps/web/src/ui/strings.ts) · `TR`, `num` | Every user-visible word, in one file, in Turkish. No i18n framework on purpose: one language, and one module means the product's whole voice is reviewable in a single diff. |
| 16c | [ui/](../apps/web/src/ui/) · `UploadHero`, `ReadProgress`, `TransportBar`, `ScoreCard`, `EditPalette`, `accidentals.ts`, `AdvancedPanel`, `DecodePanel`, `StripPanel`, `Segmented`, `StatusLine`, `ErrorNote`, `PitchRangeNote`, `errors.ts` | The interface. `UploadHero` owns `#page-input` and takes click/drop/paste into one read path. `TransportBar` carries two clusters: **listening** (play/stop, tempo, metronome, usul, makam) and, since 2026-08-08, **reading** — transposition (in KOMAS, labelled by scale degree where one lands: "4 ses (22 koma)"), keep-the-staff, and the accidental mode. Those three came UP out of `Gelişmiş` because they change what a player sees and hears. `AdvancedPanel` is the collapsed `Gelişmiş` and holds `#strips-input` (⚠ `app-smoke` must open `#advanced` before setting files on it). `DecodePanel` (`#decode-panel`) shows **the model's raw tokens for the last page read** — every id, its vocabulary spelling and its logprob, plus the stitcher's warnings — from App's `rawDecode`, which exists only because `stitchDecoded` consumes the tokens and returns a document, so nothing downstream keeps them. ⚠ It is NOT `StripPanel`, which re-serializes the score currently DRAWN; only this one shows what the model said. `EditPalette` is the editor's **armed-tool** column (`#edit-palette[data-armed]`, `[data-tool]` per button), rendered by `ScoreCard` as a **sibling** of `.kv-score` — never inside it. It also carries its own **Çal/Dur** (`#palette-play` / `#palette-stop`, and `[data-play-from]` naming the bar), which plays from the **last edited bar**: in edit mode a sheet click selects or inserts, so click-a-bar-to-play is off and this replaces it. ⚠ `#palette-play` and the transport's `#play` both carry `data-play-state`. ⚠ Its glyph spans are `pointer-events: none`: a Bravura note glyph paints outside its em box, and one tool's ink over another meant a click armed the neighbouring value (clipping the ink instead cut the stems off, so the buttons are taller rather than clipped). Edit mode also widens the page by the palette's footprint — the engraved sheet is a fixed 1020 px and cannot shrink to make room. `accidentals.ts` is the single list both the palette and the modal's picker offer. It carries six note values, six **rests** (`data-tool="rest:1/4"` — the same tool with `rest: true`, so insert/apply do not fork), all **thirteen** alterations (the AEU signs, natural, and the numbered ±2/±3, each with its own Bravura sign) and the **tuplet** (SMuFL `tuplet3`, `data-tool="tuplet"`) — the one two-click tool, so it is also the only one with a state between clicks (`tupletAnchor` in `App`, surfaced as `#sheet-surface[data-tuplet-anchor]`). |
| 16d | [styles/](../apps/web/src/styles/) · `tokens.css`, `base.css`, `app.css` | The design system: `:root` custom properties, element defaults + the Bravura `@font-face`, then the `.kv-*` components. ⚠ No selector may reach inside `.kv-score svg` and no transform may touch that container — `tools/render/render.ts` crops training strips from it by rect. |
| 17 | [PianoRoll.tsx](../apps/web/src/PianoRoll.tsx) · `xOf`/`yOf`/`yToKoma`, draw effect, pointer handlers | Canvas piano-roll: x=time, y=comma. Drag a note to change pitch; drag its right edge for duration. |
| 17b | [Fingerboard.tsx](../apps/web/src/Fingerboard.tsx) · `Fingerboard` · [ui/fingerboardGeometry.ts](../apps/web/src/ui/fingerboardGeometry.ts) · `STRING_LINES`, `toDisplay`, `pointOnString`, `fingerboardOutline` | The third view (F3): a violin neck with a dot that follows playback, plus a tick at every position **this score** uses on each string — so the uneven, microtonal spacing on screen is the music's own. The photo is raster, everything over it is SVG. ⚠ Two rules it lives inside: it is driven by `getPositionMs()`, the same audio clock as the sheet's playhead, so it cannot drift from the sound; and it takes App's `timeline` as a prop and **never calls `buildTimeline`** — that is the one place the makam bend and the transpose are already in `freqHz`, and `PianoRoll` above does rebuild it (harmless for a roll, wrong for a finger position). For the same reason nothing is labelled with `noteName`, which `withKomaDeltas` leaves stale. `fingerboardGeometry.ts` holds every pixel: the strings were tracked down the image and line-fitted to 0.06–0.10 px residuals, and the nut→bridge run scales to 328 mm, so the calibration is checkable rather than fitted. Swapping in a better photo is a data change. ⚠ The rotation that lays the neck down is an SVG transform on an inner `<g>`, never CSS on a container — `.kv-score` is screenshotted by rect. |
| 18 | [SheetView.tsx](../apps/web/src/SheetView.tsx) · `SheetView`, `vexDuration`, `buildStaveNotes`, `drawSignature`, `drawLyrics`, `drawThinSharps`, `drawSlurArc`, `drawStaccatoDot` | VexFlow-engraved staff: real stems/beams/dots + AEU-only accidentals, triplet brackets / tie arcs / slashed grace notes (from `rhythm.ts`, strips_v2_2), justified rows, a lyric line (syllables/melisma/hyphens), an engraved header, a playhead cursor, click-to-seek, and (edit mode) an HTML overlay of clickable **measures and notes** — `NoteBox[]`, recorded in the same walk that feeds the playhead, drives select / ✕ / drag-to-change-pitch (pointer events with `setPointerCapture`, since the note leaves the pointer on the first step). A click on **blank staff** with a note value armed inserts one instead: `pitchAtHeight` reads the staff position off `MeasureBox.topLineY` (VexFlow's treble line 0 is F5, one diatonic step per half staff space) and takes its alteration from the drawn key signature, and a **ghost oval** previews it — moved by mutating the element, never through state, and carrying `data-insert-pitch` so a check can prove the preview and the insert share one mapping. ⚠ The ghost is a CSS oval, not a Bravura glyph: nothing under `.kv-score` may set a font. With the **tuplet** armed each note target carries `data-tuplet="start|member|anchor|end|blocked"`, and a `blocked` one is `pointer-events: none` — that is what makes an invalid target unclickable rather than merely unresponsive. The overlay also carries the **off-meter mark** (`[data-omr="bar-warning"]`, `data-bar-fill="over|under"`): each bar's `measureBeats` against the DERIVED meter — never `Measure.lengthBeats`, which is computed from the bar's own contents and so is true by construction — edit mode only, and the first and last bar are exempt when short. ⚠ The overlay is a **sibling of the SVG**, never inside it, and the per-note rects deliberately do **not** ride on `onLayout` (that payload is the training-strip crop contract). Three accidental modes (`every` / `keysig` / `measure`=carry — the strip renderer's `mode`). `drawThinSharps` (opt-in, `?thinsharps=1`) redraws the four AEU sharps as SVG at real-print bar weight — the 2026-07-26 fidelity fix that stopped küçük's three bars fusing into a koma; see [STATUS.md](STATUS.md) / [rung3/round2.md](rung3/round2.md). Two **label-free distractors** are drawn as raw SVG here rather than as VexFlow modifiers, because a modifier feeds `StaveNote.getBoundingBox()` and would move every note box: `drawSlurArc` (`?slurseed=`) teaches that an arc alone is not a `\tup3`, and `drawStaccatoDot` (`?staccatoseed=`, 2026-08-15) teaches that a dot means *longer* only BESIDE the notehead — the augmentation-dot confusion. ⚠ `getNoteHeadBounds()` returns the notehead's **anchor**, not its ink edges, which is what made two placement drafts fuse the dot into the notehead; see [rung3/levers.md](rung3/levers.md) Lever 6. |
| 19a | [MakamModal.tsx](../apps/web/src/MakamModal.tsx) · `MakamModal` | The post-decode makam prompt: shows the guess **and its evidence** (matched signature, karar, makams sharing that signature), lets the user change it or choose "play as written". ⚠ Its backdrop blocks clicks, so every smoke check that decodes then clicks calls `tools/browser/makamPrompt.ts` first. |

[main.tsx](../apps/web/src/main.tsx) just mounts React — you can ignore it.

### Part D — ML tooling, Phase 2 (synthetic) and Phase 3 (real pages) (skim after A–C)

Added after Phase 1; not part of the core→harness spine above.

| File(s) | One line |
|---|---|
| [tools/render/](../tools/render/) | Synthetic-data generator: `lilypond.ts` serializes note-model strips to the OMR model's LilyPond label format, `render.ts` (Playwright) crops PNG+label pairs from the live harness render, `repeats.ts` detects + injects repeat signs, `navmarks.ts` injects navigation marks (segno/coda/D.C./Son), `rhythm.ts` recovers triplet groups + tie-splits from the exact durations (real data, no injection — `\tup3`/`\tupend`/`\tie`; strips_v2_2), and since 2026-08-08 also answers the editor's *which notes can be a triplet* (`plainTupletBase`, `tupletRunFrom`, `closedTupletAt`) so the tool and the engraver cannot disagree, `respell.ts` seeds the büyük-enharmonic respell, `decode.ts`/`decode-cli.ts` verify labels, `labels-cli.ts` serves labels for a real page's measure ranges (the Rung-3 emitter's TS half), `stitch.ts`/`stitch-cli.ts` are the Rung-4 stage-8 STITCHER (decoded page tokens → editable note model: `\sig` resolution, tie/tuplet/grace fold-back, repeat/volta/da-capo expansion; `stitch-test.ts` = structure unit tests + the 194-score label round-trip). Browser side: `apps/web/src/stripExport.ts` (crop rects + labels from the sheet layout), `textNoise.ts` (seeded distractor text). Since `strips_v3` (2026-07-21) the renderer is **carry-dominant** — `mode: "measure"` (key signature + measure-scoped carry, i.e. how real pages print) with conventional per-makam signatures from `data/makam_signatures.json`, plus label-free **slur distractors** (`slurseed`) so an arc alone doesn't read as a triplet, and since 2026-08-15 opt-in **staccato distractors** (`--staccato-noise` / `staccatoseed`) so a dot away from the notehead doesn't read as an augmentation dot; `--thin-sharps` turns on the real-print sharp weight. Since 2026-08-19 there is a THIRD tuplet mark, opt-in behind `--concave-tuplet` (`?concavetuplet=1` → `drawTupletArcConcave`): a **continuous** arc with the "3" hanging inside its concavity, on a per-piece coin, measured off two real scanned editions the owner supplied — the counterexample to this project's own "16 of 16 marks break the arc" ([METRICS-TUPLETS.md](METRICS-TUPLETS.md)). ⚠ It is off by default and **no trained arm may carry it**: it changes a share of every piece, which is exactly how `--print-noise` once rode along unconditionally into ~40k strips. `verify-labels.ts` is the PIXELS-vs-LABELS gate (2026-07-26): it re-opens every job from the corpus manifest, reads the accidental glyphs out of the live SVG (SMuFL codepoint, or stem/bar count for the redrawn AEU sharps) and checks each crop rect's glyphs against that strip's label — the check whose absence let the `sigTolerant` defect ship. ⚠ It needs the dev server running (`npm run dev:web`) and it inspects **accidental glyphs only** — it cannot see a rhythm or duration change. Until 2026-08-05 a run that checked **zero** strips exited 0 and read as a pass (`checked - ok > 0` is false at 0/0), which was observed for real: 1,019 jobs "verified", 0 strips checked, exit 0, dev server down. It now fails loudly at 0 and prints `PASS` only when it actually verified something. **Read [tools/render/README.md](../tools/render/README.md) — it's the full tour of this directory.** |
| [tools/render/ly-*.ts](../tools/render/) | **The SECOND ENGRAVER (Round 3, Lever 4 — built 2026-08-18).** Real LilyPond renders the same labels, so a second visual domain costs no new labelling. `ly-engrave.ts` translates a strip LABEL into LilyPond source — it re-decides nothing: a note the label marks is written at that alteration and forced with `!`, a note it leaves bare is written at the drawn signature's alteration under `\accidentalStyle "forget"`, so LilyPond's own accidental engine cannot add or drop a sign; the `\time` per measure comes from `deriveTimeSignature` (LilyPond BEAMS by it) and every measure carries a bar check. It THROWS on repeat/volta/nav tokens rather than approximating them. `ly-svg.ts` reads a LilyPond SVG — staff lines, and glyph identity **by font outline**, self-calibrated by rendering the nine signs (twice: Emmentaler is an optical-size family, so a grace note's accidental is a different outline — a full-size-only table silently missed them). `render-ly.ts` is the corpus arm: labels from `labels-cli.ts --ranges`, one LilyPond run per piece, one SVG page per strip, cropped by Playwright at the corpus geometry (336 px tall, 30 px staff spacing). `verify-labels-ly.ts` is this arm's PIXELS-vs-LABELS gate — it re-engraves from the manifest and compares drawn accidentals with label tokens in reading order. Numbers and the null domain-gap result: [METRICS-ENGRAVER.md](METRICS-ENGRAVER.md). |
| [tools/core/](../tools/core/) | **Node-only unit suites for `packages/core`, run by `npm test`.** `edits-test.ts` covers the edit primitives and round-trips every note in the bundled scores through `spellingOf` → `withPitch`. `usul-test.ts` (2026-08-11) covers the usul stroke tables and `buildPercussionTrack` — that strokes sit inside their cycle, ascend and open on a düm; that a short final bar drops what runs past it; that a usul with no table is silent rather than invented; and that a stroke and a metronome click naming the same position land on the same millisecond. ⚠ It checks the tables are **well-formed, never that they are musically right** — that is the owner's ear, [MANUAL_CHECKS.md](MANUAL_CHECKS.md). |
| [tools/browser/](../tools/browser/) | `run-page.ts` — runs any harness page in headless Chromium (Vite node API + Playwright) and asserts on the global it exposes, turning pages that were checked by eye into commands: `npm run gate:browser` (the ONNX gate), `npm run probe:cv` (the opencv.js parity probe). `--expect P/T` tallies the page's own ✓/✗ marks instead of trusting a boolean — the OMR gate reports a single `FAIL` while the known ORT-web int8 `\tup3` wobble exists, so only a pinned tally (27/28) can tell that from a real regression. `app-smoke.ts` (`npm run smoke:app`) drives the REAL app with Playwright — sets the "Read strips" input to a page's crops, waits for the read, switches to Sheet, plays, and downloads the saved JSON — covering the `onStrips` → `loadDoc` → render → transport wiring a headless pipeline test cannot reach. `page-smoke.ts` (`npm run smoke:page`) is the W7 twin: it uploads ONE PAGE IMAGE, so the browser slicer runs inside the app, and gates the strip count against `slicer_ref.py` (⚠ not the page's `_manifest.json` — that would fail W7 for a W4 reason) plus a **responsiveness** bar, since a tab that cannot answer JS during the 35 s sweep is not shippable. It prints every status line with a timestamp: on a minutes-long check a stall and a slow run look identical otherwise. Since W9 it also asserts **where the decode ran**: with `VITE_DECODE_URL` set the summary must say the server did it, and pointing that variable at a dead port is how the FALLBACK is exercised — same page, same score, on the user's own machine. The console error the browser logs for the refused request is ignored by host match only, so a real error in the fallback path still fails the run. `build-smoke.ts` (`npm run smoke:build`) builds and drives `dist/` itself, with the weights on a second origin; `--weights-url` points it at the REAL Hub instead of the local stand-in, which matters because the Hub reflects the caller's origin behind a 307 rather than sending `*`. `live-smoke.ts` (`npm run smoke:live`) drives the DEPLOYED site — since `ALLOWED_ORIGINS` was set, a localhost preview is refused by the decode server, so this is the only run left that exercises the shipped configuration: Netlify's own headers, the origin lock satisfied rather than bypassed, and the weights fetched from the Hub over the real network on the fallback. `coldstart-smoke.ts` (`npm run check:coldstart`) puts a deliberately-cold proxy in front of a real decode server, so the cold window is a parameter rather than a race: one run proves a page still lands on the SERVER after a 12 s boot (the 2026-08-08 `remote.ts` retry), a second proves a load that genuinely FAILED is *not* waited on — counted as `/health` polls that must not happen, because "retry every 503" would strand a user behind a broken container for the whole 40 s budget. ⚠ It is the only check that can see this at all; every other one runs against an already-warm server, which is why `smoke:live` was the thing that found the bug. `makamPrompt.ts` is the shared dismissal for the post-decode makam modal — its backdrop covers the viewport, so `app-smoke` and `page-smoke` would fail on "element intercepts pointer events" at their first click after a decode; one helper keeps the two agreed on the element ids. |
| [apps/web/src/slices/](../apps/web/src/slices/) | **Slice inspector** (`/slices.html`, owner request 2026-08-05) — upload a page, see the crops the slicer cut from it. A SEPARATE page from the app on purpose: it loads no model and builds no score, so it cannot disturb what the editor has open, and it is quick (~1.6 s to slice against ~19 s to read). Runs the same `slicePage` the app's "Read page" button runs, so the crops shown ARE what the model would be fed; each one is captioned with the slicer's own decisions (row/crop index, the `row_x0`–`row_x1` span, the pads, which measures of the row it covers, `split_wide`). Strips and pages are deletable and downloadable; everything is in memory for the session only. Each crop also prints its VERTICAL PLACEMENT — `music 3.3↑/1.9↓ sp, frame 4.60↑/2.60↓`, in red with the shortfall when a side is cut — so a badly-cut strip explains itself instead of merely looking wrong. Strips are decoded **automatically** as soon as a page is sliced (untick *read with the model* to slice only, ~1.6 s a page against ~1.2 s a strip — it was on-demand at first and the button simply was not found, which was the right verdict on that design) and prints each strip's label under it **with note names substituted** (`b'16` → `si'16`, the same solfège the note model uses), the model's RAW tokens beneath it, and the strip's `minLogprob` beside it. Both spellings are shown on purpose: only the raw one can be pasted into the stitcher or diffed against Python. |
| [apps/web/src/omr/](../apps/web/src/omr/) | **The in-browser OMR runtime** — the product decode path, extracted from the Rung-1.5 gate harness at MVP rung W1 so React can use it, and **since W9 it is also what the decode server runs**. `decode.ts` (`greedyDecode` = encoder once → first-step decoder → decoder-with-past loop, the JS port of `onnx_parity.onnx_greedy_decode`; `argmaxLast` also returns each token's log-probability; `decodeStrip` packages ids + tokens + `hitCap`/`minLogprob`/`meanLogprob`). ⚠ It imports **no ORT runtime**, only types from `onnxruntime-common`: adding `import * as ort from "onnxruntime-web"` back would break the server at import time, which is the whole reason it is gone. The `Tensor` constructor arrives on `Sessions` instead, and the encoder is separable from the greedy loop (`runEncoder` / `decodeFromHidden` / `sliceHidden`) so the server can run ONE batched encoder pass over a page while the browser passes n = 1; `summarizeDecode` holds the scoring rules once for both. `preprocess.ts` (`preprocessToCanvas` = DonutImageProcessor steps 1–4 on canvas — the `Math.trunc` calls transliterate HF's `int()` steps and are load-bearing; `sourceSize`/`canvasFromImageData` let the slicer's canvases feed it), `pixels.ts` (step 5, the rescale, DOM-free — **the client/server seam**: the client does the geometry and uploads the finished 409×583 PNG, the server applies only this, so there is no second resampler to prove equal to the canvas one), `remote.ts` (`decodeStripsRouted` — server first when `VITE_DECODE_URL` is set, this machine otherwise or when the server does not answer; the weights are fetched only if the fallback fires, and a user's own abort is deliberately NOT a fallback trigger), `session.ts` (`getMeta` is split from `getModel` so the server path can read `preprocess.size` without pulling 211 MB), `types.ts` (`Sessions`, `ModelMeta`, `StripDecode`), `page.ts` (**MVP W7** — page image → `decodeGray` → guarded skew → `sliceStage3` → crops as canvases in `StripInput` shape; the twenty lines between the slicer and the decode path, plus the progress and event-loop yields a UI needs and a parity harness does not. Import it LAZILY — it pulls ~13 MB of opencv.js), and **`slicer/`** — the TypeScript port of `page_to_strips.py` (MVP W4–W6, guide in [mvp/slicer-port.md](mvp/slicer-port.md)): `constants.ts` (every constant with the Python line it came from, plus `pyRound()` — Python rounds half-to-even and `Math.round` does not, which silently retunes two barline gates), `cvOps.ts` (**the only opencv.js importer**; everything else works on plain typed arrays so the transliteration diffs against the numpy — plus `openHorizontalRowSums`, the closed form of a `len`×1 MORPH_OPEN's row sums, which took the skew sweep's per-call cost 856 ms → 6.8 ms with no change in output. ⚠ Its border rule is the subtle part: `morphologyEx` erodes as if outside the frame were foreground, so an edge-touching run survives whole however short it is. `npm run check:deskew` is what holds the two implementations identical), `prepPage.ts` (`estimate_skew`/`deskew` ported — NOT the no-op the plan assumed, 15.3% of corpus pages take a real rotation — with `detect_page_quad`/`crop_to_page` left as the camera seam. The sweep is a **generator** with two drivers, `estimateSkew` (sync, the parity path) and `estimateSkewAsync` (yields between rotations so the tab survives its 35 s); `guardedAngle` holds `deskew`'s two guards so a caller that estimates ahead of time decides identically; the skew sweep takes its binarizer from `pageBinarizer` **once** rather than per rotation, which is both correctness and 41× the cost), `staves.ts` (`detectStaves` plus `staffLineRows` — the candidate-row half, split out so the pale-line guard is measured against the **same** signal `detectStaves` gates on — and `pageBinarizer`/`binarizePageInk`, the page-level choice between Otsu and a paper-relative threshold for pages whose staff lines are too pale for a two-class split. ⚠ PAGE level only: `binarizeInk` is still what the barline gates call per row, where the guard is meaningless because one row holds one staff. The two order statistics it rests on, `percentileUint8` and `medianUint8AtLeast`, are pinned against numpy by `tools/vision/binarize-test.ts` in `npm test`), `rows.ts`, `barlines.ts` (the riskiest file — `_is_thin_stroke`'s staff-row `continue` does NOT reset the run, `_terminal_overshoot`'s inner walk carries four state variables with four different reset rules, and the three per-ROW `binarize_ink` calls must not be hoisted into one Otsu; `hasNotehead` lives here but is only called by W6's windowing), `windows.ts` (`spanCap`/`splitWide`/`windowMeasures` — the measure packing; `_split_wide`'s `sorted(set(cuts))` can collapse two targets onto one gutter and the escalation loop DEPENDS on that, Python's `min(near)` compares tuples so JS needs an explicit second key, and **budget mode WAS not ported at all — it now is, opt-in only**: `rowCostFeatures`/`estimateTokens` port `row_cost_features`/`estimate_tokens`, and `windowMeasures` takes an optional `tokenBudget` that only `?dense=` sets. ⚠ It is an experiment with **unverified browser-vs-Python parity** and it is reached from nowhere else; the marked block, the option and the `?dense=` read in `App.tsx` delete together — [METRICS-SLICER-WINDOWS.md](METRICS-SLICER-WINDOWS.md)), `slicer.ts` (`sliceStage1`/`sliceStage2`/`sliceStage3` — the last adds the driver's pad/trim and returns crop SPANS, with `cropStrip` cutting pixels only when a caller wants them). ⚠ It is a **transliteration**: do not restructure, do not "improve" it, and do not optimise the 41-rotation skew sweep here even though it costs ~35 s/page. **Two rules:** nothing here may touch the DOM by id (that import-time `getElementById` is what made the gate unimportable), and this is deliberately NOT `packages/core`, which ROADMAP §2 keeps free of platform APIs. |
| [apps/web/tools/](../apps/web/tools/) | The two steps that turn the harness into a **deployable site** (W9 hosting). `copy-ort.mjs` puts ORT's threaded runtime — `ort-wasm-simd-threaded.jsep.mjs` *and* its `.wasm` — in `public/ort/` so `omr/ortEnv.ts` can set `wasmPaths = "/ort/"`. That pair is not cosmetic: left to the bundler the glue gets inlined, ORT spawns its worker from bundled code that touches `document`, and **every session creation in the production build hangs** — the fallback froze forever and dev could not show it. A `?url` deep import was tried first and fails, because ORT's package `exports` does not expose `./dist/*.wasm`. `prune-dist.mjs` then removes what only a checkout needs (the 332 MB of graphs, the render corpus, the probe dumps, the now-duplicate bundled wasm, and since 2026-08-08 the third-party-derived scores) and **fails the build** if the result crosses 60 MB, contains an `.onnx`, has **any `.json` at the dist root**, or carries audio outside `audio/` or over 1 MB in total (2026-08-11). The assertion is the point, not the deletion: forgetting to delete a directory is easy, and a failing build cannot regress quietly. The root-`.json` rule is the copyright guard — everything under `public/` is served to whoever guesses the name, so an unlinked score is still published; a name list would miss a new one, and `fonts/glyphnames.json` sits a level down so it needs no exception ([THIRD-PARTY.md](THIRD-PARTY.md)). |
| [apps/server/](../apps/server/) | **The decode server (MVP W9)** — Node + `onnxruntime-node`, and the load-bearing fact about it is what it does NOT contain: no decoder. `decodeBatch.ts` imports `apps/web/src/omr/decode.ts`, the browser's own module, so the greedy loop, the stopping rule and the logprob scoring are the same lines on both sides rather than a third implementation to hold in parity (the bill the slicer port paid over three rungs). `model.ts` is the only file that differs between the two runtimes — it creates the sessions and puts ORT's `Tensor` constructor on them, which is what keeps `decode.ts` free of a runtime import. `index.ts` is the HTTP surface: `POST /decode` takes base64 PNGs that are ALREADY rotated/resized/padded by the client (the seam is `apps/web/src/omr/pixels.ts`, and it is there so the server never needs a second resampler), `GET /health` reports `ready` so the client's fallback can tell "cold" from "broken". The model loads at BOOT, after the port is bound — Cloud Run routes as soon as the port opens, and a container that binds late reads as a failed start. Decodes are serialized in-process (`runExclusive`): ORT already uses every thread it is given, so a second concurrent page buys no throughput and doubles peak memory. `limits.ts` holds the safety caps as data (body bytes, strips per request, per-IP rate) so `npm run check:limits` can assert them; `png.ts` reads IHDR *before* decoding, because a 20,000 × 20,000 PNG is a few KB on the wire and 1.6 GB in memory. `Dockerfile` builds from the REPO ROOT (it has to — the bundle reaches into `apps/web/src`), `cloudbuild.yaml` exists because that means the Dockerfile is not at the context root, and `.gcloudignore` exists because `apps/web/public/models/` is gitignored and Cloud Build would otherwise upload a context with no weights in it. ⚠ Not deployed yet — see [mvp/deploy.md](mvp/deploy.md). |
| [tools/vision/parity/](../tools/vision/parity/) | Browser-vs-Python decode parity. `arm-b.ts` decodes **Python's own crops** in headless Chromium and diffs against the `strips_v2` decode caches — the **ceiling** the ported slicer is judged against at W6 ("within 1 pp", never 100%), since a canvas draw is not PIL BILINEAR. ⚠ It compares token streams **after** the stitcher's `normalizeTokens`: the two sides serialize differently (`\sig\komaFlatb` vs `\sig \komaFlat b`) and comparing raw strings put the ceiling at 10% instead of 96.7%. `--json` dumps per-strip agreement with crop width and confidence, which is how the "wider crops resample worse" hypothesis was killed. `edge-cases.ts` proves non-strip images (blank, black, 8×8, wrong orientation) return a loadable doc instead of throwing. `slicer-parity.ts` (`npm run parity:slicer`) scores the ported slicer against **local Python** — `scripts/slicer_ref.py`, which is both the control arm and the sample definition, since the `strips_v2` manifests are NOT reproducible by the current code (1,680/1,704) and scoring against them failed pages where the port matched Python line for line. `--inject-skew` skips the 41-rotation sweep by feeding Python's angle in, which is what makes a full-corpus run affordable. Since W5 it also gates the bar count/positions and reports **reject-list agreement** — stricter than the bar list, and the only check that can see two gates both sitting near their thresholds on one column. W6 adds the strip entries (count, window fields, `row_x0`/`row_x1`, the width/measure invariants) plus how often the two rare paths fired, since a pass would otherwise imply an exercise it never got. `deskew-check.ts` (`npm run check:deskew`) runs BOTH `qualifyingLineRows` implementations on the same rotated image at every angle the coarse pass evaluates, over real pages, and requires zero disagreements — the exactness claim that lets the sweep be 126× cheaper without re-earning W4-W6. `arm-a.ts` (`npm run parity:arma`) is the decode verdict: it slices a page with the PORT, decodes those crops, decodes Python's crops for the same page, and compares the two arms **pairwise on the same (system, window)** with a McNemar exact test — ⚠ never against arm B's 86.0% *level*, which at n=450 has a ~1.6 pp standard error and cannot resolve the 1 pp bar the plan originally asked for. **W9 adds the server arm:** `server-parity.ts` (`npm run parity:server`) has the browser preprocess each crop and hand back BOTH the PNG it would upload and the ids it read, then sends those same bytes to the server — PNG is lossless, so both runtimes see identical pixels and nothing but ORT can differ. It prints the browser's logprob AT the diverging token, which is what separates "the two runtimes tipped a near-tie" from "one of them is wrong", and `--replay <fixture>` re-runs the server half with no browser at all (seconds, not minutes) — the only reason the batch-8-vs-batch-1 control was affordable. `server-bench.ts` (`npm run bench:server`) replays a fixture for **vCPU-seconds from the server's own `process.cpuUsage()`**, which is what Cloud Run bills and is not the wall time once threads > 1, plus the crops-vs-page-image payload comparison. `server-limits.ts` (`npm run check:limits`) runs the deploy.md safety checklist against a live server and prints the two items it CANNOT check (the billing cap, `--max-instances`) instead of counting them as passed. `decode-pool.ts --server <url>` decodes the gold pool on the server so quality is scored against `_realval_v2` with the same scorer as the browser — agreement cannot say which side is right, and that is the question a release turns on. |
| [tools/vision/page-structure.ts](../tools/vision/page-structure.ts) | **Per-page stitch stats over the whole real corpus** (2026-08-18). Runs the SHIPPED stitcher and core helpers — `stitchStrips` with `expand:false`, `deriveTimeSignature`, `measureBeats` — over every `*_decode.json` and reports, per page, written measures, notes, stitch warnings, and how many interior bars are **off-meter**. It is TypeScript for one reason: re-deriving "is this bar off-meter" in Python would be a second implementation of a rule the editor draws, and this repo has paid for that class of duplication before. ⚠ It also reports **`modeShare`** beside the derived meter, because `deriveTimeSignature` takes the *modal* bar length and on a badly decoded page that mode is a coin flip — without it, "off-meter" counts the derivation failing rather than the music (symptom: derived meters of 2/8 and 3/8, which no Turkish usul uses). Corpus result — 37.8% of interior bars — is in [METRICS-DIAGNOSTICS.md](METRICS-DIAGNOSTICS.md), and it is what tempered the error-localisation claim in [mvp/editor.md](mvp/editor.md). Consumed by `scripts/rung3/build_page_queue.py`. |
| [apps/web/src/checks/](../apps/web/src/checks/) | `stripsHarness.ts` (+ `apps/web/strips-harness.html`) — headless entry to the *shipped* decode path for the parity tools; deliberately a browser page, not a Node lookalike, because the canvas resampler only exists in a browser. `serverParityHarness.ts` + `apps/web/server-parity.html` — the CLIENT half of the decode-server contract (W9): for each strip it returns both the PNG bytes a real client would POST and what the browser itself read, so the server can be handed identical pixels and the comparison is about ORT alone. `preprocessOnly` skips the browser decode and loads no weights, which is how the gold pool gets preprocessed for the server arm without paying for a second decode. `logprobCheck.ts` + `apps/web/logprob-check.html` + `scripts/logprob_ref.py` — proves the browser's confidence signal transfers before W8 builds highlighting on it. Both sides decode the gate's `.pixels.bin` reference tensors, so the image pipeline is out of the picture. It does **not** assert per-token equality: ORT-web and ORT-Python int8 disagree by up to 8.6e-2 (the documented numerics gap), so it asserts that every token lands on the same side of the validated `min_logprob < -1.0` threshold. `npm run check:logprobs`. `slicerHarness.ts` + `apps/web/slicer-harness.html` — headless entry to the ported slicer; `stage1` returns per-system geometry and the strip entries but never pixels, because a 10-system page normalizes to ~10 MB of row images and serializing those over CDP would dominate the run. Its `crops` entry point is the exception and exists only for `arm-a.ts`, which has to decode what the port actually cut. |
| [apps/web/src/probe/](../apps/web/src/probe/) | `cvProbe.ts` + `apps/web/cv-probe.html` + `scripts/cv_probe_ref.py` — written as throwaway (the plan had it deleted at MVP rung W6) and **kept**: it is the only check that would catch an opencv.js version bump changing a primitive under the port, and it costs one command. It proves opencv.js reproduces OpenCV-Python's Otsu / MORPH_OPEN / connectedComponents / INTER_AREA before the slicer is ported. Two arms: fed Python's own grayscale bytes it must be *exact* (algorithm parity); fed the browser's PNG decode it only drifts (`imread(IMREAD_GRAYSCALE)` converts inside the decoder, which a browser cannot reach). See [mvp/README.md](mvp/README.md). |
| [scripts/](../scripts/) | Corpus building (Python, never shipped): `select_pieces.py` picks the (piece, transpose) render jobs by greedy max-min AEU coverage — since 2026-07-26 it also EXTENDS an existing selection (`--keep`, `--boost-class`, `--per-makam-cap`, `--sig-table`) instead of re-rolling it, and refuses exam pieces by SymbTr id (`--exam`); `export_scores.py` writes the picked pieces to `apps/web/public/scores/`; `build_makam_signatures.py` learns the conventional printed signature per makam from the adjudicated real labels (AEU theory only as fallback); `make_split.py` splits BY PIECE; `symbtr_to_json.py` / `symbtr_to_audio.py` are the SymbTr readers; `check_docs.py` guards these docs. `prepare_strokes.py` (2026-08-11, feature track F2) downloads the CC0 VCSL drum recordings and prepares F2's stroke samples — ⚠ its interesting part is that it **identifies** the strokes rather than trusting file names: VCSL numbers the darbuka's five articulations without saying which is the open centre hit, so each is measured (sub-200 Hz energy, spectral centroid, decay) and the mapping follows the physics, with the self-describing frame drum as the control that validates the metric. It also excludes VCSL's two `Hand` takes on measurement (40+ dB below the struck ones) and levels the three strokes to fixed targets rather than inheriting the session's. `prepare_voices.py` (2026-08-13, F1) does the same job for the instrument voices and is deliberately the *opposite* kind of script: it **copies rather than prepares** (the VSCO voices ship byte-identical, verified by sha256) and spends its effort on measurement instead. ⚠ Since 2026-08-14 it has a **second acquisition path** for the kanun, which arrives as one 2-minute take rather than one file per note: it splits at the onsets, and the split is trusted because the measured pitches must form a chromatic run — a splitter's mistakes are invisible file by file and only show up between neighbours. That path also measures a *plucked* string differently (spectrum not period, attack at the rise not the first sample); both differences were found by ear, and both now have guards. `tools/audio/serve-voices.ts` (`npm run serve:voices`) exists so a new voice can be heard before it is uploaded — it sends the CORS headers the dev server's COEP `require-corp` demands, which a plain static server does not — YIN on every file, a label offset derived per library, and hard failures on anything the offset cannot explain. That is what caught VSCO's clarinet labels sitting an octave low and one file mislabelled by a semitone. `slicer_ref.py` dumps Python's slicer stage 1 (prep_page → detect_staves → normalize_row → detect_barlines) as the **control arm** for the TypeScript port, and the file it writes also defines the sample the browser side runs — resumable, ~4.2 s/page for the whole corpus in ~2 h. It records each row's bar list AND its rejected candidates, and prints how far the manifests have drifted from both. For W6's strip entries it calls **`page_to_strips` itself** into a temp dir rather than re-implementing the driver's pad/trim block: the port is transliterated from those same lines, so a second hand-written copy could encode the same misreading twice and then agree with itself. That second pass is what doubled the per-page cost. |
| [src/vision/](../src/vision/) | Python fine-tuning side: `eval_omr_transformer.py` (Step-1 model gate), `data.py` (dataset/label wiring), `overfit10.py` (Rung-1 gate), `onnx_parity.py` + `make_browser_gate.py` (Rung-1.5 ONNX/browser gate), `audit_coverage.py` (Rung-2 dataset gate; fed by `scripts/select_pieces.py` / `export_scores.py` / `make_split.py`). **Rung-2 training kit:** `augment.py` (on-the-fly input-realism augmentation — preview grid is the human gate; **three** profiles since 2026-08-19: screenshot, photo, and a **scan** profile behind `scan_share`, default 0.0 = off, whose ops are built lazily so a run that does not ask for scans constructs exactly what it constructed before. ⚠ Two things measured there apply to every run this project has made: albumentations 2.0.8 seeds its transforms from OS entropy, so **no augmented image is reproducible from a seed**, and `line_dropout` masks THIN INK ONLY — `dark − open(dark)`, which a beam cannot enter — because that is what keeps the op label-preserving. `--scan-share` on `train.py` and `domain_gap.py`; [rung3/scan-profile.md](rung3/scan-profile.md)), `modeling.py` (shared model/tokenizer setup so train + eval can't drift), `train.py` (the scaled Colab fine-tune: AMP, warmup+cosine, val loop, checkpoint/resume — recipe in its docstring), `eval_omr.py` (headline per-class AEU accidental accuracy + SER via id-space alignment; since 2026-07-27 it also splits recall by PRINT POSITION — `\sig` block vs notehead — which is how Round 2's koma↔küçük confusion was traced to the key signature), `quantize_onnx.py` (int8 quantization of the exported graphs). **Rung-4 page pipeline:** `page_to_strips.py` (classical-CV slicer: page → training-shaped strips via staff/barline detection + scale normalization; `docs/PIPELINE.md` §1 stages 2–6; carries the 2026-07-25 **photo front-end** — auto-deskew, crop-to-quad de-warp, `STAFF_HOR_FRAC=0.11` — all no-ops on clean scans. **2026-07-29 overhaul:** width and measure caps are now actually enforced; `TRIM_SHARED_EDGE` stops neighbouring crops sharing pixels; `place_band()` + `row_music_extent()` float the staff inside the fixed frame so low beams are not cut off; `row_cost_features()` / `estimate_tokens()` predict a strip's decoded length so over-budget crops are flagged in the manifest; `window_signature()` / `window_cache_ok()` key decode caches on the whole windowing. Knobs, all defaulting to the shipped behaviour: `OMR_WINDOW_MODE` (legacy|budget), `OMR_TOKEN_BUDGET`, `OMR_EDGE_TRIM`, `OMR_VPLACE`, `OMR_VPLACE_MIN_HEAD`, `OMR_MEASURES_PER_STRIP` — see [METRICS-SLICER.md](METRICS-SLICER.md) for what each was measured to do), `decode_page.py` (end-to-end page decode: slicer → ONNX greedy decode → per-row token streams + `<page>_decode.json`, the stage-8 stitcher's input). **Round-N (real-data) additions:** `make_realval_pool.py` (materialises the real-val pool `eval_omr.py` scores for the pre-registered selection number), `degrade_probe.py` (degraded-strip hallucination probe). Results log: [MODEL_EVAL.md](../src/vision/MODEL_EVAL.md). |
| [scripts/rung3/](../scripts/rung3/) | **The real-page (Phase-3) loop** — narrative in [rung3/](rung3/README.md). *Collect:* `collect_nota.py`, `collect_tuplets.py`, `find_tuplet_pieces.py` (`scripts/collect_notalar.py` did the neyzen corpus). *Label:* `match_symbtr.py` (name-match a real page to SymbTr = free ground truth), `emit_strip_labels.py` (real strip PNG → token label; `--val-side` restricts the run to real-val pieces through `data.is_real_val_piece` rather than a hand-made list; hard-fails when two source pages resolve to one page stem, because strip dirs are keyed by stem alone — `collect_tuplets.neyzen_stems()` is where stems are made unique, and it distinguishes a real collision from a duplicate upload), `decode_pages_gpu.py` (Colab batch decode), `review_ui.py` (one-keystroke verdict UI for every queue: adjudication, `photo-gold`, `exam-fix`, `realval-hard-v2`, `reslice-all`; ⚠ **a PENDING row's `corrected_label` is a machine SUGGESTION, not a human correction** — a human correction always arrives as verdict `fix` — so since 2026-08-21 it is DRAWN (`suggHtml`, diffed against the label) and **`ok` clears it**. Both were wrong before: the suggestion was invisible while still winning `e` *and* `a`, so `ok` silently stored text nobody had seen on 12 exam rows; `/img/` is keyed by QUEUE — `QUEUE_IMG_ROOTS` binds each queue to the crops it was built from, because a strip filename survives a re-slice and its pixels do not; queues over `EAGER_MAX` rows ship counts only in `/api/state` and load their rows from `/api/rows` when the tab is opened; since 2026-08-21 a `FULL_AUDITS` entry may name **its own strip root**, because `build_full_audit` had that root hardcoded to `data/real/strips` — for the `b8-*` queues, whose crops are the re-slice, that would have seeded every edit box with a July `rung3-labeler` decode of a *different* crop with nothing on screen to say so; the four pre-existing full-audit queues keep the old default), `build_reslice_queue.py` (one queue over EVERY strip the re-slice decoded — 33,804 crops / 1,704 pages — worst-first, seeded with the page cache's decode; joins ONLY `strips_v2emit`, the sole emit made from those crops, see [rung3/labeling.md](rung3/labeling.md)), `build_realval_v2.py` (`--report` the exam-vs-real-val difficulty mix, `--queue N` stage the hard tier **worst-first**, `--build` assemble the rebuilt pool; queues are versioned per re-slice and `--strip-root`/`--pools` must point at the current slicer's output), `promote_labels.py` / `rule_fix_notafull.py` (adjudicated labels → training or exam manifest), `build_testset.py` (freeze the exam), `build_exam_v3_queue.py` (2026-08-20: cut the `examv3` growth queue over the exam pages we own but do not grade — it derives those pages from `testset.json` minus the clean manifest rather than listing them, and merges one emit made from a **fresh slice** of those pieces into `data/real/strips_examv3`. The separate crop root is the whole point: the emitter slices in place, and the crops under `data/real/strips` are hardlinked into `strips_exam_v2_clean/`, so re-slicing there would silently change the pixels the frozen exam's gold describes. ⚠ Its first version did the opposite — it reproduced the retired slicer's geometry to match the 46 graded pages, and produced 265 px crops that cut beamed groups in half; the docstring keeps that trade so it is not re-made. `--carry-from` offers an earlier cut's hand-typed corrections again on the crop holding the same music, as **pending suggestions** in `corrected_label` and never as verdicts, and `link()` replaces a hardlink that points at another root because strip filenames survive a re-slice while pixels do not; `--plan` prints the emit command and writes nothing. ⚠ its `gold_conflict` rows are synthesised from ALREADY-ACCEPTED strips, which never pass through `emit_review`, so their decode has to be read out of the page's own `_decode.json` — the first version left the field empty and the viewer showed no model decode at all (fixed 2026-08-21). `--rebuild` (2026-08-21) takes it from a growth queue to a rebuild of the WHOLE exam, and `carry_gold()` moves the frozen exam's 326 labels onto the new crops by measure span — splitting them into agreed / suggested / conflict / lost, so the 43 that two independent SymbTr derivations agree on need no human and the rest are confirmed against the new picture rather than retyped). *Measure:* `score_slicer.py`, `score_clean_baseline.py`, `decode_photos_exam.py` + `score_photos_exam.py` + `photos_exam_report.py` (photo domain), `score_photo_gold.py` (hand-labelled photo strips), `build_photo_gold_queue.py` / `build_exam_fix_queue.py` + `apply_exam_fix.py` (gold re-audit), `sharp_adjudication_report.py` + `sharp_width_test.py` (the microtonal-sharp diagnosis that traced the weakness to our renderer). Since 2026-07-26 `score_photos_exam.tally` also buckets gold by PRINT POSITION (`\sig` block vs notehead) and `score_photo_gold.py` / `score_clean_baseline.py` print that split — the microtonal sharps are scored almost entirely in the signature, which a pooled per-class number hides. `build_realval_v2.py` (2026-07-28) rebuilds real-val to the exam's difficulty mix — `--report` shows the gap and how many hard strips are owed, `--queue N` stages the `realval-hard` review queue (see [rung3/labeling.md](rung3/labeling.md)). **Round-3 pre-render probes (2026-07-28)**, each carrying its pre-registered bar AND its result in the docstring so it is not re-litigated: `empty_crop_probe.py` (`--sweep` decodes the whole exam and buckets edits by crop width and note count — the tool that reproduces the 562-edit total and gave per-strip attribution for the first time), `width_split_probe.py` (splits wide strips at a gutter and scores against identical gold — closed the "cut them narrower" idea), `beam_weight_probe.py` (beam thickness in staff spaces, ours vs real print — closed "our beams are too heavy"), `staff_geometry_probe.py` (dose-response ladder over scale/shift — found the slicer's staff-size bias, worth 12–15.5% of corrections). **The tuplet mark (2026-08-12):** `tuplet_mark_probe.py` locates digit-like candidates in real editions and writes matched-staff-size tiles plus the post-encoder view, then `--accept <indices>` prints the geometry over the ones a HUMAN confirmed — it deliberately builds no mark detector, and its own false positives (lyrics, the tempo mark) are why. **Extended 2026-08-19** to read the third mark style off loose PAGE images (`--images`, no manifest and no `\tup3` filter) and to report `concave_geometry` — the clearance under the arc, whether digit and arc are ONE component, and where the digit sits along the span. Two of the three additions are facts about SCANS rather than about tuplets: `--destaff` erases the staff lines because a scanned page is otherwise a single connected component (measured: one blob 2,026 px wide), and `--staff-thresh` exists because a full-width row on a scan rarely reaches the 0.6 ink fraction that defines a staff line — ⚠ **the pools keep 0.6**, or the numbers already published stop reproducing; `tuplet_mark_sheet.py` composes the real-vs-old-vs-new comparison the owner judges the shape on ([rung3/tuplets.md](rung3/tuplets.md)); `build_tuplet_val.py` (2026-08-13) assembles `_tupletval`, the A/B's selection pool — every `\tup3`-bearing strip already on the VAL side of `strips_tup` + `_realval_v2`, exam pieces refused, decode-derived labels dropped, and a filename that appears in both pools resolved to the newer slicer's crop (same name, different pixels — 8 of them). It reports the gold-group count because the pre-registered resolution limit is computed from it ([rung3/round3-criteria.md](rung3/round3-criteria.md)); **The page queue (2026-08-18, built then DEFERRED — see [BACKLOG.md](BACKLOG.md)):** `build_page_queue.py` ranks whole PAGES by **structural error evidence** — off-meter bars, stitch warnings, `hit_cap` strips, slicer-flagged crops — for a page-level correction pass, refusing exam pieces and capping any one makam's share. ⚠ It deliberately does **not** rank on model confidence, and the docstring carries the two measurements that ruled it out: worst-confidence-first captures 4% of known disagreements in its worst decile (**lift 0.44×**, worse than random), matching W8's drop, and a label/decode disagreement is ~78% of the time the *label* being wrong, so it cannot serve as the target either. ⚠ It also has **no scan-quality filter**, stated rather than hidden: the obvious candidate `nd` is `lev(label, decode)/len(label)`, not degradation ([METRICS-CORPUS.md](METRICS-CORPUS.md)). **Its scorer is now consumed** by `build_label_batch.py` (2026-08-18) — the page-level *UI* stayed deferred, the ranking did not. `build_label_batch.py` cuts a **page-complete labelling batch** out of `reslice_all.csv` using that ranking, writes it as its own review-UI queue (schema-identical, same crop root, rows in reading order within each page) and merges the verdicts back into the master queue with `--merge-back` — reporting rather than silently resolving a strip that already carries a different verdict there. `--clean` restricts the ranking to **born-digital** pages via `born_digital_stems()`, a PyMuPDF check that page 1 embeds no raster image and is drawn with >150 vector operators; that is a file-format fact rather than a heuristic, which is the whole reason it is trusted where `nd` and confidence are not, and its census corrected the long-standing "neyzen is clean vector PDFs" line ([METRICS-CORPUS.md](METRICS-CORPUS.md)). `--scanned` (2026-08-19) reads the same set the other way for `batch3`, and two flags exist because that tier is the first one that can contain what the born-digital filter excluded by construction: `--exclude-pages` takes a JSON list of page stems with a reason each — handwriting triaged by eye, and pages whose crops no longer re-slice — so a judgement lives in a file rather than in a shell history, and `--stats --batch N` prints the batch's **fix rate and bad rate** while it is being labelled, which is the number that decides whether to read on (a row is seeded with the decode, so an `ok` changes the training data by nothing). `check_crop_staleness.py --out` (same date) writes the per-page verdict as JSON for the same reason: its stdout list is truncated at 10 and re-running it costs ~12 min a batch, which is how an exclusion list ends up hand-copied and short. `paired_arm_score.py` (2026-08-19) decodes TWO checkpoints over one pool and reports the per-strip difference with an exact sign test and a bootstrap CI — the read a trained arm is decided on, since at 47–202 strips an unpaired difference can rest on which strips happen to be hard. It imports `eval_omr.align` rather than re-aligning, so an edit means the same thing in both files (the trap `crop_geometry_probe.py` documents about its own re-alignment). ⚠ It keys on the image filename, so `_realval_v2`'s 5 duplicate rows collapse to 197 unique strips in the scan pool — identical treatment for both models, but not comparable to `eval_omr`'s 202-row totals. `split_realval_tiers.py` (2026-08-19) partitions `_realval_v2` into pools `eval_omr.py` can read — by TIER (`tier_of`, imported never re-implemented) and by MEDIUM (scan / born-digital). ⚠ Its docstring carries the finding that made it necessary: the `hard` tier's 110 rows are gold seeded with a model decode and then confirmed, so descendants of that model are flattered and it scores *better* than `mid`; Lever 7 is therefore scored on the medium split ([rung3/scan-profile.md](rung3/scan-profile.md)). ⚠ Anything it cuts is **training only** — the ranking picks the worst pages in its tier, so exam growth needs a different sample ([rung3/labeling-queues.md](rung3/labeling-queues.md)). `crop_geometry_probe.py` (2026-08-15) asks the question the other probes do not — not whether we draw a symbol wrong, but how much of a strip the encoder is given. It writes down the fixed-409×583 arithmetic, re-aligns a **spent** `--show-errors` dump against gold and buckets the strips by the staff spacing the encoder actually sees, pinning density and sequence length in turn so the two obvious confounds are separated; `--make-padded` is the causal half, widening crops with more of their own empty staff so resolution falls while content and gold stay identical, with the dose-response reading pre-registered in its docstring ([rung3/levers.md](rung3/levers.md)) — ⚠ and the reason it tiles a quiet window rather than extending the last column is itself a measurement: a crop **ends on a barline**, so its last column is 36% ink and `BORDER_REPLICATE` would pad with a black band; `pale_line_probe.py` (2026-08-17) is the same genre pointed at the SLICER rather than the model: it replays every corpus page under plain Otsu and under `page_binarizer`'s choice and reports fires / recovered / **regressed**, which is the only number that can veto the fallback. It exists because the guard's corpus claim was originally recorded in a code comment with no script behind it — and because the guard's *rejected* wider version had already been shown to destroy 62 working pages, a claim of this shape has to stay re-runnable. It runs no model and skips deskew, since the choice is made from the unrotated page anyway; `tuplet_ab_score.py` then reads the A/B — both arms decode that pool, each gold `\tup3` occurrence is scored by `eval_omr.align`, and the two outcome vectors are compared **paired** with an exact McNemar, because at 54 groups an unpaired difference of a few points can rest on one group. **Lever 6's two scorers (2026-08-20)** exist because the metric they compute has no home in `eval_omr.py`: the augmentation dot is a **suffix inside a duration token**, not a token, so no per-class row can ever show it. `staccato_falsedot_score.py` reads the primary — the two 110-strip pilot pools that differ only by the staccato marks, where both golds carry **zero** dotted durations by construction, so "decoded a dot gold does not have" reduces to "did a dotted duration appear at all". It scores any number of checkpoints and pairs them per strip with an exact McNemar, greedy decode being deterministic and the two pools sharing filenames. ⚠ Its first job was to reproduce the 2026-08-15 baseline it was written to replace, and it does, to the digit — that ad-hoc measurement was never committed, which is the reason both scripts exist at all. `staccato_realdot_score.py` reads clause 2 the other way round — real dots **lost**, on easy+mid only, hard tier decoded and printed but never gated, per the signed pre-registration. It imports `decode_pool` / `mcnemar_exact` from the first rather than re-implementing them. |

## The 15-minute path (the spine)

If you only have a little time, read just these and you'll understand the whole flow:

1. `parser.py` → `Event` (what a row is)
2. `tuning.py` → `koma53_to_freq` (comma → Hz)
3. `symbtr_to_audio.py` → `main` (Phase-0 flow end to end)
4. `types.ts` → `NoteModelDocument` (the JSON contract in TS)
5. `scheduling.ts` → `buildTimeline` (data → timed notes)
6. `App.tsx` → `App` (how the harness ties core + backend together)

## Two ideas worth pausing on

- **The `AudioBackend` boundary** ([scheduling.ts](../packages/core/src/scheduling.ts) defines
  it, [webAudioBackend.ts](../apps/web/src/webAudioBackend.ts) implements it). Everything that
  touches a real audio API lives behind this interface — that's what lets the mobile app reuse
  the core verbatim and only rewrite the backend.
- **Bars come from `offset`, not from counting beats** ([measures.ts](../packages/core/src/measures.ts)).
  SymbTr's `offset` column already marks barlines (an integer = one usul cycle), so this works
  for non-whole-note usuls like aksak (9/8), not just düyek (8/8). The `bar` is assigned once at
  load and travels with each event so editing can't scramble the grouping.

## How to read actively (not just stare)

Run this with files 1–4 open:

```bash
python3 scripts/symbtr_to_audio.py data/raw/<score>.txt --info
```

The `--info` table (note name → koma → Hz) **is** the data flowing through steps 1→2.
Then open the exported JSON next to `types.ts` (steps 5–6) and you'll literally see the
bridge: the same fields, Python on one side, TypeScript on the other.

```bash
python3 scripts/symbtr_to_json.py data/raw/<score>.txt -o apps/web/public/sample.json
npm install && npm run dev:web   # open the URL: ▶ Play, toggle Sheet, ✎ Edit a measure
```
