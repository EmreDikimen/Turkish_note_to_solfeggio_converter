# Decisions — what was decided, when, and what overturned it

purpose: one line per decision so nobody re-opens a settled question or re-runs a cancelled experiment
audience: agents and the owner, before proposing any change of direction
updated: 2026-08-21

Status values: **LOCKED** (stands), **OVERTURNED** (replaced by evidence), **DROPPED** (abandoned
on purpose), **SUPERSEDED** (a later decision covers it). Reasoning for the abandoned ones is kept
in [log/superseded.md](log/superseded.md) — read it before re-proposing anything here.

## Product and architecture

| Date | Decision | Status |
|---|---|---|
| 2026-07-02 | **Ship web first, then mobile.** React web now, React Native later over the same TS core. | LOCKED (reversed an earlier mobile-only decision) |
| 2026-07-02 | **No production backend.** Inference + audio run in-browser / on-device; Python is training/data only and never ships. Reason: a hosting subscription isn't affordable. | **OVERTURNED 2026-08-05** by the row below. The affordability premise did not survive: scale-to-zero CPU hosting did not exist on these terms in July, and free tiers look likely to cover friend-scale traffic at $0. **Audio stays local, and the training/shipping split is untouched for training** |
| 2026-07-02 | **Platform specifics behind adapter interfaces** (`AudioBackend`), so mobile reuses `packages/core` unchanged. | LOCKED |
| 2026-06-20 | **Two-layer pitch model**: exact 53-TET komas for sound, snapped AEU signs for the printed staff (`toAeuAlter`). | LOCKED |
| 2026-08-07 | **The makam bends the SOUND ONLY, and only where a source says so.** Selecting a makam adds comma deltas to the sounding koma (`buildTimeline` and nothing else) — the engraving, `Save JSON` and the training strips are untouched, and no key signature is redrawn. The table carries **documented deviations only** (uşşak-family segah −1.5, sabâ hicaz +1.5, hüzzam hisar +1, segah karar −1, plus an explicit *no deviation* for hüseyni/muhayyer/tâhir); every other makam is selectable and plays as written. `none` is the default. Detection declines (`none`) rather than ranks when the karar contradicts the signature — a wrong makam detunes notes that should not move. Sources and the table: [mvp/makam.md](mvp/makam.md) | LOCKED (owner, 2026-08-06) |
| 2026-08-02 | **Finish the pipeline and release an MVP before training Round 3.** The model freezes at `round2-stage2-best` int8 for the whole MVP track. Reason: Round 3 is an unknown-payoff content-mix bet that changes nothing a friend would notice, while the product half of the 2026-07-27 goal (error localisation) has never been built, and feedback cannot be collected without a pipeline. Track: [mvp/README.md](mvp/README.md) | **PARTLY OVERTURNED 2026-08-05** (owner). "Finish the pipeline first" stands. **The freeze and the pause do not**: Round 3 now runs in parallel, and the friends build takes a better model as soon as one exists — see the four scope rows below. The premise that changed is what the release is *for*: it asks friends about the interface, not the model, so model work no longer has to wait for it |
| 2026-08-02 | **Port the slicer with opencv.js**, not hand-rolled typed arrays. Parity over bundle size — 13 MB is noise beside 211 MB of weights, and `INTER_AREA`/`connectedComponents` are where a hand-roll would silently drift. Validated at W0: all five primitives bit-identical on identical input bytes, both sides OpenCV 5.0.0. | LOCKED |
| 2026-08-02 | **MVP accepts screenshots and clean scans only**; the camera-photo front-end (`detect_page_quad`, `crop_to_page`, `estimate_skew`, `deskew`) is not ported. It is already a documented no-op on clean input, and real uploads are mostly screenshots. The port keeps a `prepPage` seam so it can slot in later. | **PARTLY OVERTURNED 2026-08-03** — see the next row. The scope (screenshots + clean scans) stands; the "no-op" premise held for the crop and failed for the deskew |
| 2026-08-03 | **`estimate_skew`/`deskew` ARE ported; `detect_page_quad`/`crop_to_page` are not.** Measured, not read: **15.3% of corpus pages (272/1,781) take a real rotation** and 0% take a perspective crop, and skipping the rotation took one page from 10 staves to 0. 22 of the 23 pages failing the first parity run were exactly the 23 deskewed ones. `prepPage` stays the seam for the crop. ⚠ Cost is ~35 s/page in the browser — a W7 problem, not a W4 one ([METRICS-SLICER-PORT.md](METRICS-SLICER-PORT.md)). | LOCKED |
| 2026-08-03 | **The slicer port is scored against LOCAL PYTHON, not the `strips_v2` manifests.** The manifests are not reproducible by the current code — 1,680/1,704 (98.59%), below W4's own 99.5% bar — because 1,578 of 1,781 page dirs were sliced on Colab. Scoring against them read 86.7% and failed pages where the port matched Python line for line. `scripts/slicer_ref.py` builds the reference and defines the sample; manifest agreement is reported as the weaker second reference. Same lesson as the arm-B decode ceiling. | LOCKED |
| 2026-08-04 | **The Python control for W6 RUNS the real driver** — `slicer_ref.py` calls `page_to_strips` into a temp dir instead of re-implementing its pad/trim block. The port is transliterated from those same ~40 lines, so a second hand-written copy could encode one misreading twice and then agree with itself. Costs a second stage-1 pass (~4.2 s/page, ~2 h for the corpus); validated against the manifests on the smoke sample (106/106 strip entries). | LOCKED |
| 2026-08-04 | **W6's window bars are scored on rows whose BAR LIST agrees, with the raw number printed beside them.** Windows are computed from the bars, so a 100% window bar silently demands a 100% bar bar — which W5 itself set at 99.0%. All 6 raw field mismatches sat on the only 2 pages whose bar count differs (W5's diagnosed ±1 grayscale near-ties). Second time a criterion written before that residue was understood has failed a port that agrees with Python; W4's zero-staff bar was the first. **Restating a bar is allowed; hiding the raw number is not.** | LOCKED |
| 2026-08-04 | **Arm A is compared to arm B PAIRED (McNemar), never to the 86.0% level.** At n=450 the ceiling's standard error is ~1.6 pp, so the plan's "within 1 pp" bar was inside the noise. Result: 12 vs 4 discordant, p = 0.077 — no detectable difference — plus the finding that settles attribution, that all 16 discordant strips have **identical crop widths**. | LOCKED |
| 2026-08-05 | **An UNCLOSED tuplet run still gets its bracket** (`tupletGroupsIn`, `tools/render/rhythm.ts`). A `\tup3` the model reads but cannot close — a dropped member, a stray `\tupend` — used to yield no group at all, so `vexDuration` snapped each member to the nearest plain value: the page drew a rhythm that was definitely wrong, with no mark saying so. Measured over the 1,704 decode caches: **1,287 notes, 22.9% of `\tup3`-bearing pages**, now **0**. ⚠ The function is shared with the label serializer on purpose, so this moves synthetic labels too — **5 measures in 1 of 190 training pieces** (`mahur--sarki--duyek--ipek_saclarin`), which changes only on a future re-render; `strips_v4` on disk is untouched. 217/217 round-trip holds in both modes, and both draw paths were rendered on the three worst real pages with **0 dropped measures**. | LOCKED |
| 2026-08-05 | **DECODE MOVES TO A SERVER. The app slices in the browser and POSTs the crops; a scale-to-zero CPU server runs the model.** Owner decision, stated reason: **protect the user's machine** — a page burns ~19 s of multi-threaded CPU on the client and every user pays that heat on their own hardware. This reverses the 2026-07-02 no-backend rule and the `CLAUDE.md` hard rule with it. What it does NOT change: audio and the editor stay local, the W4–W6 slicer port stays client-side (it is what keeps the upload small), and **the in-browser decode path is KEPT** — `gate:browser`, `parity:armb`, `parity:arma`, `smoke:page` and the W3 browser-vs-gold quality result all rest on it. ✅ **The thermal premise was confirmed by the owner the same day**: the machine still gets hot at the current ~25 s page, from direct use — so the re-test this row originally owed is **not owed**. It is an observation rather than an instrumented number, and heat on the owner's own laptop is precisely the thing the owner is the authority on. ✅ **Deployed and measured 2026-08-06**, and the outcome is the one this row was chosen against: on Cloud Run a page is **slower than in the owner's own browser** (250 s vs 166 s over 128 strips, plus a 10.6 s cold start), because a shared vCPU is ~3.5× slower than an M4 core. The decision was made on thermals, not speed, and it survives its own benchmark. Numbers: [METRICS.md](METRICS.md). | LOCKED (owner decision) |
| 2026-08-05 | **Ink above the staff may claim room only up to 3.5 sp (`VPLACE_TOP_CLAIM_SP`).** Uncapped, a slur or ornament above pushed the staff down and sheared the BEAMS below — the ink that carries the durations — which is how a real page came back unreadable. Measured over 120 pages / 901 rows: capping loses **0 px** inside the ledger-note zone, exactly as before, while cutting beam loss **19,932 → 17,231 (−13.6%)**. **Bottom-first was tried and rejected**: it saves beams by destroying 500 px of real ledger-note ink. By construction the cap moves only rows already in conflict. ⚠ It is an information argument, not a decode result; at 2.6% of rows an A/B is underpowered. `OMR_VPLACE_TOP_CLAIM=99` restores the old rule, and the slice inspector toggles it. | LOCKED |
| 2026-08-06 | **What ships is not what the checks were running, three times in one day.** The built web app's fallback hung (`document is not defined` — the bundler inlined ORT's worker glue); the container exited on `pngjs`'s CommonJS `require` inside an ESM bundle; and an oversized upload returned **503 instead of 413** through Cloud Run's proxy because the socket was destroyed mid-request. Every one was invisible to `dev:web`, `dev:server`, `smoke:page` and the 27/28 gate, because each of those runs the *convenient* artifact. Standing rule that follows: **a check must run the artifact that ships** — `npm run smoke:build` (the built site, real headers, weights cross-origin) and `npm run check:bundle` (the container's bundle) exist for exactly this, and `check:limits` is run against the LIVE service, not localhost. | LOCKED |
| 2026-08-06 | **The client preprocesses; the server receives finished 409×583 PNGs.** The W9 seam sits AFTER Donut preprocessing, not at the raw crop: the browser rotates/resizes/pads (`preprocessToCanvas`) and uploads that image, and the server applies only the rescale (`omr/pixels.ts`). Reason: a server-side resize would add a **third resampler** — the canvas draw already is not PIL BILINEAR — and a rung to prove it matches, which is the exact bill reusing `decode.ts` was chosen to avoid. PNG is lossless, so both sides feed the encoder identical bytes and ORT is the only thing left that can differ. ⚠ **The plan's third reason for this seam was WRONG and is withdrawn**: "upload size — ~19 small crops instead of one full-resolution page image". Measured over 6 pages, the upload is **0.11×–2.03× the page image, median ~1.7×** — usually BIGGER. The seam still stands on the other two reasons (no second resampler; slicing stays free on the client), but not on bytes. | LOCKED |
| 2026-08-06 | **ORT lives behind `Sessions`, and `decode.ts` imports no runtime.** Types come from `onnxruntime-common`; the `Tensor` constructor travels beside the three sessions, so the same module runs on `onnxruntime-web` in the browser and `onnxruntime-node` on the server. `apps/web/src/omr/types.ts` predicted this move for the mobile app and W9 is what came to collect. The encoder is also split from the greedy loop (`runEncoder`/`decodeFromHidden`) so a page can be batched server-side without a second decoder. Verified by the gate: **27/28, unchanged**. | LOCKED |
| 2026-08-06 | **The server is judged against the browser by a PAIRED test on GOLD, not by agreement.** Agreement is 93.8% of strips (120/128) — and it is the wrong instrument twice over: it cannot say which side is right, and it moves with the batch size. Same tool W6 used instead: both arms decoded `_realval_v2` and scored by `score_browser_gold.py`, paired over 267 hand-verified strips. **No detectable difference** — exact-match discordance 3 vs 5, **McNemar exact p = 0.727**; total edits browser 780 vs server 768, per-strip sign test p = 0.664. ⚠ Non-claim: "no detectable difference" at n=267, not "identical" — the two runtimes genuinely diverge on ~6% of strips, at tokens the model was on average only ~55% sure of. | LOCKED |
| 2026-08-06 | **W10 SHIPS AT 35–55 s A PAGE. Only `--cpu-boost` is bought, and it rides the redeploy that has to happen anyway.** Owner decision. Splitting a page across instances is the only option that touches the wait a friend actually feels — options 1 and 2 attack the **cold start** (10.6 s, of which 9.5 s is loading the graphs), not the 31–51 s of decode — and it costs a rate-limiter rewrite (it counts requests, and a page becoming four requests makes the 20/min cap fire after five pages), `--max-instances` 3 → ~8, a partial-failure policy, and a token-for-token chunked-vs-unchunked check. Reason for deferring all of it: **W10 asks two friends about the interface**, so their reaction to the wait is the measurement that decides whether the work is worth doing — buying it first spends the effort against a guess. ⚠ **Deferred, not dropped, and the trigger is a friend saying the wait is annoying.** `--cpu-boost` is unmeasured for us (Google says "up to"), so it is taken because it is free on a redeploy, not because it is proven. Menu and prices: [mvp/latency.md](mvp/latency.md) | LOCKED (owner decision) |
| 2026-08-06 | **The app is hosted on NETLIFY, not Cloudflare Pages — a hard limit, not a preference.** Cloudflare Pages refuses any single asset over **25 MiB** (their published limit, re-read the same day) and `dist/ort/ort-wasm-simd-threaded.jsep.wasm` is **26,827,543 bytes = 25.58 MiB** — over by ~613 KB. Netlify has no cap in the way and reads the same `public/_headers`, so **nothing in the repo changed**: this is the 2026-08-02 row's "the choice stays open" clause being spent, and the reason it was written that way. ⚠ **Two alternatives were considered and NOT taken now.** Importing `onnxruntime-web/wasm` (CPU-only) instead of the full bundle would use the non-jsep binary — **12.86 MiB against 25.58**, taking `dist/` from 43.3 to ~30 MB — and we only ever ask for `executionProviders: ["wasm"]`, so nothing in use is lost; it is deferred because it changes the fallback's runtime and owes `gate:browser` + `smoke:build`, and that should not ride a hosting deadline. Serving `/ort/` cross-origin from the Hub was rejected outright: it puts ORT's own runtime behind `require-corp`, the combination this project has already been bitten by. Walkthrough: [mvp/hosting-setup.md](mvp/hosting-setup.md) | LOCKED |
| 2026-08-06 | **A STYLE PASS SHIPS BEFORE W10.** Owner decision, after seeing the deployed site: it is a working harness and looks like one. Reason, which is the release's own logic: **W10 exists to ask two friends about the interface**, so putting an unstyled page in front of them spends both of the only two first impressions available on feedback already in hand ("make it look better"). ⚠ Scope is **presentation only** — slicing, decode, the fallback and the origin lock do not move, and `npm run smoke:live` is what says so afterwards. ⚠ It also means W10 is no longer one message away, which is the cost being accepted. ✅ **DONE 2026-08-07** — scope held; see the row below for the one thing it changed outside presentation. | LOCKED (owner decision), DONE |
| 2026-08-07 | **THE DEPLOY CHECKS ASSERT ON DOM STATE, NEVER ON USER-FACING COPY.** Five Playwright tools matched English prose (`text=Turkish OMR`, `/read a page:/`, `/read on the server/`, button names like `/Save JSON/`), which made the wording load-bearing and would have broken every one of them the moment the UI became Turkish. The facts are now separated from the sentence: `apps/web/src/ui/status.ts` produces `{ text, state, kind, where, counts }` and `#omr-status` renders the non-text half as `data-*`; buttons carry ids and `data-play-state`. ⚠ Translating the regexes was the alternative and was **rejected** — it moves the coupling one step and breaks again at the next rewording, and rewording is what a style pass *is*. ⚠ It landed as **step 0, before any styling**, so the tools went green against the OLD UI first and every later red was attributable. Side benefit: `data-where="local-fallback"` is *stricter* than `/read on your machine/`, which also matched a build with no decode server configured at all. | LOCKED |
| 2026-08-07 | **THE PER-MEASURE EDIT MODAL IS REPLACED BY A MUS2-STYLE ARMED PALETTE OVER THE WHOLE SCORE** (owner). **Düzenle** opens a palette beside the sheet; you **arm a tool** (note value, accidental, tuplet) and **click the score**. Clicking a note selects it, shows an **✕** to delete, and **dragging it up or down** moves its pitch carrying its accidental (⚠ the brief first said scroll wheel; the owner replaced it with a drag on 2026-08-07 — a wheel fights the page's own scrolling and moves the note in jumps). Inserting = arm a value and click **anywhere empty** — between notes, before the first, after the last. **Editing is whole-score, never measure-scoped** (that scope is the thing being removed), and **no zoom** (dropped as unnecessary, which also removes the re-engrave trap it would have created). The palette carries its own **Çal/Dur**, and **Çal starts from the LAST EDITED MEASURE** — that is what replaces click-to-seek, which edit mode necessarily consumes. ⚠ **`\repstart`/`\repend` are deliberately NOT editable**: the stitcher **unfolds** repeats (the music is written out twice), so inserting a repeat barline into flattened music would corrupt it — this also dissolved the schema fork an earlier draft raised. ⚠ **Editing tokens and re-stitching was considered and REJECTED**: playback needs the flattened doc, and a repeated passage renders twice from one token so a click cannot be attributed to a pass. Brief: [mvp/editor.md](mvp/editor.md) | LOCKED (owner decision), **SLICE 1 DONE 2026-08-07** (select / ✕ / drag-pitch / undo-redo); **STEPS 1–8 + 10 DONE 2026-08-08** (palette, its Çal/Dur, insert, the tuplet tool, the off-meter mark, and the modal DELETED — out of order, owner's request). ⚠ The deletion took four things with it; **rests and the numbered ±2/±3/±8 alterations were rebuilt into the palette the same day** (owner), so the standing cost is editing a **lyric** and exact **koma/Hz** entry — and ⚠ a numbered alteration is stored exactly but **drawn snapped** to the nearest AEU sign ([mvp/editor.md](mvp/editor.md)). Only `Save JSON` (step 9) is still owed |
| 2026-08-07 | **ONE SET OF EDIT PRIMITIVES, IN CORE — and the per-note rects do NOT ride on `onLayout`.** Building the editor's first slice found the app had two disjoint edit vocabularies over one document: the piano-roll patched `koma53` + `freqHz`, the modal rebuilt events from a `{letter, octave, alter}` spelling. Since the sheet reads staff position from `parseNoteName(ev.noteName)` and `updateEvent` never wrote `noteName`, **a roll drag moved the sound and left the notehead behind** — a live bug, now fixed by routing both paths through `packages/core/src/edits.ts`. ⚠ Two things were deliberately **not** changed: the roll's *duration* drag still leaves `durationBeats` stale (fixing it means quantising a continuous drag — the palette's job), and `noteAE` stays exact rather than AEU-snapped (the Python exporter snaps it, `stitch.ts` and the modal never did; `withPitch` matches the two TS producers). ⚠ The brief said to extend `onLayout` with per-note rects; **rejected** — that payload is the contract `stripExport.ts` / `render.ts` crop training strips by, the only consumer is the overlay in the same file, and `onLayout` is an engrave dependency, so a second non-stable callback prop would re-engrave forever. They are local `SheetView` state. Verified: 302 strip PNGs byte-identical before/after. | LOCKED |
| 2026-08-07 | **TUPLETS ARE EDITABLE AND MUST BE EXACTLY THREE NOTES; INVALID TARGETS ARE NOT CLICKABLE** (owner + a code finding). `\tup3` is **arithmetic, not an object** — `isTupletMember` is "the duration's denominator is divisible by 3" and a group closes when the run sums to a plain value — so the tool multiplies each member by ⅔ and needs no schema change. Selection is **first note, then last note**, and the page must **refuse** any end note that would not make a valid tuplet (dim it, do not pop an error): contiguous, equal durations, **exactly three members**. ⚠ Three is not arbitrary — the drawn digit is **hardcoded `"3"`** (`SheetView.tsx:392`), so a six-member run would satisfy `tupletGroupsIn` and then draw a bracket that lies about the rhythm. Until that digit is derived from the group size, three is the only honest length. | LOCKED, **BUILT 2026-08-08** — and building it added one rule the brief did not have: **a member must be a PLAIN `1/2^k` value** (no dots, no tie-splits, nothing already a tuplet). Three equal members at ×⅔ sum to `2v`, which is plain only when `v` is, so a dotted run would draw the *incomplete-group* bracket — the mark that means the MODEL misread something. Two owner calls came with it: **clicking any member of a closed group removes it** (one tool, both directions), and **dimming starts when the tool is armed**, not after the first click |
| 2026-08-07 | **AN EDIT ABSORBS INTO ITS BAR — BAR LINES NEVER MOVE — AND A BAR OVER *OR* UNDER ITS LENGTH WARNS** (owner). Rippling the following events so bar lines re-flow was rejected: on a decoded page the bar lines came from the model's own `|` tokens, and re-flowing rewrites the structure a corrected page most wants to keep. The warning is an **indicator, never a block** — `isMeasureValid` stops greying out a Save button (which would make the ✕ refuse to work) and starts feeding a mark. ⚠ **The obvious reference length is circular**: `Measure.lengthBeats` is computed *from the bar's own contents* (`measures.ts:123`), so `isMeasureValid` is true by construction on any freshly loaded score and can only ever mean "you changed this bar". The reference must be the **derived meter** (`deriveTimeSignature`). ⚠ **Measured side effect worth verifying further**: against the derived meter, interior bars off-meter are **0/32, 0/60 and 0/108 on three clean SymbTr scores** but **8/28 on a real decoded page** — i.e. the warning is silent on correct music and lights up where the model misread a duration, which is the error localisation W8 was dropped without delivering. ⚠ n = 1 decoded page; the 8 are *candidates*, not confirmed errors, and mid-piece usul changes (`Kod` 51) are a known false-positive source. | LOCKED (owner decision), **BUILT 2026-08-08** as a `+`/`−` badge in the edit overlay. Three calls taken while building: **edit mode only** (a friend should not meet eight warnings on first sight of a decoded page), **the first and last bar warn only when OVER** (a pickup and a closing bar are legitimately short — ⚠ so a triplet made in bar 1 shows nothing, which reads exactly like a broken indicator), and the modal's Save gate is **gone** |
| 2026-08-07 | **`Save JSON` IS DELETED — it will not be used** (owner). ⚠ This **retires the stated rationale for the editor rework**: earlier drafts justified it as "the editor is the Rung-3 labeling loop's tool, so seconds per correction is labelling throughput", and that stops being true when the export goes. It is defensible because the labelling loop's **primary** path is `scripts/rung3/review_ui.py` (per-strip verdicts, its own queues), not the web app; what is lost is the unused page-level export. The honest remaining reason for the editor is simply that **a friend with a wrong note should be able to fix it** — a product feature, and enough. Removes `onDownload`/`#save-json`, two `app-smoke` checks, and the [PIPELINE.md](PIPELINE.md) references. | **SUPERSEDED 2026-08-15** by the row below — the deletion was never carried out and is now dropped, so the button stays |
| 2026-08-07 | **NO WEBFONT — the display serif is the one SheetView already engraves in (Georgia stack).** Two reasons, and the first is not aesthetic: **`Cross-Origin-Embedder-Policy: require-corp`** (which the app needs for ORT's wasm threads) **blocks a font CDN outright**, so Google Fonts was never an option regardless of taste. The second is that using the engraver's own serif makes the page and the notation read as one object instead of two typefaces arguing. ⚠ Self-hosting an OFL serif in `apps/web/public/fonts/` next to Bravura remains open and is a **one-token change** (`--font-display` in `styles/tokens.css`); it was not taken now because it costs a Turkish-diacritic check (ğ ı İ ş ç ö ü) and a `font-display` decision on the first-impression path. | LOCKED, revisitable |
| 2026-08-06 | **`ALLOWED_ORIGINS` carries the localhost dev origins, not just the app's host.** `https://komavision.netlify.app` plus `http://localhost:5173` and `:4173` (revision `omr-decode-00004-nc2`). Reason: the lock otherwise blocks `npm run dev:web` from reaching the live decode server, which is exactly what frontend work needs. ⚠ This is a convenience, not a hole — a CORS origin is trivially forged outside a browser, and the things that actually bound abuse are the rate limit, the payload caps and `--max-instances 3` (`check:limits`, and [mvp/deploy.md](mvp/deploy.md) says so about the limiter already). ⚠ Cost: `smoke:build` from a localhost *preview* still fails against the live server, because it serves on a different port — use `smoke:live` for the deployed chain. | LOCKED |
| 2026-08-05 | **The skew sweep is a GENERATOR with two drivers, not an async copy.** W7 needed the 41-rotation search to stop blocking the tab for 35 s. `estimateSkew` runs the generator to completion (the parity path), `estimateSkewAsync` steps it and yields between rotations (the app); `deskew`'s two guards moved into `guardedAngle` so both reach the same decision. No arithmetic changes — verified by re-running the parity harness with the REAL estimator on 20 pages (4 of them rotating): **deskew angle identical 20/20**, W4/W5/W6 all still PASS. A duplicated async copy was the alternative and would drift the first time a gate is retuned. | LOCKED |
| 2026-08-05 | **W7's strip-count bar scores against `slicer_ref.py`, not the page's `_manifest.json`.** Written against the manifest first, it would have failed the app's wiring for a W4 reason (local Python reproduces only ~98.6% of those manifests). Manifest count is printed beside it as context. Same lesson as the 2026-08-03 row and the arm-B ceiling — third time. | LOCKED |
| 2026-08-05 | **`@techstark/opencv-js` is named in Vite's `optimizeDeps.include`.** It reaches `App.tsx` only through a lazy `import()`, so Vite's static scan misses it and discovers it at the first upload — re-optimizing and full-reloading the tab mid-slice, which discards the upload and hangs the app at 0% CPU. Naming it makes it a startup cost. Dev-server only; a production build has no dep optimizer. | LOCKED |
| 2026-08-04 | **The W0 cv-probe is KEPT, not deleted at W6 as planned.** It is the only check that would catch an opencv.js version bump changing a primitive under the port, and it is one command (`npm run probe:cv`). | LOCKED (reverses a planned deletion) |
| 2026-08-02 | **Grayscale parity is ±1, not equality** — `cv2.imread(IMREAD_GRAYSCALE)` converts inside the PNG decoder and a browser never sees that; OpenCV's own two paths already differ by ±1 on 7.4% of a colour page's pixels. Justified by measurement, not tolerance: the slicer's output is bit-identical under that perturbation ([METRICS-SLICER.md](METRICS-SLICER.md)). | LOCKED |
| 2026-08-02 | **Weights ship from Hugging Face Hub**, app from a COOP/COEP-capable static host (Cloudflare Pages / Netlify / Vercel), fetched as ArrayBuffers into the Cache API. GitHub Pages cannot set response headers → single-threaded wasm, 2–4× slower. | **LOCKED, and it does NOT lapse** — revisited at W9 2026-08-05 and kept. The earlier note here guessed the COOP/COEP requirement would mostly go away once decode moved server-side; it does not, because the client keeps an in-browser decode **fallback** (row below), and that fallback needs both the weights and wasm threads. What changes is *when*: weights are fetched **lazily, only if the fallback fires**, never on the normal path |
| 2026-08-05 | **THE FRIENDS RELEASE TESTS THE INTERFACE, NOT THE MODEL — and it goes to exactly TWO friends.** Owner decision. The question being asked is *"what should I add as a feature?"*, not *"how good are the readings?"* Three things follow, and each is its own row below: model work stops waiting for the release, the release stops being gated on accuracy, and the release scope shrinks to what two people can react to. ⚠ This is the row that re-scopes the whole MVP track — read it before proposing work for W9/W10 | LOCKED (owner decision) |
| 2026-08-05 | **ROUND 3 IS UNPAUSED and runs in PARALLEL with the product work.** Owner decision, reversing the pause in the 2026-08-02 row above. Reason: the pause existed so Round 3 could be aimed by real feedback, but the feedback now being collected is about features, so it will not aim Round 3 either way. The two tracks are independent — the product track never trains, the model track never touches the app. | LOCKED (owner decision) |
| 2026-08-05 | **The friends build SWAPS to a better model as soon as one lands** — it is not frozen at `round2-stage2-best` for the friends period. Owner decision. Cheap now that decode is server-side: a model swap is a server redeploy, and the client downloads nothing. ⚠ **The cost, stated rather than hidden:** a friend's comment can no longer be attributed to a known model version, so decode-quality remarks from this period are anecdotes, not measurements. Accepted because the feedback wanted is about features; **the exam remains the only thing that judges a model**. | LOCKED (owner decision) |
| 2026-08-05 | **The PUBLIC release is gated on Round 3's exam result.** Owner: two friends first; then Round 3 — if the result is good, open it to everyone; if not, Round 4, and so on. So the ladder no longer ends at W10: W10 is the two-friend build, and public launch is a later rung behind a model bar. ⚠ The bar itself is **not written down yet** — [rung3/round3.md](rung3/round3.md) owes it, on the user-effort metric (≥90% of pages ≤5 corrections; baseline 57%), before training starts. | LOCKED (owner decision) |
| 2026-08-05 | **PHONES ARE OUT OF SCOPE until the web app is done.** Owner decision — web first, phones considered afterwards. This does not reverse the 2026-07-02 "web first, then mobile" row; it says the mobile half is not being designed for now. ⚠ **Consequence for the server case:** the strongest argument in [mvp/deploy.md](mvp/deploy.md) for moving decode off the client was iOS Safari evicting the ~200 MB of cached weights, forcing repeated re-downloads on cellular. That argument is **parked, not refuted** — it returns intact when phones do. The server therefore rests on the thermal argument alone for this release — which the owner confirmed the same day still holds at the current ~25 s page. | LOCKED (owner decision) |
| 2026-08-05 | **W8 (confidence highlighting) is DROPPED from the friends release.** Owner decision, taken on the evidence rather than around it: the pre-registered bar — flag 10% of tokens, catch ≥60% of errors — is **NOT met**, and the best achievable at that budget is **26.3%**. A usable soft cut existed (`min_logprob < -0.5`: flag 22.6% of strips, catch 57.1% of edits, 2.5× lift) and was **not** taken. **Said out loud, as the goal change requires: this leaves half of the 2026-07-27 goal — "the app shows you where the errors are" — unbuilt.** Nothing is deleted: the measurement stands, `logprob-check.html` and `npm run check:logprobs` stay, and per-token logprobs still come out of the decode path. It is a strong candidate to come back if a friend asks for it — which is exactly what the release is for. | LOCKED (owner decision) |
| 2026-08-05 | **The decode server is Node + `onnxruntime-node`, importing the browser's own decode module — NOT a Python service.** `apps/web/src/omr/decode.ts` is already verified against Python at W3 (SER 0.0818 vs 0.0821, AEU macro recall 94.8 → 94.9%, exact-match 60.2% both), so reusing it means there is **one** decode implementation, not a third to hold in parity. The slicer port cost a full parity project to establish exactly that property; buying it for free here is worth more than the speed of writing a Python wrapper. Modal's "run `decode_page.py` nearly verbatim" was the alternative and was rejected. ⚠ **This CLOSES the open question `CLAUDE.md` flagged**: the rule "Python is training/data only and never ships" stands **unchanged**, and nothing under `src/vision/` becomes shippable. | LOCKED |
| 2026-08-05 | **Hosting: Google Cloud Run, free tier.** Owner decision. ⚠ **Adopted with its known weakness on the record:** a ~1 GB container cold-starts in 10–30 s, and at two-friend traffic nearly every upload is cold — the same sparse-traffic argument this project uses to reject GPU, applied to CPU. It is survivable only because of the fallback row below: a cold-start timeout decodes locally instead of failing. **Hetzner CX22 (~€4/month, always on, no cold start) is the named fallback** if cold starts prove unacceptable in testing; keeping Cloud Run warm with `min-instances=1` costs more (~$15–25/month) than Hetzner does. | LOCKED (owner decision) |
| 2026-08-05 | **The client falls back to IN-BROWSER decode when the server fails, times out, or is cold.** Owner decision. Nearly free — the path already exists and is tested (`gate:browser`, `smoke:page`) — and it means a server outage never reads as "the app is broken" to a friend who cannot debug it. Two consequences that are easy to miss: the browser must still be **able** to get the weights, so the HF Hub delivery row above stays LOCKED with the weights fetched **lazily on first fallback only**; and the COOP/COEP host requirement stays with it, because the fallback wants wasm threads. | LOCKED (owner decision) |
| 2026-08-08 | **THE APP SHIPS NO SCORE OF ITS OWN — removal, not attribution** (owner). Every bundled sample is a SymbTr export and SymbTr is **CC BY-NC-SA 4.0**, so serving one owes attribution, binds the derived file to **ShareAlike**, and holds the app to **NonCommercial for as long as it is there**. Attribution was the cheaper fix and was **rejected**: NC is not a labelling problem, it is a permanent constraint on what this can become, and unwinding it later means re-doing the pass. So `SAMPLES` is empty, the Sample dropdown is gone, and the app opens on the upload prompt. ⚠ Two of them were **also** compositions still in copyright under FSEK 5846 (life + 70) — *safalar getirdiniz* (Avni Anıl, d. 2008), *delisin deli* (Selahattin Pınar, d. 1960) — which attribution would not have fixed at all; the other two (Tatyos Efendi, Zekai Dede) are public domain, so the dataset licence, not the music, is what removed them. ⚠ **The files stay on disk, gitignored**: `npm test`'s 217-score round-trip, `smoke:editor`'s grace-note geometry section and the manual checks all read them through `?score=…`, and local use is not distribution. ⚠ The guard that matters is **`prune-dist.mjs` failing the build on any `.json` at the dist root**, not the empty `SAMPLES` — "no UI links to it" is not "not published", since everything under `public/` is served to whoever guesses the name, and a name list would miss a *new* score. Negative-tested: exits 1. Consequence for the smokes: four of them waited on `#app[data-ready="1"]` after a bare visit, which only ever worked because a sample auto-loaded — `editor-smoke` now asks for `?score=`, the three that upload their own input wait on `#page-input`. Full licence map: [THIRD-PARTY.md](THIRD-PARTY.md) | LOCKED (owner decision), **BUILT 2026-08-08, DEPLOYED 2026-08-09** and re-verified on every deploy since — `sample.json` answered 404 again after the 2026-08-11 feature deploy |
| 2026-08-09 | **AUDIO ASSETS: CC0 WHERE IT IS EASY, NC ACCEPTED ONLY WHERE CC0 IS THIN, AND NEVER ANYTHING UNLICENSED** (owner asked 2026-08-08 whether internet audio can be used while the app is free and replaced if it is ever monetised — **yes, that is legal and it works**). The framing was corrected on one point: **NC is not illegal**, it is a condition that costs a swap later, so the categories that actually block are **unknown-licence** files and **"no redistribution"** packs — the latter covering most commercial libraries *including paid ones*, because a web app serves the raw sample file and extraction is exactly what those licences forbid. Buying a better library is therefore not an escape hatch. Where this lands per instrument: **percussion and clarinet/violin from CC0** (no harder to find than NC, so NC there would buy a future obligation for no saving), **oud/tanbur/kanun from Karplus–Strong** (no files at all), **ney recorded by the owner** (CC0 coverage of Turkish instruments is thin to nonexistent, and a sampler needs steady 2-second tones rather than performance, so amateur playing and a phone mic clear the bar). ⚠ NC would partly undo the 2026-08-08 removal-not-attribution row in a different corner of the same app — defensible, but to be chosen deliberately. ⚠ Binding regardless of source: **no audio in the bundle** (loaded by URL like the weights), **`source` + `license` recorded per file from the first one** (these fail as "nobody knows where this came from", not as lawsuits), and each file added to `/THIRD-PARTY.txt` as it lands. Full rules: [features/README.md](features/README.md) | LOCKED (owner decision), **AMENDED 2026-08-09 by the row below** |
| 2026-08-09 | **THE AUDIO IS SOURCED, AND IT IS ALL CC0 — bendir, darbuka, tef, clarinet, violin and kanun, with no NC file anywhere** (owner: *"we can basically start with these instruments"*). The search was run and every licence read on its own source page: **VCSL** and **VSCO 2 Community Edition** (both CC0 1.0 on GitHub) cover percussion and clarinet/violin; two **CompMusic/UPF** Freesound uploads by Barış Bozkurt cover the Turkish side — bendir düm+tek strokes (140291) and **kanun, all chromatic tones, isolated** (211133). Three amendments to the row above, all evidence-driven. (1) **CC BY was missing from its licence table** and it is a *cleared* category, not a lesser NC: it costs one attribution line and is then safe commercially forever. That is where the only free ney material lives ([Freesound 194637](https://freesound.org/people/ajaysm/sounds/194637/), a Huzzam scale, CC BY 4.0) — a stopgap, not a replacement for the owner's own recording. (2) **Kanun is no longer forced onto Karplus–Strong**; K–S is still tried first, but a real recording now exists to A/B against. Oud and tanbur stay K–S — measured: tanbur has 1 CC0 hit, zurna 0. (3) **Ney's emptiness is now measured rather than assumed** — 11 CC0 hits, none usable. ⚠ Two traps recorded so they are not re-litigated: **VSCO 2 CE's `Readme.txt` contradicts its own CC0 `LICENSE`** (it asks for credit and for the samples not to be sold directly) — CC0 governs, the readme is a request, so credit anyway and avoid the mixed-provenance Ivy Audio/legacy piano material; and **CC0 on Freesound is the uploader's claim**, trusted here only because the two files are named research recordings from a university project. Per-file list, contents and the prep each needs: [features/audio-sources.md](features/audio-sources.md) | LOCKED (owner decision), **assets cleared, F0 not started** |
| 2026-08-08 | **THE TAKEDOWN ROUTE IS THE ISSUE TRACKER, NOT AN EMAIL** (owner). The footer needed a way for a rights holder to object; publishing `dikimenemre@gmail.com` on a public page was **rejected** in favour of the repo's issues URL — same reachability, no personal address for scrapers. The other two footer lines are **statements of fact about the code, not disclaimers**: uploads are not stored (`apps/server/src/index.ts` never writes an image to disk — it logs a size and a count), and the app holds no work of its own. ⚠ That first line becomes **false** the day the server persists an upload; `strings.ts` must change in the same commit. | LOCKED (owner decision) |
| 2026-08-09 | **THE APP'S SHEET MUST SAY WHAT THE APP PLAYS — the `sigTolerant` printing rule is now the RENDERER's, not the app's** (owner). In `Standart (ölçü boyunca)` the staff drew a same-direction alteration bare under the donanım, so a **koma bemol under a küçük-bemol signature printed as nothing and read as a küçük bemol while sounding a koma**. Not an edge case: **134 of the 213 bundled scores** have at least one letter whose signature sign hides a different same-direction alteration. The rule stays ON for renderer-driven pages, because a synthetic page has to imitate a real printed edition (which does print bare and leaves the intonation to the performer) — and goes OFF for a human, where a sign prints whenever the alteration differs from the one in effect at all. One flag (`SIG_TOLERANT` in `App.tsx`, off unless the URL carries `mode=`) feeds **both** `SheetView` and `buildStrips`, so pixels still equal labels on either setting; every render job and every `verify-labels` replay carries `mode=`, so **`strips_v4` and its labels do not move** (verified: identical glyph tally on `?mode=measure`, before and after). ⚠ Two gaps the owner keeps deliberately: the **makam bends the sound only** (uşşak, hüzzam — the 2026-08-07 row), and a **±2/±3 alteration is drawn snapped** to the nearest AEU sign because art-music notation has no sign for it (154 such notes in the bundled scores). | LOCKED (owner decision) |
| 2026-08-10 | **THE FEATURE TRACK OPENS, ON `main`, SCOPED TO F0 + F2** (owner: *"start planning adding other features"*). The post-beta ideas raised on 2026-08-08 were designed but unstarted; this starts them. **No branch** — the feature track touches `webAudioBackend.ts`, `usul.ts`, `App.tsx` and `TransportBar.tsx` while Round 3 touches `src/vision/`, `data/` and `docs/rung3/`, so the isolation a branch would buy already exists and the rebase ceremony would be pure cost for a single person. Scope is **F0 (the audio scheduler) + F2 (usul percussion) only**; F1 (instrument voices) and F3 (fingerboard tab) stay designed-not-started. ⚠ Stated out loud rather than glossed: **W10 exists to ask two friends what features to add, and this builds features before asking.** Defensible — the owner named these ideas themselves — but it is a choice. ⚠ This does not touch Editor Step 9, which remains Track A's next action. Plan: [features/README.md](features/README.md) | LOCKED (owner decision), **F0 + F2 BUILT 2026-08-11** |
| 2026-08-10 | **USUL PERCUSSION SHIPS SYNTHESISED, NOT SAMPLED — and the toggle is SEPARATE from the metronome** (owner). Two calls in one row. (1) Düm/tek/ka are made in `scheduleStroke` from an oscillator and a bandpassed noise burst, so F2 works end to end with **no download, no licence question and nothing to keep out of `dist/`** — the same "Level 0 first, find out whether more is wanted" argument F1 makes for its own timbres. The CC0 files cleared on 2026-08-09 are still the plan and swap in behind that one method, at which point the swap discipline (no bundling, `source`+`license` per file, `/THIRD-PARTY.txt`) starts to apply; **none of it applies yet, because no file is loaded**. (2) Percussion is its **own checkbox**, not a mode of the metronome: one marks the beats and the other plays a rhythm, and learning a usul against a steady pulse means wanting both at once. ⚠ The unresolved half is **data, not code** — 6 of the 10 stroke tables are the standard simple forms and 4 are marked `[derived]` (our reduction of that usul's beat grouping). `npm test` proves they are well-formed and **cannot** prove they are musically right; a wrong Düyek is obvious to a musician and invisible to every check here. The owner's ear is the gate: [MANUAL_CHECKS.md](MANUAL_CHECKS.md). | **(1) OVERTURNED 2026-08-11 by the owner's ear** — *"I do not like it much. We need to use real sounds."* The synthesis cleared its own written bar (düm and tek are tellable apart, and audible after the attack fix) and still is not good enough to want to play along with, which is the bar that actually mattered and was not the one written down. **CC0 samples are now required, not optional**; the swap discipline starts applying with the first file. What the bet bought is not nothing: F2's scheduling, the core track, the toggle, the volume stage and the checks are all correct and untouched by the swap, which reaches exactly one method (`scheduleStroke`) plus a loader — the seam it was designed around held. **(2) The separate-toggle half STANDS.** ⏭ Stroke tables still await verification, and now **after** the samples land — see the note in [features/README.md](features/README.md) |
| 2026-08-11 | **REAL SAMPLES ARE REQUIRED FOR F2 — the synthesised strokes are rejected** (owner, after listening). Reverses the "synthesised, not sampled" half of the row above. The lesson is about the **bar**, not the code: the plan wrote down *"düm and tek must be unmistakable from each other"*, the synthesis met that, and the owner still did not want to play along with it. A distinguishability bar was the wrong proxy for a musical one, and it was met on the way to failing. ⚠ Two second-order consequences. (1) **The stroke tables must now be verified AFTER the samples land, not before** — judging whether a Curcuna pattern is right through a drum you dislike conflates two questions, and the `[derived]` patterns are precisely the ones needing an unclouded ear. (2) **The swap discipline is now live**: no audio in the bundle, `source` + `license` per file from the first one, `/THIRD-PARTY.txt` as each lands, and a `dist/` guard mirroring `prune-dist.mjs`. None of it applied while nothing was loaded. Files, licences and the per-file prep each still needs: [features/audio-sources.md](features/audio-sources.md) | LOCKED (owner decision), **DONE 2026-08-11** — VCSL darbuka + frame drum are in, the synthesis is now the fallback only, and the stroke tables were **verified by the owner's ear the same day** (all ten accepted, the four `[derived]` included), which closes the second-order consequence (1) above. ⚠ The standard is one musician's ear, not a cited source |
| 2026-08-11 | **F1 (INSTRUMENT VOICES) IS THE NEXT FEATURE — AND IT IS WHAT THE FRIENDS ASKED FOR.** W10 went out, the friends used it, **they liked it**, and asked for **more instrument sounds**; the owner named violin, clarinet and kanun. So this is not a feature picked ahead of the question — **it is the question's answer**, and W10 is complete rather than half done. ⚠ Worth recording precisely, because the opposite was written down twice: F0 and F2 *were* built before anyone was asked, and that was a real tension. It resolves in the pleasant direction — the friends' independent answer points the same way the owner's own list did — but the resolution is **evidence that the guess was good, not evidence that asking is unnecessary**. Keep asking; F3 and any instrument past these three should still be aimed by what they say next. ⚠ The choice is also well-formed by luck or judgement: violin, clarinet and kanun are **exactly** the three with verified CC0 coverage ([features/audio-sources.md](features/audio-sources.md)). Ney has none and needs the owner's own recording; oud and tanbur are Karplus–Strong, code rather than files. | LOCKED (owner decision, backed by W10 feedback), **BUILT 2026-08-14** — all three shipped, deployed and accepted by ear |
| 2026-08-11 | **W10 IS COMPLETE, AND THE FEEDBACK LOOP THE WHOLE LADDER WAS BUILT FOR ACTUALLY CLOSED.** Two friends, a link, no ads and no in-app widget — just a conversation, as decided on 2026-08-05. Result: they liked it and asked for more instrument sounds. ⚠ The value here is not the compliment, it is that **the mechanism worked**: a release produced a directed answer, and that answer is now steering the next build. The 2026-08-02 plan ("freeze the model, release, then train against real feedback") was re-scoped on 2026-08-05 precisely so the release would not block the model — and both halves paid off, since Round 3 lost nothing waiting. ⚠ Non-claim: **n=2, and they are friends.** They were asked what to add, not whether the app is good, and a friend's "I like it" is not a usage measurement — `/decode` in the request log is ([METRICS-USAGE.md](METRICS-USAGE.md)). | LOCKED, **DONE 2026-08-11** |
| 2026-08-11 | **F1'S AUDIO IS UNCOMPRESSED, FULL-FIDELITY, AND LIVES IN A HUGGING FACE HUB REPO** (owner: *"we do not need to compress, I want the quality of audio to be high. We can keep the files in a repo"*). Three calls in one. (1) **No compression and no trimming** — the VSCO 2 / CompMusic files ship as recorded: full length, stereo, original bit depth. (2) **They therefore cannot ship with the app, and that is arithmetic rather than taste**: measured on the source, VSCO 2's clarinet `susLong` is 33 files averaging ~1.8 MB, so **one velocity across its 11 pitches is ~20 MB and three instruments land at 40–60 MB**, against a `MAX_TOTAL_MB = 60` dist already at 43.4. (3) **So a Hub repo serves them**, through the `VITE_AUDIO_URL` indirection built and tested hours earlier — the pre-registered trigger firing exactly as written, not a new decision. ⚠ **Not committed to git**, deliberately: ~50 MB of binaries in a **public** repo is permanent, and this project already has files stuck in its history needing a `filter-repo` rewrite to clear ([THIRD-PARTY.md](THIRD-PARTY.md)). The Hub is where the 211 MB of weights already live. ⚠ **`MAX_AUDIO_MB = 1` stays at 1** — it is what forces this, so nothing in F1 should touch it. ⚠ **The earlier "compression is F1's real obstacle" concern is WITHDRAWN, not solved**: it assumed size was a constraint, and the owner removed that premise. Its one durable finding survives — these are **7–10 second sustains**, which outlast almost any notated note, so **no sample needs looping**; a shorter trim would have *introduced* that problem. The 0.5–1 MB-per-instrument estimate this table used to carry was wrong by ~20× and is retired. | LOCKED (owner decision), **DONE 2026-08-13 for clarinet + violin** — files staged byte-identical (sha256-checked) and hosted through **`VITE_VOICES_URL`, not `VITE_AUDIO_URL`** (see the 2026-08-12 row below). Measured on the real files: clarinet 20.2 MB / 11 pitches, violin 35.4 MB / 15, and the sustains are **7–16 s**, so the no-looping finding holds with room to spare |
| 2026-08-11 | **AUDIO SHIPS FROM THE APP'S OWN `public/audio/`, behind `VITE_AUDIO_URL` — bending the "never bundle audio" rule, with the trigger to un-bend it written down now** (owner, asked directly: *"is it possible for maintaining if we keep all of the sounds in Netlify or should we keep them in a hub"*). Two drum kits are **660 KB** against a 60 MB dist budget; the never-bundle rule was written against **211 MB** of weights and does not carry here. What made that rule worth having is kept by other means: `apps/web/src/audio/strokeKits.ts` carries a `source` and `license` per file, and the loader resolves its base from `VITE_AUDIO_URL` exactly as `session.ts` does for `VITE_WEIGHTS_URL`. ⚠ **The indirection is built now although nothing uses it**, which is the whole decision: F1's instrument voices are ~4 MB *per* sampled instrument, so the current arrangement stops working partway into F1, and retrofitting the base-URL split afterwards would mean changing every call site and re-arguing this. ⚠ **The trigger is pre-registered and mechanical**: `MAX_AUDIO_MB = 1` in `prune-dist.mjs` fails the build, and its error message says to set `VITE_AUDIO_URL` and upload rather than raise the number. The migration path is **tested, not assumed** — `smoke:editor` was run once against a cross-origin stand-in host and fetched all 12 files under COEP. | LOCKED, but its **mechanism is OVERTURNED 2026-08-12**: the trigger never fired for the drums, and the voices got their own variable instead. See that row below |
| 2026-08-12 | **THE DRUMS STAY WITH THE APP; ONLY THE INSTRUMENT VOICES GO TO THE HUB, ON THEIR OWN `VITE_VOICES_URL`** (owner: *"drums essential to use, so we keep them at the app but we can upload the violin and clarinet audio from repo by user's request"*). This **overturns the mechanism** of the two rows above, not their intent: they assumed F1 would fire the pre-registered trigger and move `VITE_AUDIO_URL` to a Hub, and `loadStrokeKit.ts` said so in a comment. The defect that makes the merged version unshippable is that **`VITE_AUDIO_URL` is one base for the whole `audio/` tree** — pointing it at a voices repo takes the drums with it, 404s them, and drops percussion back to the synthesis the owner rejected by ear on 2026-08-11, **silently**, because the fallback still makes a sound and no local check would see it. So: two variables, two homes, two independent failure modes. ⚠ Consequence worth stating, because three files predicted the opposite: **`MAX_AUDIO_MB = 1` is now a PERMANENT guard on the drums, not a trigger that has fired.** A future kit that trips it is a decision to make, not an automatic move. ⚠ The reasoning behind the original trigger survives intact for anything that is *not* essential to playback. | LOCKED (owner decision) |
| 2026-08-13 | **F1's SAMPLE PITCHES ARE MEASURED, NEVER PARSED FROM A FILENAME — and doing it caught three things reading could not.** The method is the one `prepare_strokes.py` used on VCSL's unnamed darbuka files, applied to a library whose files *look* self-describing. (1) **VSCO's clarinet labels sit a whole octave below sounding pitch** (`DCClar_susLong_D2_…` sounds D3, 146.7 Hz, consistent across all 11 files) — parsing them would have transposed the entire instrument an octave and sounded plausible enough to ship. (2) The **violin needed no offset**, which is what makes the clarinet's answer trustworthy rather than a guess — the same control role the self-describing frame drum played for the darbuka. (3) **`DCClar_susLong_F#5` is mislabelled by a semitone at source**: it sounds F6 (1397 Hz, confirmed against its own second partial at 2794 Hz). It is kept under its original name, because the files ship untouched and the name is provenance; `hz` is what plays. ⚠ A fourth correction came out of the same pass and is not about audio: the pitch gaps are **5 semitones** at their widest (D–F–A# is a minor third then a *perfect fourth*), so the worst stretch is **±2.5 semitones**, not the ±1.5 the docs claimed nor the ±2 that planning assumed. Pinned by `tools/audio/voices-test.ts` so it cannot drift back into prose. | LOCKED (measured) |
| 2026-08-13 | **A SAMPLED NOTE IS PEAK-MATCHED TO THE SYNTHESISED NOTE IT REPLACES, AND WILL THEREFORE SOUND QUIETER.** The files ship byte-identical to the library's, so a voice cannot be levelled in its file the way a drum stroke is; the level is one `gain` per voice in the manifest, clamped so `gain × peak ≤ 0.98`. That guarantees F1 adds **nothing** to the limiter's workload — the opposite failure to F2's, where a 0.89 düm was *added* to a 1.0 note and clipped. ⚠ The cost is real and is not a bug: a `PeriodicWave` has a ~4–5 dB crest factor and a bowed sustain 10–12 dB, so peak-matched the voice is a few dB down in RMS. Loudness-matching would need a peak near 2.0, i.e. "patlamış" again with different arithmetic. **Escalation order if the owner wants more, pre-committed: raise `gain` to the clamp → add a `Çalgı sesi` slider → never `MASTER_GAIN`**, which was tuned against the drums and would move them too. The clamp sits at 0.98 rather than 1.0 for a dull reason worth keeping: both numbers are rounded to three decimals into `instruments.ts`, and at exactly 1.0 the rounding pushes the product over its own guarantee. | LOCKED, **pending the ear** (MANUAL_CHECKS check 24) |
| 2026-08-13 | **THE ATTACKS AND TAILS ARE TRIMMED — IN TIME, NOT IN THE FILE — AND SLURRED ARTICULATION IS ACCEPTED AS THE PRICE** (owner, after listening: *"for clarinet, it sounds just like breath for 16th notes. for violin the creak sound annoying. It is okey to have violin and clarinet sounds like slurred"*). Measured cause: the tone does not arrive for **76–1178 ms** depending on the file, and a 16th note at 120 BPM lasts 125 ms — so a short note played the attack transient and finished before the instrument spoke. Quantified on a 125 ms window, the clarinet's D2 sample was **35.8% tonal and 13.4× quieter** at the file start than 1.18 s in; short notes were therefore not merely breathy but far quieter than long ones, which is the sound the report describes. ⚠ **This does NOT overturn "the files ship untouched"** — the fix is a `skipS`/`endS` playback window per sample, so the wavs stay byte-identical, the sha256 table and the provenance claim stay true, and re-tuning by ear costs no re-upload. ⚠ **The cost is real and was accepted explicitly**: every note now begins mid-sustain, so nothing is tongued or re-bowed and the line sounds slurred. ⚠ Per FILE, not per instrument — the clarinet's own onsets run 111 ms to 1178 ms, so one constant would clip one note and leave breath on another. | **SUPERSEDED THE SAME DAY** by the row below: the amplitude threshold overshot and the trim is now per NOTE as well as per file |
| 2026-08-14 | **A PLUCKED NOTE IS NOT CUT AT ITS WRITTEN LENGTH — IT RINGS UNTIL THE RECORDING RUNS OUT.** A kanun has a separate course per note, so the previous string keeps sounding when the next is plucked; cutting each note at its duration was modelling an instrument that can stop a note, and a kanun cannot. Measured first, against the clipping rule: ~7 notes overlap at 16th-note speed but their peaks do not stack, summing to 0.45 against the limiter's 0.89. Two consequences ride with it — **re-plucking damps the string** (two copies of one recording combs, which the instrument cannot do) and **the natural end waits for the last tail** (an explicit Stop does not). All three are `plucked`-gated. | LOCKED, shipped 2026-08-14 |
| 2026-08-14 | **THE KANUN IS THE FIRST DERIVED VOICE, AND "SERVED UNMODIFIED" IS NOW A PER-VOICE CLAIM.** Its source is one 2-minute take of the whole range, so its 36 notes are cut out of it and are new files by construction — the sha256 identity check cannot apply. The **source take's** hash stands in its place, and `/THIRD-PARTY.txt` plus the Hub card say plainly which folders are untouched. The mp3 is decoded ONCE and never re-encoded. | LOCKED, shipped 2026-08-14 |
| 2026-08-14 | **A PLUCKED VOICE IS MEASURED DIFFERENTLY FROM A BOWED OR BLOWN ONE, AND THE EAR PROVED IT TWICE.** Pitch comes from the SPECTRUM, not the period (stiff strings stretch their partials, so YIN read 22 cents sharp on the bottom courses — a whole koma, enough to put a microtonal accidental on the wrong note); the attack is the RISE, not the first audible sample (a note cut from a take begins with whatever preceded it — 150 ms of it on `kanun_17_Cs5`). ⚠ Both are plucked-ONLY: the pitch refinement moved violin C7 by 22 cents, because vibrato has no peak to find. Guards now fail the run for both. | LOCKED (measured + heard) |
| 2026-08-13 | **THE TRIM IS PER NOTE AS WELL AS PER FILE, AND "WHERE THE BREATH ENDS" IS MEASURED SPECTRALLY** (owner, on hearing the first fix: *"we can hear some breath sound in clarinet… but we can trim less from the beginning. Or maybe we can trim differently for different duration of the notes, calculated by metronom speed and note duration, if it does not make the app slow"*). Both halves were right. **(1) The first attempt over-trimmed** because it used an AMPLITUDE threshold as a proxy for breathiness — that waits for full LOUDNESS, which arrives well after the noise stops, and it overshot in every file (158 ms where the tone settles at 92, 765 where it settles at 275, 1178 where it settles at 404). Breath is a *spectral* property, so `toneS` is now the first moment the sound is within 10 harmonic-percentage-points of that file's own settled tonality **and stays there**. ⚠ Relative, never absolute: a clarinet's sustain is only ~94% harmonic, so an absolute bar is unreachable for some notes. ⚠ "And stays there" is load-bearing: violin C7's bow-bite shows a momentary 89.6% blip at 50 ms before falling back to 36%, and first-crossing started the note immediately before the worst of the scrape. **(2) A short note starts at `toneS`, a long note at `attackS`** — the note has to be long enough that the recorded swell is at most `MAX_ATTACK_SHARE` (0.25) of it. ⚠ **A smooth blend was tried first and is wrong**: interpolating the start point lands *inside* the transient for any file with a long one, so a quarter note on C7 began at 64% harmonic and a half note at 33% — the creak reintroduced in the middle of the range while both ends measured fine. All-or-nothing means no note ever begins halfway through a scrape. **(3) Cost: none.** One subtraction and one comparison per note at schedule time, on numbers the manifest already carries — no extra buffers, no second decode, which is what rules out the obvious alternative of shipping two trimmed copies of every sample. | LOCKED (owner decision), **pending a re-listen** |
| 2026-08-05 | **PROPOSED: slice in the browser, decode on a scale-to-zero CPU server.** The owner's trigger is thermal — an in-browser page pegs the CPU ~20 s on an M4 — but the harder argument is that iOS Safari evicts the ~200 MB of cached weights after about a week of disuse, so mobile users re-download them repeatedly. GPU is rejected as the default (sparse traffic means nearly every request is a cold start, billing 20–60 s for ~2 s of work) and so are ads (Turkish CPMs put revenue at roughly $1–4 per 1,000 pages against $5–15 of GPU cost; on CPU inside the free tier the cost is $0 and ads are unnecessary). The W4–W6 slicer port is retained and moves to the client side of the seam. Plan, options and costings: [mvp/deploy.md](mvp/deploy.md) | **SUPERSEDED 2026-08-05** by the "DECODE MOVES TO A SERVER" row above, which took it the same day. Kept for the reasoning, in particular the GPU and ads arguments, which still stand. ⚠ Its *lead* argument does not apply any more: the iOS-Safari eviction case is parked with phones (see the scope row below), so the server rests on the thermal argument alone for now |
| 2026-08-15 | **EDITOR STEP 9 IS DROPPED — `Save JSON` STAYS** (owner: *"deleting save json can be omitted"*). This supersedes the 2026-08-07 row above, which decided the button would go and was never acted on. The deletion had a real cost and no benefit left: **`smoke:editor` reads the edited document by clicking `#save-json`**, so removing the button removes the check's only way to see what an edit actually did, and it would have to be replaced by a new DOM seam before the button could go. Against that, the button is one line of UI nobody is harmed by. ⚠ What does NOT come back is the 2026-08-07 row's *reasoning*: the export is still unused, and the editor's honest justification is still "a friend with a wrong note should be able to fix it", not labelling throughput. **The editor's list is now complete** — steps 1–8 and 10 are built, deployed and checked; there is no step 9. | LOCKED (owner decision) |
| 2026-08-15 | **F3 IS THE VIOLIN, AND ONLY THE VIOLIN** (owner). Not "one instrument first" as an order to work down a list — nothing past the violin is committed. Reasons: it is fretless, so a position is the exact formula `length × (1 − openStringFreq / noteFreq)` rather than a fingering table; **Keman is already a shipped F1 voice**, so a user hears and sees the same instrument; and the friends have just said they liked the voices, which is the only aim this track has. The winds' lookup tables and the tanbur's fret table stay in [features/README.md](features/README.md) as *design*, not as a queue. ⚠ **Still open and blocking correct markers: the open-string tuning** — standard G3–D4–A4–E5 or a Turkish keman tuning. A repertoire question, the owner's call, asked the same day. Everything else in F3 takes the open strings as data and can be built without the answer. | LOCKED (owner decision) — **F3 BUILT 2026-08-16**, and the ⚠ open-string question it flags was **ANSWERED** by the row below |
| 2026-08-16 | **F3's OPEN STRINGS ARE STANDARD Sol–Re–La–Mi, AND THE TABLE STAYS OPEN** (owner). This closes the blocking question left open by the 2026-08-15 row above. The four frequencies live in `VIOLIN_TUNINGS` in [../packages/core/src/fingering.ts](../packages/core/src/fingering.ts) as **data**, so adding a Turkish scordatura is a row and touches no geometry; the picker is built and is hidden while there is only one entry, because a one-option select is dead UI and inventing a second tuning would be a repertoire claim this project has not made. ⚠ The values are derived on **this project's 53-TET grid** (a fifth = 31 commas), not twelve-tone equal temperament: open Sol is 195.571 Hz, not a tuner's 196.00. That ~4 cents is a fifth of a koma, and it is what makes an open string land at ratio 0 *exactly* for the note that should be played open. ⚠ Consequence to expect rather than discover: Turkish notation transposes down a fourth, so low written passages sound **below** the open Sol and get no marker at all (`data-finger-state="out-of-range"`) — the honest reading, and the strongest argument for a lower tuning later. | LOCKED (owner decision) |
| 2026-08-16 | **The fingerboard takes App's `timeline` and never rebuilds it.** A view that calls `buildTimeline(doc)` itself gets **written** pitches — the makam bend and the transpose are applied in App's memo, not in the document. `PianoRoll.tsx` does exactly that and it is harmless for a roll; for a finger position it would be wrong on precisely the makams the feature exists to demonstrate. Same reason nothing on the overlay is labelled with `NoteEvent.noteName`, which `withKomaDeltas` leaves stale on purpose. ⚠ This is a standing rule for any future view that shows *pitch*, not a note about one file. | LOCKED |
| 2026-08-15 | **F3's ARTWORK MAY BE A THIRD-PARTY IMAGE WHOSE LICENCE HAS BEEN READ — "own artwork" is amended, not abandoned** (owner). [features/README.md](features/README.md) required the instrument to be drawn as SVG or photographed by the owner, and that read as *you must draw a violin*, which was the single reason F3 looked expensive. What the rule protects is **provenance**, after the 2026-08-08 pass found instrument and score images with none — and a CC0 file whose chain has been followed satisfies it exactly. Landed the same day: `apps/web/public/instruments/violin-vl100.png`, Wikimedia Commons `File:Violin VL100.png`, **CC0 1.0**, front-and-side on a transparent background; its source `File:Violin VL100.jpg` is **the same author's own public-domain photograph**, so both links in the chain were checked rather than assumed. ⚠ Two things stay forbidden: an image with **no** licence, and trusting a user-upload site's CC0 tag without following the derivation. ⚠ And note the asymmetry this creates — `prune-dist.mjs` structurally guards scores and audio and has **no equivalent test for an image**, so nothing but a doc row stops the next one shipping unlicensed ([THIRD-PARTY.md](THIRD-PARTY.md)). | LOCKED (owner decision) |
| 2026-08-17 | **A PAGE CHOOSES ITS BINARIZER ONCE, AND THE PALE-LINE FALLBACK IS GATED TWICE.** `binarize_ink` is global Otsu — a **two**-class split — and a page with black noteheads and pale staff lines has **three** classes (paper / line / note), so Otsu puts the line on the paper side and erases it: measured on a 1056 px re-scanned photocopy, paper 253, staff lines 219, noteheads 27, Otsu threshold **156** → 0 staves, 0 strips, while the notes survived intact. The damage compounds, because `estimate_skew` gates on the same staff-line rows: with the lines gone the sweep maximised noise and returned **+7.5°** where the truth was about −0.25°, so the rotation destroyed what Otsu had left. `page_binarizer` now picks the binarizer **once per page** (a property of the page, not of the rotation — the skew sweep binarizes 41 times) and `estimate_skew` is handed that choice. ⚠ **The guard is deliberately hard to trip, because a wider one is actively harmful and that was measured, not guessed**: an earlier version fired whenever no row spanned half the page width — true of many perfectly readable pages, since `detect_staves` itself needs only 20% — and over the corpus it fired on **65 working pages, pushing 62 of them from 9–11 staves to ZERO**. ⚠ **The shape check is not optional either**: most pages that reach the fallback are LYRICS pages with genuinely no staff, and a more sensitive threshold groups their text baselines into "staves" with interlines of 90–471 px against a real one's 13–17, so the result is believed only when interline/height falls in `[0.0025, 0.02]`. ⚠ **`>=`, not `>`, in the paper level** (`median(gray[gray >= percentile(gray, 60)])`): a scan whose paper is one flat value has nothing strictly above its own 60th percentile, so `>` medians an empty array → NaN → an empty ink mask. That shipped once and read as working only because deskew pads its rotation with white; `tools/vision/binarize-test.ts` pins it in `npm test`. Numbers and the reproducible sweep: [METRICS-SLICER.md](METRICS-SLICER.md). | LOCKED |
| 2026-08-17 | **LEVER 4 (RENDERER DIVERSITY) COMES AFTER LEVER 1 AND BEFORE THE CONTENT WORK** (owner). This re-orders the 2026-07-27 decision that made the eighth/quarter-note mix and bar-line density Round 3's content change: that work is **not cancelled**, it moves behind a second engraver. Owner's reasoning, which matches the run of results in [rung3/levers.md](rung3/levers.md): every "do we DRAW something wrong?" check has come back no or null, so the *make-our-pixels-more-realistic* axis is at diminishing returns, while all 40,826 strips remain one engine, one font, and a staff spacing whose standard deviation is **zero**. The cheapest second domain is one we half-own — `tools/render/lilypond.ts` already emits the label language, so real LilyPond is a different engraver at no labelling cost. ⚠ Its hard constraint is the pixels-must-equal-labels rule, and `verify-labels.ts` is the gate any second renderer passes before its corpus is trainable. ⚠ The content work also keeps its own open problem regardless of order: our strips are already **wider** than the real pools' and denser music widens them further, so it needs a strip-width target rather than only a note-value histogram. | LOCKED (owner decision) |
| 2026-08-17 | **COLLECT REAL PAGES BROADLY FROM MORE SOURCES NOW** (owner), beyond neyzen.com and notaarsivleri.com. ⚠ **A cost was raised and overruled, and both halves belong here.** The argument against: **2,486 real page PNGs already sit on disk unlabelled** ([METRICS-CORPUS.md](METRICS-CORPUS.md)), so collection is not the bottleneck — labelling throughput is — and [rung3/levers.md](rung3/levers.md) Lever 3 says the first move on real data is *cleaning* what we have, because audited pitch-level label error lands on the very axis Round 3 targets. The narrower alternative offered was to grow the **exam** only, where more editions directly buy resolution on a primary floor measured over 46 pages at ±12 points. The owner chose broad collection anyway, so that is the plan; what it does **not** do is relieve the labelling bottleneck, and a bigger unlabelled pile is the expected outcome unless labelling gets cheaper. ⚠ Two rules that do not bend with it: a new source's licence must be read before anything is redistributed ([THIRD-PARTY.md](THIRD-PARTY.md)), and **exam pieces must stay refused** by any flow that feeds training. | LOCKED (owner decision) |
| 2026-08-17 | **THE TUPLET DIGIT'S POSITION IS RE-OPENED AS A MEASUREMENT, AND NOTHING IS RENDERED ON IT YET** (owner, from a real page). The owner saw the "3" set **between the curve and the noteheads** — inside the arc's concavity — which is neither shape this project has drawn: the legacy arm was a continuous arc with the digit *outside* above the apex, and the current mark is a *broken* arc with the digit in the gap at arc height. ⚠ It is a potential counterexample to a line recorded as settled: [rung3/tuplets.md](rung3/tuplets.md) has **16 of 16 marks across ~11 editions breaking the arc**, and *"not one continuous arc with a floating digit exists in the real pools"* — a sample small enough that one genuine counterexample is possible. **The decision is to measure with `tuplet_mark_probe.py` and render nothing**, because a third mark style in the same render as the geometry change would make Round 3 unattributable for the third round running. If it confirms, the shape becomes a **per-piece coin** like bracket-vs-curved (now 90/10), i.e. renderer diversity rather than a correction — which is also why it sits naturally beside the Lever 4 row above. ⚠ Temper the expectation: the mark-shape A/B came back **null** (p = 0.688), and the surviving `\tup3` lead points away from the renderer entirely (recall 96% → 81% by position within a passage). | LOCKED (owner decision) |
| 2026-08-17 | **HANDWRITTEN MANUSCRIPT IS A DEFERRED CATEGORY, NOT A COVERED ONE** (owner). The scanned sources do contain **real handwritten** sheets, mostly old — this project had assumed everything was printed. The owner's call: **mark them `bad` in the review queue and revisit later.** No code needed; the `bad` verdict already exists and already means "this crop is not usable as it stands". ⚠ Why it is worth a row rather than a note: handwriting varies per scribe in a way print does not, **0 of 40,826 practice strips resemble it**, and it would need its own pages, its own coverage argument and probably its own exam. Recording it as *deferred* stops it being quietly assumed to be covered by "real pages". ⚠ Do not let handwritten strips leak into a printed-page metric pool — they would depress it for a reason no printed-page change can fix. | LOCKED (owner decision) |
| 2026-08-17 | ~~**COLLECTION MOVES AHEAD OF LABELLING, AND IT IS AIMED AT CLASSICAL FORMS**~~ ⛔ **OVERTURNED THE SAME DAY BY THE OWNER, WHO RETESTED THE APP.** The decision rested on an owner report that the app fails on classical repertoire, including on clean computer-set PDFs. Retested by hand, it does not: classical pages read **no worse than songs** — *"it is not read it well but it cannot read the songs as well."* The premise is withdrawn by the person who supplied it, so **there is no form axis and no form-aimed collection.** ⚠ It had already been half-retracted hours earlier on mechanism: beste/nakış are *simpler* than şarkı on every countable property (that half stands). ⚠ **The other half was CIRCULAR and is withdrawn (2026-08-17)**: `nd` is not scan degradation — `emit_strip_labels.py` defines it as `lev(label_ids, decoded_ids)/len(label_ids)`, a label-vs-decode disagreement — so "their errors track `nd`" restated the finding rather than explaining it. The overturn does not rest on it; the owner's retest is independent. The measured ranking is kept as a number in [METRICS-EXAM.md](METRICS-EXAM.md); the reasoning, the supply ceiling and the process lesson are in [log/superseded.md](log/superseded.md). ⚠ **This does NOT touch the broad-collection row above**, which stands on its own reasoning. | ⛔ OVERTURNED 2026-08-17 (owner retest) |
| 2026-08-17 | **"PUBLISH FOR CLEAN PAGES FIRST" IS TO BE MEASURED, NOT ASSUMED** (owner). The idea: ship publicly for clean computer-typeset input, treat scans as a known limitation, keep improving. ⚠ **The number that made it look easy was misread and the correction belongs with the decision**: the exam's easy tier reads **94.8%**, but that is **per-class accidental** accuracy — it never measured pitch or duration, which are 68% of user corrections. The owner's own hands-on report is that clean online PDFs *do* fail, which is evidence directly against the optimistic reading. ⚠ **Corrected 2026-08-17**: this row originally read "especially on classical forms". The owner retested and found no form difference — classical reads no worse than songs — so the failure is **general, not form-specific**, and the evidence against the optimistic reading is if anything broader. ⚠ Two further obstacles: the clean tier is **10 pages**, far too few to gate a launch on, and we do not know the mix of what users upload (n=2). What to measure: page-level edit counts (not accidental recall) split by page type. ⚠ Do **not** re-add a musical-form split — that axis is dead ([log/superseded.md](log/superseded.md)). Until then the launch stays gated on the signed Round-3 floor. | OPEN — measurement owed before any launch scope changes |
| 2026-08-18 | **LABEL THE BORN-DIGITAL PAGES FIRST — "teach the clean modern sheets first"** (owner). The first labelling batch was cut over the whole corpus and its ranking returned exactly what it is built to return, the most damaged pages: old scans and **real handwritten manuscript**, which is a deferred category (row above). That batch is **parked, unlabelled**, and `batch2` was cut with `--clean` instead — 52 pages / 1,497 strips drawn only from **born-digital** pages, ranked by the same structural evidence *inside that tier*. ⚠ Why this filter and not another: born-digital is a **file-format fact** (page 1 embeds no raster image, drawn with vector operators), so no scan and no handwriting can enter — unlike every other difficulty signal here, which is either circular (`nd`) or worse than random (decode confidence). The census that made it possible also corrected a repeated claim: **0 of 1,055 neyzen PDFs are born-digital**; 88 of 1,000 nota PDFs are ([METRICS-CORPUS.md](METRICS-CORPUS.md)). ⚠ **Know what this aims at**: the OPEN "publish for clean pages first" question above, **not** the signed Round-3 floor — that exam is 41 nota / 26 neyzen pages and only 18% easy tier, so clean-page labels cannot be expected to move it much. ⚠ Anything cut this way is **training only**: the ranking selects the worst pages in its tier, so exam growth still needs a random/stratified sample ([rung3/labeling-queues.md](rung3/labeling-queues.md)). | ⛔ **SUPERSEDED 2026-08-19** by the row below — the tier was right for the question it aimed at, wrong for the exam |
| 2026-08-19 | **LABEL THE SCANNED PAGES, NOT THE BORN-DIGITAL ONES — the goal is now "good enough on scans too"** (owner). This supersedes the 2026-08-18 row above **on sequencing only**; the born-digital census and the reasoning for preferring a file-format fact over a heuristic both stand. ⚠ **Two measurements decided it, and neither existed when the earlier call was made** ([METRICS-CORPUS.md](METRICS-CORPUS.md)): the census run against the exam says **93% of exam pages are scans** (62 of 67; only 4 of 45 pieces are born-digital), so `batch2` was cut entirely from the tier supplying **7%** of what Round 3 is graded on; and the owner's own verdicts on it read **59 ok / 8 fix / 1 bad — a ~12% fix rate** against **30%** in the scanned nota pool. Since a batch row is seeded with the decode, an `ok` changes the training data by nothing, so ~88% of those looks bought nothing. ⚠ **`batch1` is NOT the answer and stays parked**: it is the most-damaged pages *corpus-wide*, so it concentrates the deferred handwritten manuscript, and 10 of its 52 pages are born-digital anyway. The plan is a **`batch3` cut with a `--scanned` filter** (the inverse of `--clean`, the same file-format fact, **not yet built**), a page-level handwriting triage before any strip work, `check_crop_staleness.py --pages-from` as always, and a **100-row probe of fix-rate and bad-rate before committing 1,500 strips** — the scanned tail is where the `realval-hard` round lost 33% of its crops as unusable. ⚠ Merge `batch2`'s 68 verdicts back first or they stay stranded in the batch CSV. ⚠ Handwriting stays **deferred** (2026-08-17 row); this decision does not reopen it. | LOCKED (owner decision) |
| 2026-08-19 | **LEVER 6 CLAUSE 2 KEEPS ITS EXCLUSION; ONLY THE WRITTEN REASON CHANGES** (owner). The staccato arm's no-regression clause covers **easy+mid tiers only**, and its stated reason — "hard-tier dropped dots are scan degradation, `nd` up to 1.14" — is void, because `nd` is `lev(label_ids, decoded_ids)/len(label_ids)`, a label-vs-decode disagreement ([METRICS-CORPUS.md](METRICS-CORPUS.md)). The clause is **signed, and re-opening a pre-registration mid-round is what makes a round meaningless**, so the gate does not move. The exclusion survives on two reasons that do hold and are recorded here in place of the void one: hard tier carries **~12 real-dot instances in total**, too few to gate on in either direction, and its gold is the least reliable pool we own. Hard tier stays **reported, never gated on**, exactly as the clause already says. ⚠ This is the settlement of the item flagged 2026-08-17 and it was taken **before** the arm was trained, not after seeing a result. [rung3/levers.md](rung3/levers.md) Lever 6. | LOCKED (owner decision) |
| 2026-08-19 | **AUGMENTATION GETS A SCAN PROFILE, AND IT IS THE NEXT TRAINED ARM — ahead of both remaining render items** (owner). `src/vision/augment.py` has exactly **two** profiles, "screenshot" (65%) and "photo" (35%, the camera pipeline: paper texture, shadows, lighting gradient, perspective). **There is no scan profile**, and a flatbed scan of a TRT-era print is neither: flat lighting and no perspective, but speckle, broken thin lines, ink spread, bleed-through, small skew and threshold damage. ⚠ [rung3/levers.md](rung3/levers.md) said the degradation axis had been "varied heavily" — true, but varied for the **deployment** distribution, not for the exam's medium, and `augment.py`'s own comment says *"Revisit against real usage at Rung 3"*, which never happened. It goes first because it needs **no re-render, no new labels and no render slot**, and it aims at the 93%. ⚠ **The trade is real and is accepted knowingly**: `PHOTO_SHARE` was set from the owner's report that real uploads are mostly web screenshots (2026-07-06), and that comment explicitly warns against pushing the mix for "harder training". Aiming augmentation at scans optimises the **exam**, which may not be what the app's own users upload (n=2 on that question — [METRICS-USAGE.md](METRICS-USAGE.md)). The scan profile is therefore **added beside** the two, not substituted for them, and the mix is pre-registered before the arm runs. ⚠ Order of the trained arms that follow, one variable each: scan profile → one-measure-per-strip render → staccato arm → the final Round-3 model. **No two together** — Round 3 has been unattributable twice already. | LOCKED (owner decision) |
| 2026-08-19 | **THE SCAN PROFILE'S MIX IS screenshot .55 / photo .20 / scan .25, and the arm's CONTROL IS A CHECKPOINT WE ALREADY HAVE** (owner). The scan share is taken mostly out of `PHOTO_SHARE`, because 0.35 was itself a guess from "uploads are mostly screenshots" — the share with the weakest evidence pays for the new one, and the screenshot case stays dominant. Signed **before** the augmentation code was written. ⚠ It is a `STACCATO_RATE`-shaped hazard: a **chosen** number, not a measured one; counting the real corpus's medium mix is how to replace it. The control is `data/checkpoints/r3-tupnew-stage2-best` — same corpus (`strips_v5_tupnew`), split, recipe, steps and seed — so the arm costs **one** GPU run rather than two, and the mix is the only difference. ⚠ Consequence: the pairing is by RECIPE, not by a same-runtime re-run, and it turns out no augmentation is bit-reproducible anyway (next row). [rung3/scan-profile.md](rung3/scan-profile.md) | LOCKED (owner decision) |
| 2026-08-19 | **LEVER 7 IS SCORED ON THE MEDIUM SPLIT (`_realval_v2_scan` vs `_realval_v2_borndigital`), NOT ON THE HARD TIER ITS DRAFT NAMED** (owner, on a measurement taken before signing). The draft's premise held — 97 of the hard tier's 110 strips are scan-sourced — but the tier is the wrong instrument: **all 110 of its rows are gold seeded with `round2-stage2-best`'s own decode and then confirmed by a person**, so descendants of that model are flattered, and it shows: hard scores **SER 0.051 against mid's 0.145**. The medium split is a file-format fact, carries 202 vs 65 strips, and separates **0.107 from 0.024** SER along exactly the axis a scan profile changes. The tier read is still produced and reported, so the two instruments can be seen to disagree. `scripts/rung3/split_realval_tiers.py` builds both; [METRICS-DIAGNOSTICS.md](METRICS-DIAGNOSTICS.md). | LOCKED (owner decision) |
| 2026-08-19 | **NO AUGMENTED IMAGE IN THIS PROJECT IS REPRODUCIBLE FROM A SEED, and it never was.** Measured while building the scan profile: **albumentations 2.0.8 seeds its transforms at construction from OS entropy**, so `random.seed` / `np.random.seed` do not control them — the same module run twice produces different pixels. `--seed` fixes the model init, the shuffling and `augment.py`'s own generator, and nothing else. ⚠ What this does NOT damage: an A/B is a comparison of **distributions**, and both arms drew from the same one; every paired result on record stands. ⚠ What it does mean: "same seed, same data" is not a claim this project may make, and a run cannot be replayed exactly. Passing `seed=` to each `A.Compose` would fix it and is **not** done now — it would be a second change inside a running arm. Recorded in `augment.py` beside `SCAN_SHARE`. | LOCKED (a measurement, not a choice) |
| 2026-08-19 | **`batch3` EXCLUDES STALE-CROP PAGES RATHER THAN LABELLING INTO THEM** (owner). 17 of the 71 pages the scanned ranking offered — **24%** — no longer re-slice to the same crops, so a verdict on them would not survive a re-slice. They are excluded, alongside 11 handwritten pages, leaving 54 pages / 1,499 strips with **0 void**. Reason: labelling time is the scarce resource and **B8 (re-emitting the training pools from the new crops) is still an open decision**, so a re-slice is a live possibility, not a hypothetical. ⚠ The cost is accepted knowingly — the excluded pages were the highest-evidence ones in the tier, and the batch's evidence/page fell 46.3 → 43.0 across four cut/check rounds. ⚠ The handwriting rule is **narrow on purpose**: drop free-hand pen manuscript, KEEP professionally hand-copied editions, because most of the corpus and the exam are exactly that ([rung3/labeling-queues.md](rung3/labeling-queues.md)). | LOCKED (owner decision) |
| 2026-08-19 | **⛔ LEVER 7 RAN AND IS A NULL — reported as a null, and the mix is NOT re-tuned.** The scan-profile arm moved nothing on the medium it was built for: paired over 197 strips of `_realval_v2_scan`, `best` **+0.071** edits/strip (95% CI [−0.203, +0.335], sign test p = 0.105) and `last` **+0.010** (CI [−0.198, +0.208], p = 0.488) — both checkpoints, same answer, so the result does not turn on the selector we already distrust. The no-regression clause on born-digital pages **passes**, with point estimates favouring the arm (−0.092 / −0.169) that also span zero. ⚠ **This is an INFORMATIVE null**: the interval excludes anything better than a **~5% reduction** in corrections, so the effect is absent rather than merely unresolved. ⚠ **Do not propose a different scan share "to see if that works"** — the share was signed before the code was written precisely so a disappointing result could not be answered by moving it, and a number tuned against this pool stops being evidence. ⏭ **One question is left open and it is the owner's**: whether the profile stays ON in the final Round-3 model. It costs nothing and did not hurt; but "keep it because it didn't hurt" is a choice made on two nulls. [rung3/scan-profile.md](rung3/scan-profile.md) · [METRICS-DIAGNOSTICS.md](METRICS-DIAGNOSTICS.md) | LOCKED (a result) · one clause OPEN |
| 2026-08-19 | **THREE RESULTS NOW SAY THE "MAKE THE PIXELS MORE REALISTIC" AXIS IS SPENT, and the third is a trained arm.** The tuplet-mark A/B was null (p = 0.688), the second engraver's domain gap was null without earning an arm, and the scan profile is null (p = 0.105 / 0.488). [rung3/levers.md](rung3/levers.md) opened by *inferring* diminishing returns from pre-render probes; that inference now has a trained model behind it. ⚠ What this does NOT touch: arms 2 and 3 are on different axes — what the encoder is **given** (one measure per strip) and a symbol it has **never been shown** (staccato) — so they stand as ordered. ⚠ What it should stop: a fourth realism arm proposed on the grounds that the first three "nearly" worked. The axes with no trained evidence against them are decoding (Lever 2), the training recipe (Lever 5) and real-label quality (Lever 3, the live labelling). | LOCKED (a reading of three results) |

## Model and training

| Date | Decision | Status |
|---|---|---|
| 2026-07-01 | **Fine-tune the pretrained `Flova/omr_transformer`** (Donut-style, image→LilyPond) rather than training a CRNN+CTC. | LOCKED — CRNN fallback formally retired 2026-07-07 on accuracy grounds |
| 2026-07-02 | **Full fine-tune at small LR**; freezing is a memory fallback only. | LOCKED |
| 2026-07-02 | **Split train/val BY PIECE, never by strip** — strips of one piece are near-duplicates. | LOCKED |
| 2026-07-03 | **No Western rehearsal data** in fine-tuning; repeat/nav coverage comes from self-rendered Turkish strips. Any doc still saying "Western rehearsal mix" is stale. | LOCKED (owner decision) |
| 2026-07-02 | **Faithful + signature label scheme**: label only what is *drawn*; `\sig … \sigend` prefix on row-start crops. | LOCKED |
| 2026-07-08 | **New tokens are appended at the END of `ADDED_TOKENS`** so every earlier id stays stable across checkpoints. | LOCKED |
| 2026-07-06 | **Augmentation is screenshot-dominant** (`PHOTO_SHARE = 0.35`) because real uploads are mostly web screenshots. | LOCKED |
| 2026-07-06 | **Augmentation stays on-the-fly in the loader**, never baked into rendered files. | LOCKED |
| 2026-07-21 | **`--every-share` defaults to 0.15**; the 3-arm sweep around it was cancelled before any run (power + premise collapse). | LOCKED at the default; sweep CANCELLED |
| 2026-07-21 | Boost `\komaSharp`/`\kucukSharp` share in the re-render. | **OVERTURNED** — komaSharp was already over-represented and precision-bound |
| 2026-07-21 | `bakiyeSharp→kucukFlat` enharmonic respell to fix the kucukFlat gap. | **HELD** — the gap is a makam-mix artifact |
| 2026-07-26 | **All four AEU sharps get the thinner real-print bars**, not just the broken one — otherwise the model learns a thickness cue that real pages don't have and bar *count* stops being the discriminator. Flats untouched (89–92%, healthy). | LOCKED |
| 2026-07-26 | **`drawThinSharps` ships OFF by default** (`?thinsharps=1` / `--thin-sharps`) so an A/B against `strips_v3` stays possible. | LOCKED |
| 2026-07-21 | Fix the microtonal sharps by balancing **inline** frequency and putting koma/küçük/bakiye side by side in one strip. | **OVERTURNED 2026-07-26** — küçük is scored 32:1 in the key signature, and the `sigTolerant` printing rule means real pages barely print it on a note. Re-aimed at glyph fidelity + signature-context diversity ([METRICS.md](METRICS.md)) |
| 2026-07-26 | **Enharmonic respell `\bakiyeFlat` → `\kucukSharp`** to manufacture küçük examples. | **DROPPED before use** — prints a spelling real editions don't use, and küçük precision is already 100%, so it could only fall |
| 2026-07-26 | **Exam pieces are excluded at SELECTION time**, not only by the train-time guard: `select_pieces.py --exam` refuses them by SymbTr id, because a synthetic render of an exam piece is training on that piece. | LOCKED |
| 2026-07-26 | **Extend the piece selection instead of re-rolling it** (`--keep`): re-running the greedy coverage pick would change which pieces are held out and invalidate the existing split. | LOCKED |
| 2026-07-27 | **Round 2 is NOT shipped.** The exam headline regressed (78.0 → 73.9% mean AEU F1) even though SER, exact-match and 9 of 11 floors improved. | **OVERTURNED the same day** — re-scoring showed the regression was a metric artifact of `\komaSharp` (n=14); Round 2 is flat-to-better on every low-n-robust measure. Ship decision reopened |
| 2026-07-27 | **Every eval reports MICRO (token-pooled) and MACRO≥30 beside the macro mean**, and `rescore_headline.py` back-fills them for past runs from stored `per_class` blocks. Reason: a 3-gold class swung the macro headline +11pp in Round 1 and a 14-gold class −4pp in Round 2, neither reflecting reading ability. | LOCKED |
| 2026-07-27 | **The project goal is now USER EFFORT, not model accuracy: ≥90% of pages need ≤5 corrections, and the app shows where they are.** (Stated on the SHARE of pages after the baseline came in at median 5 / mean 12.2 — a median target was already met the day it was written.) "85% per-class accidental mean" is demoted to a diagnostic. Reasons: it does not measure whether the app is usable (Round 2 got better for the user while that metric got worse); we are already at 84.8% on its low-n-robust form, so the headroom is two rare classes; and error *localisation* — never built — is worth more than the remaining accuracy, because finding 5 unknown errors in 250 notes costs more than fixing them. Fairness bar retained: macro≥30 ≥85%. | LOCKED (owner decision) |
| 2026-07-27 | **The macro mean STAYS the pre-registered headline; micro does not replace it.** Micro was computed after the fact and happens to flatter us (~85% vs 74%), so promoting it now would be moving the goalposts to fit a result. Micro/macro≥30 decide **whether a change helped** (they are stable); macro stays the **bar** (a rare mark misread is still a wrong note). The real fix is more `\komaSharp` gold in exam v3, so macro becomes trustworthy rather than replaced. | LOCKED |
| 2026-07-28 | **Round 3's "render the odd crop shapes" change is DROPPED.** Its cost is real (crops with ≤3 notes are 5.5% of exam strips and 20.8% of corrections), but its stated mechanism is wrong: only 1 of the 8 note-free crops in existence invented notes, against a pre-registered ≥50% bar. The model cannot *read* them; it does not hallucinate. The shape is the slicer's own deliberate trade-off (`window_measures` keeps a narrow clef+signature crop rather than lose content) and the current slicer already halved it, 3.4% → 1.4% of crops. Teaching the renderer to imitate a crop we control and are removing is backwards, and costs a full re-render. | DROPPED |
| 2026-07-28 | **Cutting wide crops narrower is DROPPED.** Wide strips (>1200 px) are 13.8% of the exam but 28.6% of corrections at 2.5× the baseline per-token rate, so the lever looked large. Measured: splitting them at a zero-ink gutter against identical gold gave **+31.8% edits** (132 → 174), worse on 15 strips and better on 5. **19 of the 45 have no internal bar-line**, so a measure-aligned split is impossible. `MAX_STRIP_W` is not a lever; width is mostly a symptom of dense measures. | DROPPED (measured) |
| 2026-07-28 | **Beams will NOT be thinned.** The sharp-bar fix (Bravura 22% too thick) does not generalise: our beams sit at the engraving standard 0.500 S and real print is *heavier* (0.567–0.765 S), so thinning would move us away from reality. ⚠ This measured thickness, **not grouping** — `USUL_BEAM_GROUPS` remains unvalidated and quarantined. | LOCKED |
| 2026-07-28 | **A space before the duration `32` is NOT a decode error.** The model writes `f'' 32`; measured, that tokenises to exactly the same ids as `f''32` (`[19, 1, 37, 95, 35]`) because the tokenizer splits the octave marks from `32` regardless — the glued form even decodes back with the space. `eval_omr.py` scores in id space, so they are identical. ⚠ Holds for `32` ONLY: `f''16`/`f'' 16` and `f''8`/`f'' 8` differ in id space. Written convention stays glued (0 spaced labels across every pool); `build_realval_v2.py --build` normalises. Do not spend review time "fixing" it. | LOCKED |
| 2026-07-29 | **The slicer's windowing constants must be retuned before the real pools are re-emitted.** The 2026-07-25 fix stopped truncating staves (old kernel pushed `x0` 70-490 px right), so rows now carry more music — but `MEASURES_PER_STRIP = 3` / `MAX_STRIP_W = 1450` predate it. Same model, same 6 pages: crops over the 59-id label budget went **20.9% -> 31.9%**, and the emitter drops those as `over_budget` (one probe page: 25 of 30 dropped, 11 of them over-budget). Sweep of usable yield: 1 measure/window **107**, 2 -> 90, 3 -> **79** — monotonic, current value worst. ⚠ Do NOT simply set it to 1: 'usable' counts budget fit only, and ignores lost context, more stitcher pieces, and a train/test mismatch against a synthetic corpus built at 2-4 measures. Also open: `MEASURES_PER_STRIP` is not enforced — the sliver-merge checks the width cap but not the measure cap, so 13 of 3,168 strips carry 4-5 measures. | **RESOLVED 2026-07-29 — the constants are NOT the lever; see the row below** |
| 2026-07-29 | **The windowing constants STAY (`MEASURES_PER_STRIP = 3`, `MAX_STRIP_W = 1450`); the retune found two BUGS instead.** The usable-yield sweep that pointed at 1 measure/window was scored on the wrong quantity: yield improves monotonically as windows shrink because it cannot charge for the near-empty crops shrinking creates, and those carry 20.8% of exam corrections. Re-scored with that cost included, 1 measure/window takes the healthy band **81.6% -> 60.4%** and near-empty crops **8.0% -> 33.9%**. Measure count is a weak proxy for the budget anyway — width explains R² 0.54 of a strip's token count, and **8.9% of single measures blow the 59-id budget alone**, which no `MEASURES_PER_STRIP` can fix. A budget-aware packer (`OMR_WINDOW_MODE=budget`) was built and decoded head-to-head: a **wash** (healthy band 75.8% vs 75.7/76.2%, bad-crop proxy 14.4% vs 14.5/14.0%), worth only +16 usable strips for +1.6pp more near-empty crops, so it **ships OFF** like `drawThinSharps`. What WAS real: the measure cap was unenforced (13/3,168) and the width cap was violated 82/3,168 times by three separate paths (the `lead` prefix re-extending window 0 after the check, `_split_wide` gutter cuts overrunning, and the driver's pad added post-check). Both fixed, verified 13 -> 0 and 82 -> 0, measure coverage invariant across 458 rows. Numbers: [METRICS-SLICER.md](METRICS-SLICER.md). | LOCKED (measured) |
| 2026-07-29 | **The staff floats inside the strip frame so low beams are not cut off** (`place_band()`, `OMR_VPLACE=0` to disable). The frame gives 4.60 sp above the staff and only 2.60 below, but real music reaches 2.68 sp below at p90 and 3.01 at p95, so **11.6% of real staff rows lost content there** against 1.4% at the top. The frame height and the 30 px spacing are NOT touched — scale is the axis the model is sensitive to — only the split between headroom and underroom, per row, from that row's measured ink extent. Bottom clipping 11.9% -> 4.4%; cost is +5.3pp foreign ink at the bottom, -2.4pp at the top. ⚠ **Decoded A/B is NEUTRAL and underpowered** (bad-crop proxy 13.8% off vs 15.0/15.6% at two doses, with **no dose-response**, so noise at 326 strips). Kept on the geometric argument — a clipped beam is destroyed information a confidence proxy cannot measure — NOT as a measured accuracy win; that needs gold. ⚠ The '+1% vertical shift is free' result was measured at ~3 px and does **not** license a 39 px shift. | LOCKED (geometric; accuracy unproven) |
| 2026-07-29 | **Neighbouring crops no longer overlap: the right edge gives back what the next strip's left pad takes** (`TRIM_SHARED_EDGE`, `OMR_EDGE_TRIM=0` to disable). Before, `PAD_PX = 6` was applied on the left with no matching right trim, so 74.8% of mid-row strips shared 6 px with their predecessor. ⚠ **The double-count worry this was raised against is NOT real** — a notehead is 22 px and the band is 6 px, and on decodes the same note ends one strip and starts the next only **1.3%** of the time at overlapping boundaries against a **6.85%** within-strip null. The actual justification is pixel/label agreement: **0 of 421** labels start or end with `|`, yet real crops showed a closing barline **61%** of the time against **5%** for the synthetic strips the model trained on; the trim takes real to 22.5% (the residue is row-final strips, which have no successor). Decoded A/B on 16 pages is a **wash** (over-budget 9.5→9.2%, bad-crop proxy 14.4→13.8%, confident 31.9→30.7%), so this is kept for structural consistency, **not** for accuracy. Overlapping pairs 195 → 0, strips and measure spans unchanged. | LOCKED (measured) |
| 2026-07-29 | **A decode cache is keyed on the FULL windowing signature, not `measures_per_strip` alone.** The old key would have let a packing-rule change reuse crops sliced by different code — the same confound that spoiled the earlier n_ids comparison. `window_signature()` / `window_cache_ok()` in `page_to_strips.py`; every cache writer and staleness check uses them. | LOCKED |
| 2026-07-29 | **Colliding page stems are qualified with the makam; DUPLICATE page stems are dropped instead.** Strip dirs are keyed by page stem alone while page images are makam-scoped, so two pages sharing a stem silently overwrite each other. The two real cases needed opposite fixes: `bir_nigah_et_ney` is genuinely two songs (different composer *and* usul) → both stems qualified, both kept; `nesem_emelim_ney` is one neyzen upload filed under two makams (byte-identical PDF **and** PNG, same SymbTr match) → uzzal copy dropped. ⚠ Renaming the duplicate would have been the WRONG fix: it puts two copies of one page in the pool, and near-duplicates on opposite sides of a piece-level split are exactly the leakage the by-piece split rule exists to prevent. Discriminator is the match target (same symbtr = duplicate, different = collision), in `collect_tuplets.neyzen_stems()` over the whole match CSV, so future collisions self-resolve. A full scan of every page image and every match row found **exactly these two**. `emit_strip_labels.py` now hard-fails on a stem collision. Fallout: the ambiguous `bir_nigah_et_ney_p1` crops were deleted (provenance unrecoverable — both pages are 1653×2338) and 5 realval-hard verdicts on them are void. | LOCKED (measured) |
| 2026-07-28 | **RE-SLICE the real pools; the labelling pools and the frozen exam are OLD-SLICER output.** Strips date 2026-07-15..17; `page_to_strips.py` was overhauled 2026-07-25 and nothing was re-sliced. Re-slicing 5 queue pages: **0 of 30 crops identical**, 2 gone, old 207 px slivers now 1435 px full rows. Decided after the owner labelled all 130 hard strips and **43 (33%) were unusable crops**, leaving 87 against 110 needed — a re-slice was required regardless. Measured benefit is real but partial: slivers and very-low-confidence crops both fall ~4x (3.0% -> 0.7%), while the moderate band (min_logprob < -0.5) is unchanged at ~10%. ⚠ The 87 usable verdicts do **not** transfer, because no crop survives unchanged. ⚠ Exam v3 needs the same treatment; until then the one-shot instrument measures a retired slicer. Scope for the real-val rebuild is only the **158 val-side non-exam pages** (~18 min), not all 1,852. | LOCKED (owner decision) |
| 2026-07-28 | **The "2% pre-shrink" is NOT a finding — it did not replicate.** Exam: 562 → 475 edits (−15.5%), reproducible across four scale values. Real-val holdout: 247 → 243 (**−1.6%**). It is exam-specific. **Cause of the error: ~15 variations were run against the frozen exam and the best-scoring one was written up as a result, before any holdout was tested** — selection on the test set. No mechanism was ever found either: resampling (down-up 555), blur (562), ink lighten (565), ink thin (589) and staff-size matching (benefit present in every size bucket) were all ruled out. ⚠ Not fully closed — real-val is the easy pool and lacks the hard tier, so re-test after the rebuild. | ⛔ NOT A FINDING — do not act on it |
| 2026-07-28 | **Any inference-side or preprocessing win must replicate on a pool other than the exam BEFORE it is written up as a finding.** The pre-shrink episode cost a doc-sync cycle and put a false headline into six files. A holdout run is ~2 minutes; it goes first, not last. | LOCKED (process) |
| 2026-07-28 | **Do not patch `page_to_strips.py` without measuring the estimator first.** Two fixes were attempted this session and both reverted: the first added a forward-merge for leading slivers and was **dead code** (byte-identical output over 67 pages); the second assumed `MAX_STRIP_W` blocked the sliver merge, which the slicer manifests disproved (0 of 18 narrow crops were `split_wide`). Both diagnoses were inferred from the file rather than measured against its output. | LOCKED (process) |
| 2026-07-28 | **The `staff_jitter` augmentation change is insurance, not a fix.** Round 3 framed off-by-one note heights as too little geometry variation ("we shake five times less than reality"). The raw truth is starker — synthetic spacing has sd **0.000**, so we shake *not at all* before augmentation — but the dose-response ladder shows variance is not what costs edits today: perturbing made the model *better*, and doubling the perturbation changed nothing. Keep the op; stop calling it a fix. | LOCKED |
| 2026-07-27 | **The Round-1 sharp diagnosis is CONFIRMED but INCOMPLETE.** Removing the label noise killed the one-directional küçük→koma fallback exactly as predicted, and küçük-in-signature went 50 → 72%. What it exposed underneath is a SYMMETRIC koma↔küçük confusion (8× vs 7×, all inside the `\sig` block, net emission 0) — a discrimination failure, not a bias. | LOCKED |
| 2026-07-27 | **Signature-packed glyphs are the next lever, not inline ones.** The whole fidelity investigation (`sharp_probe`, bar weight, küçük pitch → 0.65 S) measured INLINE glyphs; signature glyphs are packed at `SIG_GLYPH_ADVANCE = 13 px` and were never examined, and 32 of the exam's 33 küçük tokens live there. | LOCKED (opens Round 3) |
| 2026-08-11 | **`\tupend` STAYS in the vocabulary, and tuplet closure is enforced at DECODE time instead.** The token is genuinely redundant — a group closes when the running sum of sounding durations lands on a plain power-of-two value, which `tupletGroupsIn` already computes — but removing it would regenerate every label in every pool including the frozen exam gold, mid-round, to save one token per group. Constrained decoding buys the same thing for nothing: forbid `\tupend` until the sum is plain, force it when it is, and a malformed group becomes unemittable. Unlike a stitch-time repair it also shows in the exam score, which reads token ids. [rung3/tuplets.md](rung3/tuplets.md) | LOCKED (owner raised it, 2026-08-11) |
| 2026-08-11 | **A `\tup3` group with ≠3 noteheads is a review GATE, never an auto-repair.** The "3" counts units of time, not noteheads: two units merge into one longer note (a 32nd + a 16th is three units in two noteheads) and the corpus contains such groups. Wired up as a fixer, the count rule would corrupt exactly the fast 32nd-note passages the model already reads worst. The gate itself stands — it sends odd groups to a human. ⚠ This also retires the 2026-07-18 claim that all accepted groups are "exactly 3 closed notes". | LOCKED |
| 2026-08-11 | **The tuplet mark is redrawn as a BROKEN arc with the "3" set in the gap**, and the curved-arc share is raised above its current 70%. Real Turkish editions break the curve around the digit; we draw a continuous arc with the digit floating above it, so the only cue separating a triplet from a phrase slur in our corpus is a 13 px floating mark — while in real print it is a structural break. Same shape as the Bravura sharp-bar defect. Pixels only: the `\tup3` token is identical either way, so no label moves. | LOCKED (owner, from a real page). **BUILT 2026-08-12** to measured geometry — 16/16 real marks are broken, and the redraw lands on every measured quantity ([METRICS-DIAGNOSTICS.md](METRICS-DIAGNOSTICS.md)); share 70% → 90%. ⚠ **NOT A/B'd** — the recall claim is still a hypothesis, and the owner's judgement of the shape comes before any re-render. Two things measurement added: the digit is now **upright Times, not bold italic Georgia** (the italic measured 1.10 S wide against real print's 0.76, which ate the gap's clearance), and the arc's **stroke weight is deliberately left alone** even though real print draws it heavier — thickening only the tuplet arc would invent a thickness cue against phrase slurs, so it is owed jointly with `drawSlurArc`. **REVISED the same day on the owner's review of the sheet**: the mark is not one fixed shape at one height — **each arm's outer end clears its OWN end note** so the mark tilts with the contour, and the digit is **regular weight**, not bold. ⚠ The digit stays at MID-SPAN: sliding the gap toward the highest note reads right in a screenshot and is wrong (real print puts it at 0.49–0.50 of the span even on descending figures; sliding it degenerates into a stub arm). The owner's judgement of the shape is the gate this passed — the recall A/B is still unrun |
| 2026-08-11 | **The "every decode-proposed `\tup3` is a hallucination" finding (0 of 39 real) does NOT describe the live model.** It was measured on the pre-tuplet-collection checkpoint. `round2-stage2-best` has the opposite failure: precision 91.2% against a ≥70% floor, recall 83.8% against ≥85% — it misses triplets rather than inventing them. Quoting the old figure at the current model argued *against* a repair that the evidence actually supports. | **SUPERSEDED** — kept as history in [rung3/labeling.md](rung3/labeling.md) |
| 2026-07-26 | **Carry-mode pixels are fixed to the LABEL, not the other way round**: `SheetView` now draws a same-direction alteration BARE, like `sigTolerant` in the serializer and like real editions (the exam has 1 inline küçük in 352 strips). The alternative — labelling every drawn sign — would keep printing accidentals real pages omit. | LOCKED (owner decision) for the RENDERER; **NARROWED 2026-08-09** — the app's own sheet no longer takes the tolerance, see the Product row of that date. Corpus pixels and labels are unchanged |
| 2026-08-19 | **The scan profile does NOT stay on in Round 3's final model — `scan_share` is left at its 0.0 default.** Arm 1 was a null on the medium it was built for, and the case for keeping it was only "it did not hurt, and the born-digital point estimate favours it" — a choice made on two nulls. Every profile in the mix is a claim about what users upload, and `PHOTO_SHARE` was set from the owner's report that real uploads are mostly web screenshots (n=2 of real usage). Costs nothing to act on: the flag is off by default, so doing nothing carries the decision. ⚠ This does **not** retire the profile code — it stays available and its write-up stands. [rung3/scan-profile.md](rung3/scan-profile.md) | LOCKED (owner decision) |
| 2026-08-19 | **Arm 2 (one measure per strip) is DROPPED and does not take a Round-3 slot; the staccato arm takes its place.** Owner's objection, checked and upheld: the slicer already splits over-wide real crops at whitespace gutters (`_split_wide`, `MAX_STRIP_W` 1450, ~25% of crops), and what actually drops a strip is the **59-id label budget**, not width — 8.9% of *single*-measure windows blow it alone, so a measure rail cannot fix them. What arm 2 could still have bought was a *training* geometry match on the 32% of synthetic strips spanning 2+ measures; it cannot raise resolution at all, because the padding probe's ×1.00 baseline **was** the exam's 19.2 px. That is the same axis as three consecutive nulls. [METRICS-GEOMETRY.md](METRICS-GEOMETRY.md) | DROPPED (owner decision) |
| 2026-08-19 | **The "16 of 16 real marks break the arc" claim is REFUTED, and a THIRD tuplet mark style is added as a per-piece coin.** The owner produced two real scanned editions (Kemânî Sebuh / Sofyan; Avni Anıl / Düyek) drawing a **continuous** arc with the italic "3" set **inside the concavity** — between the curve and the noteheads. That is neither shape we draw: `drawTupletArc` breaks the arc around the digit, and `drawTupletArcLegacy` floats the digit *above* the arc touching its apex. The 16/16 probe missed it because **no labelled real strip in any pool carries this style**, which is also why nothing we own can score it. Disposition: draw it as a third style chosen by a per-piece coin (like bracket-vs-curved), justified on print evidence alone with **no recall claim** — exactly the 2026-08-12 redraw's footing — and land it in the FINAL model's render, never in arm 3's, which would make that arm unattributable. ⚠ Collecting real pages that use it is the other half; a shape absent from every val pool is untestable. [rung3/tuplets.md](rung3/tuplets.md) | LOCKED (owner, from real pages) |
| 2026-08-20 | **⛔ ARM 2 (STACCATO) RAN AND IT PASSES — the first Round-3 arm to move its primary.** The staccato-triggered false-dot rate goes **72.7% → 0.0%** (0 of 110 marked strips), paired **60–0** against its training control `r3-tupnew-stage2-best` (exact McNemar p = 1.7e-18) and **80–0** against the live model. Clause 2 (no-regression on real dots, easy+mid) **passes**: 65/71 kept against the control's 66/71, one discordant strip, p = 1. Clause 3 (reported, not gated) shows **no price** — every AEU and F1 cell level or slightly up, which is the outcome the slur distractor's precedent said to check for. ⚠ The arm reads the MARKED pool exactly as well as the unmarked one (99.1% exact, SER 0.0002 on both, the same single non-exact strip in each), so the marks became *invisible* rather than merely tolerated. ⚠ **What it does not establish**: the pool is our own rendered staccato, and **no labelled real strip carries one**, so generalisation to a real printed staccato is unmeasured — the same blind spot as the concave tuplet mark. ⚠ `STACCATO_RATE` remains chosen, not counted. [rung3/staccato-arm.md](rung3/staccato-arm.md) · [METRICS-UNSEEN.md](METRICS-UNSEEN.md) · [../src/vision/MODEL_EVAL.md](../src/vision/MODEL_EVAL.md) | RESULT (arm passed; the disposition below is owed) |
| 2026-08-20 | ⏭ **OWED, OWNER'S CALL: does `--staccato-noise` ride the FINAL model's render?** The arm passed, but an arm passing is not a decision to carry the flag — it is off by default, so **doing nothing leaves it out**, the same way `scan_share` was settled. The case for: it is the only Round-3 arm that moved its primary, at no measured cost. The case against, both real: the win is on **our own drawn mark** with no real-strip evidence behind it, and the final render already carries `--concave-tuplet`, so turning both on makes that render a two-variable change whose parts cannot be told apart later. ⚠ Whatever is decided, it is decided **before** the render, not after seeing the exam. [STATUS.md](STATUS.md) | OPEN (owner) |

## Real data, exams and measurement

| Date | Decision | Status |
|---|---|---|
| 2026-08-20 | **EXAM v3's 21 PAGES ARE RE-SLICED WITH TODAY'S SLICER, INTO THEIR OWN CROP ROOT** (owner: *"I want exam to be sound"*). The first cut of the `examv3` queue reproduced the crops already on disk, for consistency with the 46 graded pages; the owner opened it and found a **265 px crop holding one measure, notehead cut by the frame, beams outside it**. Cause: **no exam page has ever been re-sliced** (0 of 67 under `data/real/strips_v2`), which the 2026-07-28 row below already warned about — the warning did not travel into the exam-v3 plan. Re-slicing into `data/real/strips_examv3` (a separate root, because the emitter slices in place and `data/real/strips` is hardlinked into the FROZEN exam) took the queue from **297 hand rows to 214** and crops under 400 px from **24% to 4%**, the frozen exam's own rate. ⚠ 12 corrections typed against the first cut were carried onto the new crops as **pending suggestions** where a crop holds the same music (7 of 12), never as verdicts. | LOCKED (owner decision) |
| 2026-08-21 | **THE WHOLE EXAM IS REBUILT ON THE CURRENT SLICER** (owner: *"I can make it from scratch, it is okey. Just exam need to be okey."*), reversing the same-day plan to grow the exam on its existing crops. Reason: the exam had **never** been re-sliced, so 46 of its 67 pages measured a slicer no user gets. Cost was measured first, and at LABEL level it is far below the page-level headline: of the 326 frozen gold labels **43 agree with the fresh derivation and need no human, 178 come back as pending suggestions, 105 are lost** (88 re-packed, 10 in dropped crops, 7 ambiguous). The queue is **663 rows over 64 of 67 pages**, with 139 emitter-labelled strips. ⚠ Consequence to settle before the read: the rebuilt exam grades ~12 strips a page against 7.1, so it is HARDER, and the signed 75% floor was fixed against the easier instrument ([rung3/exam.md](rung3/exam.md)). ⚠ `strips_exam_v2_clean/` is untouched and stays the record of what Round 2 was measured on. | LOCKED (owner decision) |
| 2026-08-21 | ~~**THE TRAINING POOLS ARE RE-EMITTED WITH THE LIVE MODEL AS THE *HINT* ONLY — THE GATE STAYS ON A MODEL THAT NEVER SAW THEM**~~ ⛔ **THE SPLIT WAS OVERTURNED THE SAME DAY (see the row below); the exam half STANDS.** Original reasoning, kept because the trade is real: The emitter uses its checkpoint for two different jobs, and the owner's question — *"emitting them with a better model would make my correcting job easier"* — is right about one of them. **The hint**: `review_ui`'s edit box is a hybrid that takes `\sig…\sigend` from the label and **everything after it from the decode**, so decode quality is measured directly in keystrokes; `round2-stage2-best` is far better than `rung3-labeler` (a July throwaway, fine-tuned on 362 neyzen strips) and there is precedent — `build_realval_v2.py` re-decodes with it for exactly this reason. **The gate**: the `nd` disagreement check is the only *independent* test that a crop really holds the music its label claims, which is the test that catches span errors — and `round2-stage2-best`'s stage 2 trained on these very pools at 9× oversampling (1,523 + 395 + 148 strips), so its agreement is partly memory. A referee holding the answer key would wave through exactly the geometry errors B8 exists to fix. ⛔ **The exam is NOT re-emitted and its hint is NOT upgraded**: `round2-stage2-best` is the baseline column re-scored on the rebuilt exam, and seeding gold from the decode of a graded model anchors the key toward it — an error missed becomes "correct", and it is *that model's* error. The weak model's errors are uncorrelated with either graded model, so they are **noise, not bias**, and a one-shot read survives noise but not bias. Real-val keeps the good seed because it is the selection set, not the exam. | ⛔ SPLIT OVERTURNED 2026-08-21 · the exam half LOCKED |
| 2026-08-15 | **CROP GEOMETRY IS CAUSAL, and Round 3's next render is a GEOMETRY render** — the content work in `select_pieces.py` stays sequenced behind it. The padding probe (`crop_geometry_probe.py --make-padded`, pre-registered in [rung3/levers.md](rung3/levers.md)) widened exam crops with their own empty staff, leaving content and gold identical: edits/token rose **monotonically across all four doses**, +59% at ×2.00, CI excluding zero from ×1.50 on, and the **real-val holdout replicated steeper** (+61%). Both unpadded arms reproduced their recorded baselines exactly (the exam's to the edit, 562). This is the first of the six Round-3 pre-render checks to come back positive, and the first that is not about what we draw. ⚠ It shows lowering resolution **costs** edits; it does not show raising it **pays** — the 300-strip pilot tests that, and the short-crop hole is the failure mode it must watch. ⚠ A second look at a spent exam read; the Round-3 exam is still read once, on the final model. ⚠ **The ORDERING half of this row is superseded (2026-08-17)**: the pilot it sequenced ran and was **stopped by its own pre-registered rule**, and Lever 4 was promoted ahead of the content work the same day — so "the next render is a GEOMETRY render" no longer holds. The **causal finding is untouched**; only the render order moved ([rung3/levers.md](rung3/levers.md)). | LOCKED (finding) · ordering superseded 2026-08-17 |
| 2026-08-15 | **BUILD THE STACCATO DISTRACTOR, and trade DURATION OVER PITCH.** Owner decision, after the concern below was raised and reaffirmed. The model reads a printed staccato as an augmentation dot; measured with a paired control, it decodes a dot gold does not have on **72.7%** of marked strips against **0.0%** on the identical music unmarked. Cause is structural: `ADDED_TOKENS` has no articulation token and the renderer draws none, so 0 of 40,826 strips carry one and every dot the model has seen meant *longer*. Built as `--staccato-noise`, **off by default**, following `drawSlurArc` — pixels only, arms' manifests byte-identical, `verify-labels` PASS. Pre-registered floors: (1) the false-dot rate must fall; (2) no regression on real dots **on EASY+MID tiers only** — hard-tier dropped dots are scan degradation (7 of 12, `nd` to 1.14) and are excluded in advance, not after a result; (3) pitch/AEU F1 reported, not gated. ⚠ The concern raised and overruled: dot-**lost** outnumbers dot-**added** 12:4 on the exam, and the slur distractor bought precision 15.1%→91.2% at recall 92.7%→**83.8%, below its floor**. The owner's answer is that the exam's dropped dots come from bad scans and that hallucinated length is the costlier error — clause 2 is what holds that to account. ⚠ **FLAGGED 2026-08-18, NOT CHANGED (the row is LOCKED and clause 2 is signed):** clause 2's stated reason uses `nd` as a scan-quality measure and `nd` is not one — it is `lev(label, decode)/len(label)`, a label-vs-decode disagreement ([METRICS-CORPUS.md](METRICS-CORPUS.md)), so the justification is circular as written. The exclusion may still be correct (hard tier is defined independently of `nd`), but the reason needs replacing **before the arm is scored**, not after. Owner's call — carried in [STATUS.md](STATUS.md) item 1h. | LOCKED (owner decision) · clause 2's rationale flagged 2026-08-18 |
| 2026-07-11 | **Emitter-first, one big Round-1 run**: collect both sources (neyzen + notaarsivleri), then ONE fine-tune on both engraving styles — don't split what can be one run. | LOCKED (owner decision) |
| 2026-07-13 | Insert a **throwaway Round-0.5 labeler fine-tune** before the nota emit; Round 1 itself still trains from base weights. | LOCKED |
| 2026-07-12 | **Never auto-accept an accidental-class disagreement** between label and decode — those always go to a human. | LOCKED — avoided 187 headline-class poisonings |
| 2026-07-20 | **Ship criteria are pre-registered before training**, with every floor stated next to its measured baseline. | LOCKED |
| 2026-07-20 | **Ties carry no floor** (their ground truth is ~38% structurally noisy — gating on it would measure the labels); replaced by the arc-triggered false-`\tup3` rate ≤10%. | LOCKED (owner decision) |
| 2026-07-20 | **The exam is read ONCE per round**, on the final model; all iteration on real-val; a miss is never silently re-rolled, and any second read is labelled as leaked. | LOCKED |
| 2026-07-21 | **Round 1 runs FIRST; the additive-only re-slice moves to Round 2.** The current labeler is neyzen-only while the re-slice target is nota-dominant, so re-slicing first would manufacture false review disputes. | LOCKED (reversed the 2026-07-20 "re-slice starts first" ordering) |
| 2026-07-20 | **The exam is never touched mid-round** — its 27 over-budget recoveries are deferred to exam v3. | LOCKED |
| 2026-07-21 | **Exam-piece phone photos are EXAM-ONLY, never training.** | LOCKED |
| 2026-07-22 | **Standing rule: real-val ORDERS candidates, it does not PREDICT exam performance** (28pp gap measured). | LOCKED |
| 2026-07-22 | Every-share sweep **CANCELLED before any run** — the largest available intervention moved the metric 0.5pp, and the pathology it targeted was already fixed. | **DROPPED**, with a replacement power criterion (>0.5pp) for any future sweep |
| 2026-07-23 | **Ship Round 1 as "an improvement, not a pass"** — it missed 5 floors but strictly dominates the previous live model, and keeping the worse model live would hurt users. | LOCKED |
| 2026-07-23 | **Pivot trigger** (auto-switch to the correction-loop if the Round-2 exam closes < half the gap). | **DROPPED by the owner** — inherited an unproven premise and keyed a six-floor decision on one metric; the pivot stays a situational call |
| 2026-07-23 | Round-2 plan-review addenda items **6 and 8 dropped**; items 5/7/9 kept as commitments. | LOCKED |
| 2026-07-23 | **Train-time exam-disjointness guard**: `train.py` refuses to start if any `--real-dir` piece shares a SymbTr id with `testset.json`. Emit-time guarding was not enough — contamination re-arms whenever a pool grows. | LOCKED, shipped |
| 2026-07-23 | **The real-val rebuild need NOT be edition-disjoint** (clean tiers agree within 2pp); it must instead match exam *composition*, and exclude decode-derived labels from the metric pool. | LOCKED |
| 2026-07-25 | **Stop hand-labelling photos at 284 strips** — enough to measure, and many photos are unreadable even to a human. | LOCKED |
| 2026-07-25 | **Drop the "invented mark" (context-blind hallucination) work** — the flat family now scores 89–92%; the weakness moved entirely to the sharps. | DROPPED, kept as a note |
| 2026-07-25 | **Exam v3 must floor or weight the per-class mean by n** — one 3-gold class swung the headline ~11pp. | LOCKED, owed |
| 2026-07-27 | **Ship Round 2 as an improvement, not a pass** — the macro headline still fails (74.2% vs the ≥85% bar) but its −4.1pp move is `\komaSharp` at n=14 inside a six-class mean; on every low-n-robust measure (micro recall, macro≥30) it beats `round1-best`, ties on micro F1, and improves SER and exact-match. Same reasoning as the Round-1 ship: the rule exists to block *worse* models. | LOCKED |
| 2026-07-27 | **A gate strip is never swapped out to make the browser gate read clean.** The one failing strip stays in `GATE_STRIPS.txt` so the ORT-web int8 wobble keeps being measured. | LOCKED |
| 2026-07-28 | The `realval-hard` queue is ordered **most-confident-first**, and **every row is read**. | **OVERTURNED 2026-07-29 by the owner** — see the next row |
| 2026-07-29 | **The labelling queue is ordered WORST-FIRST, and the run may stop early on a sampled check.** The calibration decides it: the decode is already exactly right 80% of the time above `min_logprob` −0.1 and 4% below −1.0, so the confident head is mostly the reviewer confirming what is already correct while nearly every correction sits in the tail. Stopping is gated on ~20 rows drawn *at random* from the remainder, judged on whether the errors **cluster by kind** (scattered noise handicaps all candidates equally; a single repeated confusion systematically punishes the model that fixes it) — never on the ordering alone. Rows accepted unread carry `by=tail-accept`. | LOCKED (owner decision) |
| 2026-07-31 | **The ~2% pre-shrink is DEAD — do not re-propose it.** It removed 15.5% of exam corrections, failed to replicate on the old real-val (−1.6%), and its last defence was that real-val lacked a hard tier. Re-run on `_realval_v2` (41% hard, SER 0.079 — harder per token than the exam) it is **worse at every scale**: +2.7% at 1%, +5.2% at 2.5%. The sign reverses off the exam. The original was selection on the test set after ~15 variations; no mechanism was ever found. | **DROPPED**, measured twice |
| 2026-07-31 | **`_realval_v2` is the selection set; `make_realval_pool.py`'s `_realval` is only its input.** Pointing an eval at the base silently restores the pool with no hard tier — the exact defect the rebuild removed — and it fails silently, since the number still looks like a real-val number. | LOCKED |
| 2026-08-13 | **Round 3's acceptance bar is the PRODUCT number, and it is the public-launch gate: ≥75% of exam pages needing ≤5 corrections** (Round-2 baseline 57%). The accidental measures become no-regression clauses at their Round-2 values rather than targets, because Round 3 aims at pitch (40% of user edits) and duration (28%) — the macro AEU mean cannot see either, and two rounds already went into the 13%. ≥90% stays the standing product goal but is not reachable as a one-round floor from 57%, and a floor nobody can clear stops discriminating. Below the bar → Round 4, and the link stays with the two friends. Written before the corpus was rendered: [rung3/round3-criteria.md](rung3/round3-criteria.md) | **LOCKED — signed by the owner 2026-08-15**, unchanged from what was proposed on 2026-08-14. Not re-opened after the exam is read |
| 2026-08-18 | **The LilyPond arm is pinned to the corpus's staff geometry, and the label — not LilyPond — decides every accidental.** The arm renders at the corpus's exact staff size (336 px strips, 30 px spacing) so the ENGRAVER is the only variable; the consequence is that Lever 4's "spacing SD is zero" premise is untested by it, and varying document scale is a separate, engraver-neutral change. On the accidentals, `\accidentalStyle "forget"` plus a forced `!` on every marked note and the signature's own alteration on every bare one makes LilyPond's accidental engine unable to add or drop a sign — pixels == labels is then provable rather than hoped for, and `verify-labels-ly.ts` proves it per strip. ⚠ The `\time` per measure comes from `deriveTimeSignature`, NOT from a `beatStructure` override: usul beam grouping is quarantined on the VexFlow side, and importing it here would make the arm a second variable instead of a second engraver. [METRICS-ENGRAVER.md](METRICS-ENGRAVER.md) | LOCKED |
| 2026-08-15 | **The redrawn tuplet mark is KEPT on a NULL A/B, and no recall claim is made.** Both arms trained on the identical recipe; `\tup3` recall 88.9% (new shape) vs 85.2% (control) on `_tupletval` — 2 net groups of 54, paired 4-vs-2 discordant, **exact McNemar p = 0.688** against a pre-registered ~6-discordant threshold. The null branch of the decision rule applies as written: the shape stands on the print measurement (16/16 marks, ~11 editions), which is its own justification, and the experiment's contribution is only that any gain is smaller than 54 groups can see. `strips_v5_tupnew` is the corpus Round 3 continues from; neither arm ships (the exam is unread and neither is a Round-3 model). Next lever, alone: the 35% slur-distractor rate. ⚠ Also settled by the control: `staff_jitter` + the rasterizer drift + a new training environment moved this class by **zero** (control 85.2% = `round2-stage2-best`'s 85.2% on the same pool) — which could only be known by running it. | LOCKED |
| 2026-08-13 | **The tuplet A/B tests the mark's SHAPE ONLY.** Both arms render at the new 90% curved-arc share and with print realism (`printseed`) OFF, so the corpora match the shipped `strips_v4` recipe apart from the mark itself. Two reasons, both about attribution: moving the arc share alongside the shape would make a win unattributable to either, and today's `render.ts` sent `printseed` unconditionally, which would have dragged the measured-as-unvalidated `USUL_BEAM_GROUPS` into ~40k strips as a side effect of a tuplet experiment. `printseed` is opt-in from now on (`--print-noise`). Selection is ONE pre-registered number (free-running `\tup3` recall on `_tupletval`), paired McNemar, minimum resolvable effect ~±8 pp at n≈67 groups; a null keeps the redraw on realism grounds and claims nothing. | LOCKED (owner, 2026-08-13) |
| 2026-07-29 | **Labelling queues are versioned per re-slice, and each queue resolves images against the crops it was built from** (`QUEUE_IMG_ROOTS`). Strip filenames survive a re-slice but the pixels do not — 59 of the v2 candidates reuse a v1 filename and 129 of 165 also exist under the old root, so a single global search order would have shown the reviewer last week's pictures against this week's rows, silently. | LOCKED |
| 2026-08-20 | **`--staccato-noise` RIDES THE FINAL RENDER.** The open disposition from the passed arm is taken: the final model's render carries **both** `--concave-tuplet` and `--staccato-noise`. The two-variable objection is answered rather than ignored — (a) clause 3 of the arm's pre-registration measured **no price** (every AEU and F1 cell level or slightly up), so this is not a trade being made blind; (b) neither flag is an *experiment*: the concave mark rests on print measurement with no recall claim and the staccato draw on a passed arm, and the final render is not an arm; (c) ⭐ the staccato claim stays attributable whatever else moves, because it keeps its **own** paired instrument (`_staccato_falsedot_ctl` / `_staccato_falsedot_stac`) — the asymmetry [rung3/levers.md](rung3/levers.md) wrote down for exactly this moment. ⚠ What is genuinely given up: a *general* movement in the final model cannot be attributed to one flag. That was already true at ±12 points of exam precision. [rung3/staccato-arm.md](rung3/staccato-arm.md) | LOCKED (owner, 2026-08-20) |
| 2026-08-20 | **THE DOTTED (USUL) BARLINE IS PROMOTED INTO THE FINAL RENDER, AND IT IS DRAWN LABEL-FREE.** Two decisions in one row. **Promotion:** [BACKLOG.md](BACKLOG.md) item 5 held it as "owed, not next" behind the render-slot rule; the staccato arm's read has happened, and the same-shape argument is now backed by a trained result rather than an analogy — a **hole responds** (72.7% → 0.0%), a **domain gap does not** (three nulls). It is also the only item on the list the owner is currently paying for by hand: **~1 in 5** of `batch3`'s corrections so far is deleting a false `\repstart`. **Label-free, not a token:** giving it a name would make every existing real gold label silently wrong, because no pool we own annotates one — a label-free draw is *consistent* with every label on disk and therefore costs **zero new labelling**, which is also why it does not delay the render by a labelling cycle. It is exactly the mechanism that made the staccato arm work. ⚠ Consequence accepted on purpose: the model will read a dotted barline as *nothing* rather than reproducing it. Emitting one is a Round-4 question, and `ADDED_TOKENS` stays append-only for it. ⏭ Cheap first step before the render: probe the real pools for how often print actually draws one — 7.8% is a statistic about the model's guesses, not about print. [METRICS-UNSEEN.md](METRICS-UNSEEN.md) | LOCKED (owner, 2026-08-20) |
| 2026-08-20 | **EXAM v3 IS BOUNDED: label the 21 pages we already own, then STOP at 67 graded pages.** [rung3/levers.md](rung3/levers.md)'s power note offered two honest responses to a ±12-point primary; **both are taken** — option 1 (grow before the read, keeping the one-shot rule) in a bounded form, and option 2 (report the interval beside the result) on top of it. The bound is the argument: those 21 pages belong to pieces **already** banned from training, so they are free in training-data terms, while every page past 67 costs a piece **and** hand labelling to buy precision that improves only as √n. ⚠ **The decisive number is not the error bar but what it takes to demonstrate a pass**: against a 75% floor a model must *measure* ~86% at 46 pages and still ~81% at 200, so no affordable exam makes a near-boundary call crisp — that is an argument for the interval, not for buying pages. ⚠ **Binding side condition: re-score `round2-stage2-best` on the grown exam** or the signed floors stop being apples-to-apples (one CPU decode, not a retrain). The floors themselves are **not** re-opened — growing the exam changes precision, not the bar. [rung3/exam.md](rung3/exam.md) · [METRICS-EXAM.md](METRICS-EXAM.md) | LOCKED (owner, 2026-08-20) |
| 2026-08-20 | **THE FINAL MODEL SAVES TWO CHECKPOINTS AND CHOOSES BETWEEN THEM ON REAL-VAL.** `best` is currently selected on a val loss that is **94.6% synthetic** ([BACKLOG.md](BACKLOG.md) item 3), in a round graded on real pages; the fix was deferred because changing the selector mid-round makes the arms incomparable. **The arms are now read**, so the deferral's reason has expired for the final run only. Rather than swapping the selector (a second uncontrolled change), the final run keeps the current one **and** additionally keeps a checkpoint selected on a free-running real metric (edits/strip), and the two are compared on **`_realval_v2` before the exam is read.** That is legal by the standing rule and not a loophole: real-val is the selection set and the exam is one-shot — selection has always happened there. ⚠ It does **not** re-open Round 4's recipe work (EMA, label smoothing, a second seed, encoder LR), which stays in [BACKLOG.md](BACKLOG.md). | LOCKED (owner, 2026-08-20) |
| 2026-08-20 | **COLLECTION IS TARGETED, NOT BROAD — a narrowing of the 2026-08-17 "collect broadly" call, not a reversal.** Broad collection stands as permitted; what is *scheduled* is two targets, because **2,486 unlabelled page PNGs already sit on disk** and volume does not relieve a labelling bottleneck. The two things volume cannot substitute for: **(a)** pages drawing the **concave tuplet mark**, which no labelled real strip in any pool carries, so nothing we own can score it; **(b)** **tuplet-dense instrumentals** — sirto, longa, saz semaisi — which are a *measured* structural hole, not a hunch: 39.4% of triplet-bearing single measures blow the 59-id budget alone, 80.5% of 2-measure and 92.9% of 3-measure windows, so that repertoire is excluded from training **and** the exam by construction ([rung3/labeling.md](rung3/labeling.md) §1c). ⚠ **Collecting it does not by itself fix it** — the same budget drops the new pages too; the ceiling measurement below is the unlock, and collection without it buys drops. Candidate sources beyond neyzen.com and notaarsivleri.com, recorded so they are not re-searched: nota.trt.net.tr (official TRT), sahaney.com (**filterable by makam and form**, which is what makes it useful here), erdincbal.com, neyzenim.com, devletkorosu.com, sarkilarnotalar.blogspot.com, baglamaci.com. ⚠ Two rules do not bend: read each source's licence before redistributing ([THIRD-PARTY.md](THIRD-PARTY.md)), and any flow feeding training keeps **refusing exam pieces**. | LOCKED (owner, 2026-08-20) |
| 2026-08-20 | **PLAIN ENGLISH IS THE DEFAULT FOR EVERY REPLY, not only for docs.** Owner instruction, recorded in [../CLAUDE.md](../CLAUDE.md) as *How to write to the owner*: short sentences, jargon defined on first use, an analogy per mechanism — and **numbers kept with their n**, because plain English means simple words and not vague claims. It changes the voice of a reply, not the doc conventions and not [MAINTAINING.md](MAINTAINING.md). | LOCKED (owner, 2026-08-20) |

| 2026-08-21 | **ONE MODEL FOR THE TRAINING RE-EMIT: `round2-stage2-best` DOES BOTH JOBS. The split decided hours earlier is cancelled** (owner: *"Cannot we basically use last model for everything?"*). What settled it is a number that had not been looked at: the gate model does not only accept and reject strips, it **aligns rows**, and a row it cannot align throws away every strip on it. The weak referees dropped **10,695 strips as `row_unaligned`** — 4,467 in `strips_nota` (37% of rows), 5,540 in `strips_tup` (47%), 688 in `strips_r1` (47%) — against a real training set of **2,330 accepted strips**. The referee's dominant effect is **yield, not precision**, and the earlier decision optimised the small side of the trade. ⚠ The memorisation objection also shrank on inspection: reproducing the OLD label on a re-cut crop mostly raises `nd` and sends the strip to **review**, so it costs yield rather than correctness; the genuinely dangerous case is narrower — a **truncated** crop whose old version held the full span, where memory and label agree and nothing catches it. ⚠ **Guard, and it is not optional**: the emitter's seeded audit sample (`--audit-frac`) measures the escaped-bad-label rate directly, and it must be **read**, not merely produced — exam v2 sampled 2 of its 63 auto-accepts and a later full read found **51%** wrong. ⚠ **This is a lead, not a measurement**: rows can also fail to align on repeat/fold structure rather than weak reading, which the re-emit itself will separate against the numbers above. | LOCKED (owner decision) |
| 2026-08-21 | **THE EXAM'S SIGNATURE LABELS ARE NOT TRUSTED, AND THE OVERRIDE RULE IS RE-OPENED** — found by the owner while reading `examv3-full`, from the observation that the fixes were "mostly `\komaSharp` → `\kucukSharp`". All 7 such fixes sit inside `\sig … \sigend`, on the same note, in **2 pieces of one makam (mahur)**. Cause: the key signature is the ONE part of a label not derived from SymbTr — the model reads it off each row-start strip and the **majority read overwrites the derivation** (`emit_strip_labels.py`). The rule has a real justification (editions print the makam's conventional signature, not the content-derived one), but the voter is the weak `rung3-labeler`, its koma/küçük confusion is **systematic** so it wins the vote unanimously, and the `nd` gate is **blind to signatures by design**. Reach: the model overwrote the signature on **24 of 45 exam pieces (53%)**, 406 of 938 `strips_nota` pieces (43%), 98 of 293 `strips_tup` (33%), 26 of 65 `strips_r1` (40%); **8 of the 36 exam pieces carrying a signature disagree with our own makam table's majority variant**. ⚠ **A loop**: the misread becomes the label, the label trains the model, the model misreads harder — a plausible partial cause of the 9:1 `\komaSharp`:`\kucukSharp` imbalance. ⚠ **Not claimed**: n=7 in one makam is a signal, not an error rate for the override; many overrides are probably right; the makam table is a guide (mahur genuinely prints both ways, küçük 35 : koma 17). ⏭ Measurement and the proposed rule change — never let a model overwrite the table, send row-starts to review instead — are [BACKLOG.md](BACKLOG.md) item 9. [METRICS-CORPUS.md](METRICS-CORPUS.md) | OPEN (finding recorded; rule change owed) |
## Working practice

| Date | Decision | Status |
|---|---|---|
| 2026-07-15 | **Heavy compute goes to Colab** (or `nice -19` + `OMR_ORT_THREADS=2` locally) — this Mac is a fanless M4. Runs are page-cached and resumable. | LOCKED |
| 2026-07-04 | One canonical status section; every other doc points at it. | SUPERSEDED 2026-07-26 — the canonical file is now [STATUS.md](STATUS.md), not ROADMAP §7 |
| 2026-07-26 | **Docs restructured for agents**: one auto-loaded `CLAUDE.md`, one status file, one metrics file, history append-only in `log/`, superseded plans quarantined. | LOCKED |
